---
name: marketing-playbook
description: 營銷方案生成器（**強制流程 skill，違反＝本次使用失敗**）。商業對接後把客戶情況餵進來 → 產出一份完整 Word 交付文檔（現狀分析 + 策劃方案 + 預算 + 行動清單）。**啟用後第一件事必須是執行 §0 門禁（13 項一次問全）並輸出《任務規則表》等用戶確認 —— 確認前不得產出任何方案內容；交付前必須原樣輸出 12 項自檢單。** 若用戶已給足資料，仍須先出《任務規則表》確認再動筆。觸發場景：見完客戶要出方案、接到新案子、要診斷客戶卡點、要比稿。
agent_created: true
規範依賴: AI協作總規範 §2.1 硬規則 · §2.4 寫文檔前三問 Gate · §2.6 語言約定
---

# 營銷方案生成器

## 📍 第 -1 步：你要做什麼 → **只讀這幾份**（本 skill 唯一導航）

> ⛔ **本表是全庫唯一的導航來源。** `README.md`／`AGENTS.md` 裡的檔案地圖只是**給人看的鏡像**，**與本表衝突時以本表為準**。
> **一次最多同時打開 3 份檔。** 本 skill 是為「按需載入」設計的 —— **通讀 = 讀不完 + 抓不到重點**。

| 你現在要… | 只讀這些 | 明確**不要**讀 |
|---|---|---|
| **第一次接入／安裝到某平台** | 本檔（SKILL.md）全文 ＋ `AGENTS.md` | — |
| **接了一個新案**（拿到客戶資料那一刻） | `references/08-操作流程SOP.md`（S0–S8 逐步：輸入／命令／產出／校驗） | 所有 `cases/` |
| **動筆前出骨架** | `references/00-打法库.md`（§0 總表一頁速查 → 挑中的條目） | `cases/46–51` |
| **寫方案正文** | `references/01-接案输出模板.md`（逐章）＋ `references/03-方法论操作手册.md`（挑 ≥3 個模型）＋ `references/07-质量范式-便利店开学季案.md`（**深度標尺**） | `cases/46–51` |
| **找同行參照案例** | **先查下方〈反向索引〉拿到檔號** → `references/cases/{檔號}-{檔名}.md`（進檔先讀「案例清單」，再跳「深度拆解」） | **其餘 44 個行業檔**、`cases/46–51` |
| **選乙方／看某機構做過什麼** | `references/cases/46–51`（機構與方法論類） | `cases/01–45` |
| **多 Agent 分工** | `references/02-多Agent分工简报.md` | — |
| **交付前風險自檢** | `references/04-失败归因总库.md` | — |
| **客戶是小企業／新品牌** | `references/05-小企业与新品牌从零打造.md`（**這是主入口**，不是補充） | — |
| **要用平台官方模型** | `references/cases/51-本土数字营销与MCN.md` | — |
| **判斷某件事該外包還是自己幹** | `references/06-选流派矩阵与对照表.md` | — |
| **想看成品長什麼樣** | `references/范例/便利店开学季战役-交付稿.md`（＋同名 `.docx`） | `-原版.md`（僅供對照） |
| **維護／擴充本 skill** | 本檔 §七 ＋ `references/08-操作流程SOP.md` §S8 | — |
| **改腳本** | `scripts/` 對應腳本檔頭註解 ＋ 改完跑 `python scripts/smoke_test.py` | `references/` 全部 |

**⛔ 執行時明確不要讀**（讀了浪費上下文，還會誤導）：
- `README.md` —— 給**人**看的說明書，執行方案時不需要
- `AGENTS.md` —— 只在**接入新平台**時讀一次
- `references/cases/` 裡**與客戶行業無關**的檔（50 個檔只有 1 個跟你的案子有關）
- `references/cases/46–51` —— **只有要選乙方／引用方法論時才讀**，跟行業案例無關
- `references/范例/` 內的 `-原版.md`、`references/cases/*` 的「本行業對接實測清單」（與 SKILL §五 重複）

**不確定讀哪個 → 讀 `references/08-操作流程SOP.md`。** 它把每一步該讀什麼、該跑什麼命令都寫死了。

### 🔎 反向索引：拿到客戶 → 直接查檔號（**不要在 45 個檔名裡猜**）

> 這張表存在的唯一理由：第 -1 步原本只寫「你客戶所屬的那 1 個檔」，**Agent 得自己列 45 個檔名再猜**——
> 這才是「找不準」的真因。查到檔號後，路徑是 `references/cases/{檔號}-{檔名}.md`。

| 客戶是什麼 | 檔號 |
|---|---|
| **吃 · 喝** | 餐飲／茶飲／快餐／中式連鎖 **01**｜現製茶飲咖啡烘焙 **27**｜餐飲供應鏈與調味品 **39**｜預製菜與方便速食 **40**｜飲料乳品酒零食 **02**｜農業生鮮與供應鏈 **29** |
| **穿 · 美 · 戴** | 服飾運動戶外 **04**｜美妝個護 **03**｜珠寶鐘錶 **32**｜箱包皮具配飾 **33**｜眼鏡與視光 **31**｜香氛與氣味 **34**｜奢侈品與高端消費 **17** |
| **住 · 行** | 家居建材家裝 **13**｜汽車與出行 **06**｜酒店與度假 **18**｜物流與快遞 **45** |
| **零售 · 渠道** | 零售商超與便利店 **14**｜二手與循環消費 **24**｜跨境電商與出海 **20**｜文具文創與辦公 **41**｜母嬰與寵物 **12** |
| **人 · 服務** | 教育文旅與本地生活 **15**｜健身與運動健康 **36**｜口腔與專科連鎖 **38**｜銀髮經濟與健康養老 **23**｜寵物服務 **28**｜婚慶與人生儀式 **43**｜攝影與影像 **42**｜招聘與雇主品牌 **44**｜醫療美容與輕醫美 **37**｜醫藥健康與器械 **11** |
| **科技 · 工業 · 能源** | 3C 數碼與家電 **05**｜互聯網平台與 APP **07**｜遊戲與泛娛樂 **08**｜AI 產品與營銷 **09**｜企業服務與 SaaS **21**｜B2B 工業品與區域品牌 **10**｜新能源與綠色消費 **22**｜美容儀與個護小家電 **35** |
| **內容 · IP · 賽事** | 潮玩文創 IP **25**｜影視綜藝與內容 IP **26**｜體育賽事與運動俱樂部 **30** |
| **公共 · 區域 · 金融** | 城市與區域公共品牌 **19**｜金融與保險 **16** |

**查不到精確匹配時**（如「社區藥店」「眼鏡批發商」）：
1. 先找**最近的 1 檔**（藥店 → **11** 醫藥健康與器械；眼鏡批發 → **31** 眼鏡與視光），
2. **在方案裡明說檔位不完全對應**，並補一句「差異在哪」——
   ⛔ **不要因為不完全匹配就不引用**，也不要硬套（引錯比不引更傷）。

---

## ⛔ 停 —— 執行者先讀完這一段（任何情況下不得跳過）

**你不是在「參考」這份文件，你是在執行一套流程。** 四條不可談判：

**0. 動手前必讀質量標尺：`references/07-质量范式-便利店开学季案.md`。**
   它定義了「做到什麼程度算夠」——**83 項量化指標 + 16 個骨架模板 + 每個角色的產出規格**。
   **不讀它 = 不知道深度標準 = 必然產出空殼。** 這是執行失敗率最高的一環（最常見的症狀是：產出看起來完整，但沒有一個判斷站得住）。

1. **不得先寫方案後補流程。** 門禁沒問完、任務規則表沒經用戶確認 → **一個字的方案內容都不准寫。**
2. **每一輪回覆都必須以一行《流程狀態》開頭**（格式見下）。**缺了這一行，代表你已經跑偏。**
3. **發現自己跳過了任何一步 → 立即停手，回到該步重做**，並在回覆中明說「我發現跳過了 X，現在回到該步」。

### 每輪必須輸出的狀態行（固定格式，純文字，任何平台都能輸出）

```
【營銷方案 · 流程狀態】門禁 13 項：{已問全數}/13（缺：{逐項列出}）｜ 任務規則表：{未出 / 待確認 / 已確認}｜ 當前階段：{收集中 / 分析 / 分工生成 / 裁決 / 自檢 / 已交付}｜ 減法自檢：{未做 / 已做（主線：{一句話}｜反對意見：{有／無}）}
```

**為什麼要這一行**：它把「有沒有遵守流程」從**承諾**變成**可被外部檢查的事實** —— 用戶看一眼就知道你走到哪一步、有沒有跳步。沒有它，所有「我會遵守」都是空話。
> 末欄「**減法自檢**」只在進入《自檢／已交付》階段時填：**主線**能不能壓成一句話、**有沒有**一條反對客戶既有判斷的意見（見 §三 減法規則第 3、4 條）。這兩件無法機械校驗，但正是決定方案成敗的兩件。
> 非營銷任務時，本行不輸出。

**最常見的失敗方式**：執行者把本文件當「靈感庫」而非「驗收標準」—— 跳過門禁、跳過自檢、用自創的簡化檢查替代本文件的清單。**本文件的清單是唯一合法的檢查標準。**

---

## ⛔ 最高優先級條款（P0 · 優先於本文件一切其他章節，含下方協議 1–8）

> 這四條由 2026-09-17 一次真實返工事故反推（一個校園大賽項目書 1.5 萬字**重做三次**）。
> **根因不是模型偷懶，是「默認以自己的結構生成全文」凌駕了「用戶給定的結構」。**
> 完整協議（6 條鐵律 ＋ 交付前自檢 8 項 ＋ 4 條結構性根因）見 `references/11-防返工交付协议.md`。

**P0-1 · 用戶給定的結構 > 本 skill 的一切默認結構與默認流程**
- 當用戶／主辦方／招標方提供了模板、大綱、目錄、提交清單時，**它的優先級高於本 skill 的八篇結構、門禁順序、自檢清單等一切默認**。
- 一級與二級標題**必須逐字沿用**：不替換成「更好聽」的說法、不合并、不拆分、不重排、不自造層級；模板自帶的板塊名（哪怕平淡）一個字都不動。
- 模板未覆蓋但必須有的內容 → 作為**新增同級／下級章節**補進去，而不是改造原有標題。
- 模板寫「僅供參考、可調整」時，**默認仍原樣保留**，除非用戶明確要求改。

**P0-2 · 用戶給的素材要「看得見地插進去」，不能溶解**
- 用戶給的大綱／章節清單，必須作為**文檔中真實存在的可見章節**出現，**保留原本的章節名與編號**（如「一、活動背景」「4.2 核心活動模塊」）。
- ⛔ 禁止把用戶大綱的內容打散、揉進別人的模板小節裡——那等於用戶的大綱消失了。位置可按邏輯排，但**章節名與編號要在正文裡看得見**。

**P0-3 · 「skill 自檢通過」≠「交付達成」**
- 本 skill 的自檢單（協議 3）通過，**只代表滿足了本 skill 的默認標準，不代表滿足了用戶的驗收項**。
- 用戶自定義的驗收項（主辦方格式、字數、匿名、雙格式、附件齊全…）**必須另行逐條核對，並原樣輸出結果**。
- ⛔ 禁止用「我們的自檢全綠」作為「可以交了」的證明。

**P0-4 · 形狀類任務先交 1 頁目錄，確認後再寫正文**
- 當任務涉及**結構／粒度／組織方式**（要不要用模板、用幾章、章節怎麼排）：**第一版只交一頁目錄**（只寫標題層級），附一句「確認目錄後我再填正文；你也可以直接改標題」。
- **收到確認前，禁止生成正文。** 目錄改一次是分鐘級，正文改一次是小時級。
- 唯一例外：用戶明確說「直接給完整版」。
- 一句話含**兩個約束**時（如「用官方模板＋也要有大綱十章」）：動手前**分別複述兩個約束**，確認都聽懂了再動手。

---

## ⚡ 強制執行協議（先讀這裡；違反任何一條＝本次使用失敗）

> 本節是硬約束，優先級高於本文件其他所有章節的「建議」。執行者（AI）必須機械式遵守，不得「參考精神」。

### 協議 1 · 啟動即立規（門禁答案釘死）
- 第一輪對話：按 §五 門禁清單**一次性**問全（映射到實際場景），同時確認「交付物定義」「**目標字數**（先問用戶；未指定時預設 **5,000–10,000 字**）」與用戶特殊要求。
- 問完後**必須產出《任務規則表》**：**13 項**門禁答案＋交付結構＋紅線＋工具路徑，請用戶確認；確認前不得產出任何方案內容。
- 用戶此後的**每一次糾偏 = 規則表升級**：先復述「規則表已更新為 …」並徵得確認再動手；禁止把糾偏當一次性修補。

### 協議 2 · 結構衝突時「合併，不取捨」
- 當用戶要求的文檔結構（比賽提交清單、客戶模板等）與 §三 八篇結構不一致：**默認把八篇內容按映射裝進用戶結構，一個硬要素都不許丟**（五要素行動、禁用詞口徑、預算能對上、KPI、人力檢查、假設驗證）。
- 映射規則：用戶結構的每個模組 → 對應八篇章節；八篇中用戶結構沒有對應位的內容 → 放入附件，不得刪除。
- **禁止「為了貼合用戶格式而砍深度」——這是本 skill 歷史上最大的翻車點。**

### 協議 3 · 交付前自檢單（必須原樣輸出在交付回覆中）
宣告交付前，**必須在回覆中逐項輸出下表並標 ✅/❌**；任何一項 ❌ → 回去改，不得聲稱完成：

| # | 自檢項 | 結果 |
|---|---|---|
| 1 | 門禁 13 項已問全（含目標字數）並寫入《任務規則表》 | |
| 2 | 未經證實的假設已在文首單獨標註 | |
| 3 | 文檔結構完整（八篇＋附件，或按協議 2 映射後無缺項） | |
| 4 | 字數達標（以《任務規則表》確認的目標字數為準；未指定時預設 5,000–10,000 字） | |
| 5 | 每條打法/動作五要素齊全（做什麼/誰/何時/花多少/怎麼驗收） | |
| 6 | 禁用詞與口徑章節存在，物料文案已逐字對照 | |
| 7 | 預算分項加總＝合計；引用數字全部有來源 | |
| 8 | KPI 可測（有基準值與觀測方式）＋決策節奏已寫 | |
| 9 | 執行人力檢查＋刪減順序已寫 | |
| 10 | 關鍵假設＋驗證方式＋Plan B 已寫 | |
| 11 | 無內部過程章節（事實底稿/多Agent分工/裁決記錄）洩入交付稿 | |
| 12 | 交付回覆中已附本自檢單 | |

### 協議 4 · 工具盤點先於路徑
- 需要讀取外部資料/平台內容時，**第一步先盤點**：已連接的連接器、MCP、現成 API。連接器可達 → 禁止用瀏覽器爬蟲硬繞（浪費用戶額度的最大來源）。
- 涉及付費生成（圖片/視頻/模型）→ 先報預估消耗並徵得用戶同意。

### 協議 5 · 交付形態紅線
- 對外交付一律簡體中文 `.docx`（除非用戶另有約定）。
- 內部過程文檔（事實底稿、Agent 分工、裁決記錄）**不得出現在交付稿中**——用戶要的是作品，不是工作過程。

### 協議 6 · 每輪輸出《流程狀態》行（**跨平台唯一的「強制力」來源**）
- **每輪回覆的第一行**必須是固定格式的流程狀態行（格式見本文件首屏）。
- 缺這一行 **＝ 已經跑偏**，用戶可據此直接指出，執行者必須補上並回到正確階段。
- 階段推進時同步更新：`收集中 → 分析 → 分工生成 → 裁決 → 自檢 → 已交付`。
- **為什麼這條最重要**：本 skill 是純 Markdown，任何平台都沒有強制執行機制。**把流程狀態變成用戶可見的固定輸出**，是唯一能在所有平台上生效的約束方式 —— 做不到就是沒遵守，一目了然。

### 協議 7 · 跨平台一致性（本 skill 要在任何 Agent 上跑通）
- **禁止依賴平台專有能力**（特定工具名、專有語法、特定 hook）。所有流程必須是**純文字可執行**的。
- 能力不足時按 `AGENTS.md` §四 的能力對照表降級，**降級不等於跳步**（例如派不出多 Agent 就順序扮演，但五個角色仍要一一產出）。
- 支持 hook 的平台（如 Claude Code／WorkBuddy）**可選**加裝硬約束，配置方法見 `AGENTS.md` §九；**但不得以「沒有 hook」為由降低執行標準**。

### 協議 8 · 卡死處理（**強制不得變成卡死**）

> **最高優先級是「用戶拿到東西」。校驗是保障，不是障礙。**
> 一個把人卡死的流程，比一個寬鬆的流程更糟。

**三種卡死與處置**：

| 情況 | 處置 |
|---|---|
| **同一項校驗連續 2 次不過** | **停止重試** —— 把「卡在哪／試過什麼／需要你決定什麼」攤給用戶，並給 2–3 個選項讓用戶選（改做法／跳過該項／換方案方向） |
| **腳本報錯或環境不可用**（沒裝套件、沒代碼權限、檔案讀不到、編碼異常） | **降級到 Markdown 協議繼續產出**，不要中斷任務；在交付說明中註明「本稿未經腳本校驗」 |
| **用戶明說「跳過／先出稿」** | **允許跳過**（`build_docx.py --force`），但必須：① 聲明「本稿未通過交付前校驗」② 列出未通過項 ③ 不得聲稱已完成 |

**禁止的行為**：
- 同一個錯誤反覆重試超過 2 次（燒用戶額度）
- 因為校驗不過就遲遲不交付（超過合理輪次必須上報，而不是自己死磕）
- **靜默跳過校驗**（跳過一律留痕，讓用戶知道哪裡沒檢查）

**用戶側的救援話術**（見 README §一點八）：卡住時說「**卡在哪了？把錯誤原文貼出來**」或「**跳過這項，先出稿**」。

> **交付物定義**：一次完整的營銷項目 = **一份 Word 文檔**，內含現狀分析報告 + 營銷策劃方案 + 預算明細 + 行動清單 + 風險與假設。
> 不是 Markdown，不是聊天裡的文字。**最終必須產出 `.docx` 文件。**
>
> **跨工具可用（重要）**：本 skill 是純 Markdown、無任何專有語法，**任何 Agent 平台都能用**（ChatGPT／Claude／Claude Code／豆包／本地 Agent／其他）。
> - 接入方式（各平台怎麼裝）→ 見根目錄 `AGENTS.md`
> - 環境沒有多 Agent 能力 → 有降級方案（見 `references/02-多Agent分工简报.md` §二）
> - 環境沒有文件系統 → 產出結構化 Markdown ＋ 樣式對照說明（見 §四「交付執行：腳本」）

---

## ⛔ 第 0 步：門禁（強制，不可跳過、不可猜測）

**任何要產出交付稿的任務，都必須先問清楚。沒問清之前 —— 不准出方案、不准往下走、不准用「一般情況」代替。**

> ⚠️ **門禁覆蓋範圍（2026-09-14 補）**：**不只「新項目」** ——
> **重新生成、改結構（例如改成精煉版）、改版、換量級，這些任務同樣要先問。**
>
> **教訓**：用戶要求「再生成一個羅森範例」時，執行 AI 因為「素材現成」就直接開跑，
> 沒問結構／量級／字數，違反了門禁第 13 項。**素材現成 ≠ 需求明確。**
>
> **機械強制（已落地）**：`build_docx.py` 現在要求提供《任務規則表》（`--rules`），
> **沒有它就直接拒絕出稿** —— 「先問用戶」不再是自覺，而是**出稿的硬前提**。

### 執行方式
1. 對照下面 13 項清單逐項核對，**列出還缺哪些**
2. 把缺項**一次性**問完（不要一項一項擠牙膏）
3. **等用戶回答**。用戶沒答完之前，停止，不要產出任何方案內容
4. 用戶答完 → **產出《任務規則表》請用戶確認**（見協議 1）→ 確認後才進第 1 步

### 13 項門禁清單（全部拿到才放行）

| # | 要拿到什麼 | 為什麼是門檻 |
|---|---|---|
| 1 | **賣什麼**（產品/服務、客單價、毛利） | 不知道毛利就沒法定價與選品 |
| 2 | **賣給誰**（人群、場景、什麼情況下會買） | 決定內容與渠道 |
| 3 | **現在什麼規模**（營收／門店數／用戶數／訂單量） | 判斷方案量級 |
| 4 | **卡在哪**（客戶**原話**怎麼說） | 決定問題類型（A–H） |
| 5 | **預算多少**（總額、是否一次性、誰出錢） | 沒有預算 = 沒有方案 |
| 6 | **有什麼現成資源**（人手、渠道、私域、內容能力、老闆能否出鏡） | 決定動作能不能落地 |
| 7 | **時間要求**（死線、關鍵節點、日曆） | 決定節奏與取捨 |
| 8 | **硬約束**（合規、場地、授權、人力上限、不能做什麼） | 撞了就是白做 |
| 9 | **對接人與決策人**（誰拍板、決策鏈） | 決定方案給誰看 |
| 10 | **競爭對手**（誰在搶、憑什麼搶） | 決定差異點 |
| 11 | **過去試過什麼**（有效的、無效的、被誰坑過） | 避免重複失敗 |
| 12 | **怎麼算成功**（驗收標準） | 決定 KPI |
| 13 | **交付結構 ＋ 內容量級 ＋ 字數**（三件事一起問） | **先問用戶，不要自己替他決定**。下限＝便利店開學季案水平（要素齊全＋有判斷力＋有主線）、**上限不設限** |

> **第 13 項的問法（⛔ 給選項讓用戶選，不要開放式地問）**
>
> **① 結構**（給兩個選項）
>
> - **精煉版**（像便利店開學季案）：終稿 = **結論與決策** ＋ 能執行的動作。**必須包含**：
>   - **診斷結論**（例：「對抗學長的五條結構性軟肋」—— 對手哪裡打不動）
>   - **組合策略**（例：「三檔套裝 ＋ 滿減階梯」—— 產品與價格的組合設計）
>   - **特別專案**（峰值日的加碼動作，例：報到日家長專案）
>   - **機制**（例：需求登記機制怎麼運作）
>   - 定位口徑／觸達／文案／KPI（含工具與決策節奏）／預算／行動清單／風險與假設
>   - **留支撐稿的只有**：**推導過程與資料堆砌**（競品逐環拆解的 79 行、完整禁用詞 59 條、現狀數據羅列）
>   - ⛔ **最常見的錯誤**：把「診斷」也當成「分析」砍掉 → 方案變成「只有動作、沒有判斷依據」的執行清單，**沒說服力**。**砍過程，不砍結論。**
>
> - **完整版**：上面全部 ＋ **現狀分析的完整展開**與競品逐環拆解（八篇＋附件）
>
> **② 量級**（⛔ **每個選項都要標預估篇幅** —— 用戶才有尺寸感）
>
> | 選項 | 篇幅 | 適合誰看 |
> |---|---|---|
> | **一頁紙** | 1–2 頁（約 800–1,500 字） | 決策人快速過目 |
> | **精簡版** | 約 200–300 行 | 只看決策與執行 |
> | **精煉版** | 約 300–400 行（便利店開學季案原版 371 行） | 要直接拿去執行 |
> | **標準版** | 5,000–10,000 字 | 常規交付 |
> | **完整版** | 不限（含全部推導與附件） | 內部存檔／大項目 |
>
> **③ 硬限制** —— 「有沒有字數或頁數的硬要求？」（比賽、招標、客戶模板常有規定）
>
> - **下限＝便利店開學季案水平**（要素齊全 ＋ 有判斷力 ＋ 有一條主線）、**上限不設限**。
> - ⛔ **不許開放式地問「你要多長」** —— 用戶沒有尺寸感，答不出來。
>   **必須給具體選項 ＋ 預估篇幅**，讓用戶勾選。
> - 用戶沒特別說 → 預設「**精煉版／標準版**」，並寫進《任務規則表》。
>
> 詳細問法見 §五 對接實測清單（40+ 條原話問法，按 8 組分類）。

### 門禁例外
僅當用戶明說「先按假設出一版」「我就想看看思路」時，才可跳過門禁；此時**必須在文檔首頁標註「本方案基於以下未經驗證的假設」並列出假設清單**。

---

## 一、工作流（六步 · 內容維度）

> **先分清兩條線，別混**：
> - **本節「六步」＝內容工作流**（事實 → 分析 → 生成 → 裁決 → 交付）
> - **「S0–S8」＝操作流程**（工具怎麼跑）→ 見 `references/08-操作流程SOP.md`；嚮導：`python scripts/flow.py`
>
> **對照**：事實底稿 ↔ S2 ｜ 現狀分析／生成 ↔ S3–S4 ｜ 裁決 ↔ S4 ｜ 交付 ↔ S7–S8。
> ⛔ **強制層**：`scripts/flow.py → composer.py → selfcheck.py → run_pipeline.py` 一關不過就不出稿。
> ⛔ **內容再多，不經 `composer` 注入知識、不過 `selfcheck`，就不算完成。**

### 第 1 步：產出「事實底稿」（內部文件，先做）
把門禁收集到的一切整理成事實底稿 —— 這是後面所有 Agent 的唯一輸入源。內容含：
客戶與場所 ｜ 目標與預算 ｜ 時間軸（含日曆節點）｜ 戰場事實 ｜ **硬約束** ｜ 現有資產 ｜ 競爭格局 ｜ 已確認決策 ｜ 待解決問題。
（此步借用便利店開學季案的「事實底稿」做法：**任何方案變更都需先回到這裡核對**。）

### 第 2 步：現狀分析（體檢）
按 §三 文檔結構第一篇寫。必須回答：
- 生意現狀（數據事實，不是形容詞）
- 問題診斷（歸到 A–H 哪一類，見下表）
- 真正的卡點（**我的判斷**，不是複述客戶原話）—— 寫法是**重構問題**，不是複述現象
- 已排除的假設（顯示判斷過程）
- 約束與風險

#### 問題類型 A–H（診斷章的歸類骨架，2026-09-16 明確定義）

| 類 | 問題 | 典型客戶原話 | 對應打法章節 |
|---|---|---|---|
| **A 認知** | 沒人知道我／記不住 | 「沒人知道我們」 | §1 認知類、§2 內容類 |
| **B 交易** | 知道但不買／轉化低 | 「來的多買的少」 | §8 定價與轉化類 |
| **C 渠道** | 想買買不到／鋪貨與觸達斷 | 「想買找不到」 | §5 渠道類 |
| **D 信任** | 不敢買／缺證明 | 「怕被坑」 | §7 信任類 |
| **E 復購／私域** | 買了不回頭／沉澱不了 | 「回頭客少」 | §4 私域類 |
| **F 定價** | 賣不動／價格力弱／毛利薄 | 「只能靠打折」 | §8 定價與轉化類 |
| **G 組織** | 人力與執行力跟不上 | 「沒人做」 | 02-多Agent分工簡報（執行人力檢查） |
| **H 合規** | 監管紅線約束 | 「這行不能說」 | cases/11 醫藥、16 金融合規專節 |

> **用法**：主類型寫 1 個、並列次要類型（例：「E 復購／私域＋引流（A 認知／B 交易）｜性質：交易問題為主」）。
> **判據**：客戶原話只是入口，**歸類由你給**——客戶說「沒人來」，若數據顯示到店沒跌、成交跌了，那就是 B 不是 A。
> **診斷章三件套（必齊）**：① 問題類型 A–H 標籤 ② **卡點重構句**（「不是 X——是 Y」的句式）③ **核心動作一句**（窗口期內做什麼、把什麼變成什麼）。

### 第 3 步：多 Agent 分工生成（**每次都走，不省略**；執行細節見 `references/02-多Agent分工简报.md`）

| 角色 | 負責什麼 | 產出 | 必寫要素（**要素齊全才算合格 —— 不是字數越多越好**） |
|---|---|---|---|
| **（前置）事實整理** | 把門禁收集到的一切整理成事實底稿（所有角色的唯一輸入源）——**非角色，由主理人做** | 事實底稿 | 見第 1 步 |
| **策略 Agent** | 三次收窄、**打法組合表**、選品與貨盤、定價與機制、節奏排期（渠道分工／預算／KPI／風險**只給量級摘要**，明細歸觸達/財務風控） | 策略篇 | **① 打法組合（多段文字，7 要素，見下）②** 選品 **≥8 個 SKU**（各含毛利與走量預期）／定價 **≥3 檔**各給依據／時間線**按天列**並標峰值日／機制逐個寫清規則 |
| **品牌 Agent** | **競品掃描**、定位、差異化支點、命名、**禁用詞與口徑** | 品牌篇 | 頭號對手 **≥5 環節四段式**／其他對手 **≥3 個**含威脅等級／禁用詞 **≥5 類**並標後果／一致性審核清單 |
| **觸達 Agent** | 渠道選擇（含為什麼**不**用某些）、用戶路徑、關鍵詞與搜索、內容分發 | 觸達篇 | 渠道 **≥4 個**各含理由／預算／預期／執行人；路徑**能畫出**且標流失風險 |
| **文案 Agent** | 物料清單與逐件完整文案、一線話術、群內話術 | 文案篇 | 物料 **≥12 件**，每件含**位置＋完整原文＋成本**；話術是**能照着念的原話** |
| **財務/風控 Agent** | 預算明細、盈虧線、走量預期、KPI 表、**風險四件套**、關鍵假設 | 財務風控篇 | 預算 **≥6 項**且加總＝合計／KPI **≥8 個**含預警線／風險 **≥3 條**且四件套齊全 |
| **主理人（我）** | 裁決衝突、合併成稿、跑雙校驗 | 合併稿 | 依《任務規則表》確認的字數，去重合併（不刪要素） |

> ### ⚠️ 關於篇幅：**先問用戶要多長，不是越長越好**
>
> - **字數由門禁第 13 項先問用戶決定** —— 比賽／招標常有字數頁數上限；客戶也可能只要**一頁紙摘要**；內部討論稿又可能要完整版。**不問就默認 5,000–10,000 字。**
> - 上表的「≥N」是**內容完整性要求**（該寫的要素一個不能少），**不是「字數越多越好」**。
> - **深度的定義**：該有的要素齊全 ＋ 每個要素寫到**可執行**（有動作、有負責人、有數字、有驗收）。
> - **反面教材**：把「深度」做成「字數競賽」→ 方案變長但空話變多，客戶不買單。**宁短而實，不長而虛。**
> - 若客戶只要摘要：**要素照樣齊全，但每個要素壓縮到最精煉** —— 壓縮的是論述，不是要素。

>
> **角色數量按項目規模匹配**（便利店開學季案實際動用了 **7 個參與者**＝5 個角色＋事實整理＋主理人）：
> - **大項目**（多戰場／預算 ≥10 萬或週期 ≥1 個月）→ **5 個執行角色全上**，一個不能少
> - **小項目**（單店／單一目標／預算 <5 萬）→ 可合併為 3 個（策略＋品牌／觸達＋文案／風控），但**每個角色的必寫要素不變**
> - **不變的原則**：**角色可以合併，要素不可以缺失。**

**分工規則**：
1. 各 Agent **只讀事實底稿**，不互讀彼此的草稿（避免互相遷就、越寫越像）
2. 出現衝突（例如品牌 Agent 說不能用某詞、文案 Agent 卻寫了）→ **主理人裁決**，並在文檔裡記錄裁決理由
3. 全部完成後，主理人跑 §六 自檢清單

**環境不同，做法不同（本 skill 不依賴特定工具）**：
- **能派獨立 Agent** → 五個角色各自獨立執行，互不看草稿
- **只有一個對話** → 順序扮演，每次明確宣告「現在我是 X 角色」，只讀事實底稿、不回顧前序輸出，五個都寫完才進裁決
- **只能複製貼上** → 把 `references/02-多Agent分工简报.md` §三 的五份角色簡報逐一貼進去跑

### 第 4 步：主理人裁決與合併
把五篇合成一份文檔，衝突處按**五級優先序**裁決（客戶硬約束 → 事實底稿 → 財務可行性 → 品牌一致性 → 創意表達），記錄裁決理由，補齊過渡與索引。

### 第 4.5 步：按客戶格式範本重排（**客戶／比賽／招標給了範本時必走**）

客戶常自帶**提交格式範本**（章節清單、模板文件）。**不要為了貼合格式砍深度**（協議 2）——做法是**映射，不是重寫**：

```bash
# ① 先看自動匹配建議（不寫檔）
python scripts/reformat_to_template.py --source 方案.md --template 客戶範本.docx --dry-run
# ② 確認映射後重排（映射表 JSON：範本章節 → 源稿章節列表）
python scripts/reformat_to_template.py --source 方案.md --template 客戶範本.docx \
    --map 映射.json -o 方案-按範本.md --report 要素核對.md
# ③ 走唯一入口出稿
python scripts/run_pipeline.py --rules rules.json --plan 方案-按範本.md \
    -o 方案-按範本.docx --title "客戶名 營銷方案"
```

**三道保證（不丟東西）**：

| 情況 | 處理 |
|---|---|
| 範本要、源稿沒有的章節 | 標【待補】＋列進核對報告（該新寫的新寫） |
| 源稿有、範本沒對應位的章節 | **整節移入附錄**（協議 2：合併，不取捨） |
| **硬要素核對** | 執行摘要／問題類型／卡點重構句／打法組合／禁用詞／KPI／預算／行動清單／風險兜底／關鍵假設／自檢單 —— **丟一件就退出碼 1，不准出稿** |

> **映射由你（AI）判斷**：工具只做標題粗篩建議；語義映射要你決定
> （例：「二、市場與競爭分析」 ← 源稿「一 · 診斷」＋「五 · 定位與口徑」）。
> 範本與映射示例：`references/范例/格式范本示例-比赛提交清单.md` ＋ `格式範本映射示例.json`。

### 第 5 步：交付 Word
按 §三 規範生成 `.docx`。**有代碼執行能力時必須走 `scripts/build_docx.py`** —— 它會先跑自檢，不過就拒絕生成（強制點）；沒有代碼能力則退回 Markdown + 樣式對照說明。詳見 §四「交付執行：腳本」。

---

## 二、狀況 → 打法 路由表

**這張表是引擎。** 左邊是對接中聽到的狀況，右邊直接給打法方向。

| 聽到的狀況 | 打法方向 | 預算量級 | 見效週期 |
|---|---|---|---|
| 沒人知道我們（線下店） | 門頭即廣告 + 招牌菜/爆品命名 + 3 公里地推 + 熟客群 | 5 千–5 萬 | 2–6 週 |
| 沒人知道我們（線上） | 符號／口號 + 單平台內容打透 + 場景化搜索詞 | 1–10 萬 | 1–3 個月 |
| 來了不買 | 詳情頁/菜單重做 + 價格錨定 + 首單鉤子 + 話術 | 0–2 萬 | 1–4 週 |
| 買了不回頭 | 私域收口 + 分層發券 + 會員階梯 + 喚醒 SOP | 1–5 萬（含工具） | 1–3 個月 |
| 投流虧錢 | 先做全鏈路數據表找斷點 → 修轉化 → 再放量 | 0（先診斷） | 2–4 週 |
| 想開新品牌／第二曲線 | 先判能不能延伸 → 品牌架構設計 → 冷啟動 | 20 萬起 | 6–12 個月 |
| 想漲價 | 加價值要素保溢價 + 分層定價 + 套餐設計 | 0–3 萬 | 1–2 個月 |
| 想出海 | 先選一個市場 + 產品本地化重做 + 本地帳號矩陣 + 自播 | 50 萬起 | 3–12 個月 |
| 短期戰役（開學/開業/大促） | 定主戰場日 + 造勢期/引爆期/收口期三段 + 峰值日專案 | 按預算 | 2–4 週 |
| 老闆要做個人 IP | 內容日曆 + 不寫腳本 + 前三秒給結論 + 高管矩陣 | 人力為主 | 3–6 個月 |

> 詳細打法（怎麼做、步驟、預算、風險）見 `references/00-打法库.md`（104 條）。

---

## 三、交付物規範（Word 文檔）

> ⭐ **五塊質量硬要求**（缺任何一塊＝方案不合格。前三塊標準來自用戶認可的「便利店開學季案」，後兩塊來自 2026-09-16 用戶以《中百羅森新生開學引流方案》為基準的糾偏）：
>
> 1. **多維度** —— 現狀分析要做滿：生意現狀／問題診斷（含 **A–H 問題類型標籤**）／**已排除的假設**／**競爭與關聯品牌掃描**／約束與風險底線。不是只寫一個「現狀」了事。
> 2. **多品牌** —— 競爭掃描必須**逐個對手拆**：頭號對手按業務環節全鏈條拆（每環四件事：他怎麼做／強點／致命弱點／我們怎麼打）；其餘對手逐個列（他搶什麼／威脅等級／我們的應對）；最後落到**「對手無法複製的資產」**。
> 3. **風險把控** —— 每個風險必寫**四件套**：排序理由／**預警信號（具體數字 + 檢查時間點）**／**按觸發順序的兜底預案（含最壞兜底）**／**預防性動作**。只列風險不給預案的，等於沒寫。
> 4. **打法組合（2026-09-16 新增硬門檻，同日修正為「多段文字」形式）** —— 策略篇**必須**有打法組合：每條打法寫齊 7 個要素——**為什麼用它**／具體動作／誰做／花多少／多久見效／驗收指標／**可抄案例**。⛔ **形式用多段文字，不要用寬表**：8–9 列的表格在 Word 裡會擠成一條豎線（用戶實測反饋），根本讀不清。缺「為什麼用它」＝只有清單沒有說服力；缺「可抄案例」＝ 628 張案例卡白做。`selfcheck.py` 機械校驗 7 個要素標籤（形式不限）。
> 5. **診斷三件套 ＋ 風險掛編號（2026-09-16 新增）** —— 診斷章須齊：**問題類型（A–H）** ＋ **卡點重構句**（「不是 X——是 Y」）＋ **核心動作一句**；風險章每條須寫全**四件套**（為什麼會發生／預警信號／兜底預案／預防動作）；**寫作時內部對照 `04-失败归因总库.md` 的 12 個模式與 34 條預演，但交付稿裡只寫根因白話，不寫「模式 NN」編號**（見下方協議 5 與 selfcheck 第【10】關）。

### ⛔ 減法規則（比加法更重要 —— 這份 skill 歷史上最大的翻車點）

1. **同一件事只說一遍** —— 全文不允許逐字重複的段落／表格／清單。
   同一內容需要多處引用時，用「見 §X」而不是再抄一遍。
2. **禁止為湊數而重複／灌水** —— **上限不設限**：要寫多長由**用戶決定**（門禁第 13 項先問）。
   但**同一件事不許說兩遍** —— 重複是**缺陷**，長不是。
   判斷標準：「這句話刪掉會不會少信息？」不會 → 刪。**寧短而實，不長而虛。**
3. **必須有一條反對意見** —— 如果客戶已有判斷（例如「主戰場就是 X 日」），
   至少有一個角色要**明確提出反對並給理由**；主理人裁決後**保留這條痕跡**
   （原版靠這一條贏了：品牌角色開篇否定了客戶已拍板的戰場判斷）。
   **只會附和客戶的方案是廢方案。**
4. **必須有一條主線** —— 整份方案能被壓成一句話（例：原版「他們賣到校前的一包貨，
   我們賣到校後 15 天裡隨時能兌現的確定性」）。壓不出來就是還沒想清。

> 校驗：`depth_check.py` 的【9】重複度維度 + 【2】風險決策推理，會把第 1、2 條機械攔下；
> 第 3、4 條需人工確認，但執行者必須在《流程狀態》行裡聲明是否已寫。
>
> ⚠️ **沒有「字數上限」這種東西** —— 要素數量多少算合適，取決於**用戶要的量級**（門禁第 13 項先問），不由執行者替他決定。
>
> **一次實測的代價**（2026-09-14 對比診斷）：同一份內稿，舊標準產出 1,998 行、新標準（把
> 要素下限全填滿）產出後仍輸給 371 行的原版 —— 因為原版有主線、有取捨、有反對意見，
> 而且每個判斷只說一遍。**要素齊全不等於方案成立。**

### 基本設定
- **格式**：`.docx`（不是 markdown、不是聊天文字）
- **份數**：**一個文件**裝完（現狀分析 + 策劃方案 + 預算 + 行動清單）
- **語言**：**簡體中文**（客戶看的；skill 內部文件仍用繁體）
- **篇幅基準**：**以門禁確認的目標字數為準——先問用戶；未指定時預設 5,000–10,000 字**。深度以「硬要素齊全」衡量（五要素行動、禁用詞口徑、預算能對上、KPI、人力檢查、假設驗證），**不以字數注水、也不為省字砍硬要素**；附件（物料文案、話術表）另計
- **必備元素**：封面（項目名／日期／版本）｜目錄｜頁碼｜表格化呈現（能表格就不要長段）

### 文檔結構（八篇 + 附件）

```
封面
目錄
執行摘要（TL;DR）          —— 1 頁，結論先行：做什麼、花多少、預期什麼結果
核心結論卡片               —— 一張表講完關鍵數字

第一篇 現狀分析
  1.1 生意現狀（數據事實）
  1.2 問題診斷（歸類 + 我的判斷）
  1.3 已排除的假設
  1.4 ⭐ 競爭與關聯品牌掃描（**多品牌逐個拆** —— 本 skill 的質量分水嶺之一）
      · **頭號對手**：按業務環節全鏈條拆解（如「獲客→信任建立→成交→履約→復購」），
        每一環寫四件事：【他怎麼做／他的強點／他的**致命弱點**／**我們怎麼打**】
      · **其他對手**：**逐個**列（含渠道品牌、平台、個人賣家），每個寫
        【他搶的是什麼價值點／威脅等級（高/中/低）／我們的判斷與應對】
      · **核心差異點**：先壓成一句話，再落到**「對手無法複製的可核實資產」**（用對比表列）
  1.5 約束與風險底線（硬約束逐條列，標明「撞了就是白做」）

第二篇 策略
  2.0 ⭐ **打法組合（必寫，多段文字形式）** —— 本 skill 的核心產物：
      · **每條打法一段**：`**打法 N｜名稱**（定位語）`，下面逐個要素一行：
        `- **為什麼用它**：…`（說服邏輯：差異化錨點／成本結構／對手做不到的那點，不是形容詞）
        `- **具體動作（精準到每一步）**：…`（⛔ 拆成**編號步驟**，每步寫齊「動作／時間／誰做／物料·話術／產出」，≥3 步；只寫一句話＝不合格，`selfcheck` 第【7】關判硬錯誤。用戶 2026-09-16：「策劃具體操作流程要詳細精準到每一步——指策劃案的打法和實操」）
        `- **誰做｜花多少｜多久見效**：…｜…元｜…`（三要素合一行）
        `- **驗收指標**：…`
        `- **可抄案例**：…（展開見 2.0.1）`
        `- **為什麼這麼做**：…`（把理論/觀點**寫成內容**：這套動作背後是什麼道理、照著改為什麼不會跑偏。⛔ 只寫一串理論名字＝沒寫）
      · ⛔ **每條打法的做法必須來自 `references/00-打法库.md`**（含「怎麼做」步驟），不許自己編一套
      · ⛔ **不要用寬表**：8–9 列的表格在 Word 裡會擠成一條豎線（用戶 2026-09-16 實測反饋）
      · 「可抄案例」列只放索引，**表後必須另有 2.0.1 文字展開**（別人怎麼做的＋我們怎麼用）
      · 3–7 條為宜；一頁紙／摘要版可壓縮，但不得整節省略
  > ⛔ **知識要「寫成內容」，不是「標出處」**（selfcheck 第【8】關機械校驗）：
  > - **策略篇**要真的用到 **≥3 個理論**（用中文名寫進做法裡）＋ **≥1 個書籍觀點**（《書名》＋它的主張）。
  > - **每條打法的「為什麼這麼做」必須寫開**（≥25 實字）—— 講清楚這套動作背後是什麼道理，
  >   **不是列一串理論名稱**。
  > - 「可抄案例」必須是「**品牌** —— 他做了什麼 ▶ 結果：數字」；只有品牌沒結果不算可抄。
  > - **第三篇 定位與口徑**要點明用的是哪一套定位理論，並說清楚它用在這一步的哪個決定上。
  >
  > ⛔⛔ **交付稿不得出現任何内部坐标**（selfcheck 第【10】關硬錯誤、不出稿）：
  > `§X.X`／`（03 §C1）`／`（作者49）`／`cases/xx.md`／`references/`／`打法库`／`模式 NN`／
  > `composer.py`／`SKILL.md`。
  > **客戶不需要知道我們內部怎麼編號、檔案放哪、用了哪個腳本** —— 那些寫進
  > `composer.py --internal` 那份文件裡，只有你和維護者看。
  > 注意這不是「少寫」：理論照用、案例照引、**而且要寫得更詳盡**，只是不掛編號。
  > 用戶 2026-09-17 原話：「不是拿了案例就行了，說有什麼理論是沒有任何意義的 ——
  > 要根據這些案例、這些理論、這些觀點寫具體的操作、具體的做法。」
  2.1 ⭐ **三次收窄**（先說清：這次**不做什麼**、戰場縮到多小、為什麼 ——
      收窄是方案成立的前提，不做取捨的方案等於沒方案）
  2.2 方案成立的前提
  2.3 被低估的資產（現有資源裡被忽略的 2–3 個）
  2.4 節奏排期（按天/按週，標出**峰值日與黃金窗口**）
  2.5 產品/服務與貨盤（選品邏輯、定價、套裝、滿減）
  2.6 特別專案（針對峰值日的加碼動作）

第三篇 定位與口徑
  3.1 定位與差異化支點（3 條）
  3.2 ⚠️ 禁用詞與紅線（**什麼話絕不能說**）
  3.3 對不同人說什麼（客戶／用戶／家長／監管／合作方，各一套口徑）
  3.4 命名與記憶點

第四篇 觸達與渠道
  4.1 渠道選擇（為什麼用這些、為什麼不用那些）
  4.2 路徑設計（用戶從哪知道你 → 怎麼到店/下單）
  4.3 內容與投放節奏
  4.4 觸達的硬風險

第五篇 落地文案與物料
  5.1 物料清單（每件：放在哪、寫什麼、多少錢）
  5.2 話術（一線人員照着說）
  5.3 需要客戶核實的事實

第六篇 KPI 與追蹤機制
  6.1 追蹤工具（土辦法也要能落地，含成本）
  6.2 每日/每週只看這幾個數
  6.3 決策節奏（什麼數出現就調整、怎麼調）

第七篇 預算明細
  7.1 預算表（分項 + 合計，**必須能對上**）
  7.2 盈虧線測算（保本點／健康線）
  7.3 追投與止損規則

第八篇 執行與風控
  8.1 行動清單（每條：做什麼／誰做／什麼時候／花多少／驗收）
  8.2 ⚠️ 執行人力檢查（**每個動作的負責人是否有時間做**；人力不足時的刪減順序）
  8.3 ⭐ **風險清單 —— 每個風險必寫「四件套」**（本 skill 的質量分水嶺之二）
      · **先說清為什麼會發生在我們身上**（內部對照 `04-失败归因总库.md` 找同類死法；
        ⛔ 交付稿**不寫**「模式 NN」這種編號 —— 客戶看不懂，selfcheck 第【10】關會攔），
        例：「**時機錯誤**：兩週窗口沒抓住 → 必須現在備貨＋招募，開學週集中引爆，不拖」
      · **排序理由**：為什麼這個最緊急／最可能（並說明其他風險為何排在後面）
      · **預警信號**：**具體數字 + 檢查時間點**（例：「9/12 中午 12:00 復盤：大件動銷 < 25%」）
      · **兜底預案**：**按觸發順序分步**（第一步做什麼 → 第二步 → … → **最壞兜底**），
        並說明**為什麼先做這個而不是直接降價**（先試免費的，再花錢的）
      · **預防性動作**：比兜底更重要 —— 能在事前把風險消除掉的動作（如「只跟可退貨的檔口合作」）
      > **只列風險不給預案的，等於沒寫。**（此標準來自便利店開學季案，那份的風險章節是本 skill 的質量基準）
  8.4 關鍵假設與驗證（每條假設 + 驗證方式 + 驗證成本 + 不成立時的 Plan B）
  8.5 待解決問題清單（我還沒搞清的事）
  8.6 不做什麼（明確劃界，防期待錯位）

附件
  A 物料文案全文
  B 話術表
  C 客戶原始資料與約束（客戶告知的原始情況、原始數據、硬約束）
     > 正文**不得**出現「事實底稿／Agent 分工／裁決記錄」這類內部過程字樣；本附件是唯一例外
  D 參考案例（同行做過的、可抄的）
  E 交付自檢單（12 項，逐項 ✅/❌）—— **同時必須在聊天回覆中原樣輸出**（協議 3）
```

### ⛔ 表達格式原則（**決定「讀得下去」還是「看得頭疼」**）

> **用戶定調（2026-09-14，對比兩版排版）**：
> ① **表格轟炸**（什麼都塞表格）→ 用戶：「有點混亂、**不能一眼看出重點**」
> ② **列表式**（粗體錨點 ＋ 一句話一條 ＋ 適度留白）→ 用戶：「排版比較**舒服、清晰**」
>
> ⛔ **不要一律用表格，也不要一律用列表 —— 按信息類型選。**
> 表格在需要時**照用**（對比型**必用**表格，它比列表清晰得多）；不要矯枉過正把該用表格的地方也改成列表。
> 判斷標準：讀者要「**對照比較**」→ 表格；讀者要「**順序閱讀**」→ 列表。

| 信息類型 | 用什麼 | 例 |
|---|---|---|
| **對比型**（≥2 個對象逐項比較） | **表格**（列 ≤5） | 對手軟肋 vs 我們打法；套裝檔位 vs 內容 vs 價格 |
| **說明型／步驟型／清單型** | **列表**（`•` 或 `a.` `b.` `c.`） | 排期按階段；風險逐條；機制步驟；渠道說明 |
| **案例說明**（別家怎麼做的） | **敘述＋列表**（絕不用表格） | 品牌 → 面對什麼 → 做了什麼 → 結果 → 我們怎麼用 |
| **要素多、文字長的組合**（打法組合／商業模式拆解） | **多段文字**（一條一段，`**打法 N｜名稱**` ＋ 要素標籤） | 每要素一行（`- **为什么用它**：…`）；⛔ 寬表在 Word 會擠成豎線 |
| **假設與對策**（條件→動作） | **列表**（`a.` `b.`） | 每條：假設 → 驗證方式 → 不成立時怎麼辦 |
| **多欄位並列**（物料清單／渠道清單／預算／指標表） | **表格**（列 ≤5、格 ≤30 字） | 物料／位置／內容／成本；渠道／理由／人力／預算／預期 |

> ⛔ **多列清單不要用列表**（2026-09-14 用戶糾正）：「**物料用表格吧，太亂了**」——
> 像物料這種「名稱／位置／內容／成本」多列並排的信息，**列表會破壞列的對齊關係，讀起來亂**；
> 表格才看得清。**判斷：只要一條信息有 2 個以上並列欄位 → 用表格。**

**列表的標準寫法（照着抄）**：
- **帶條件／時間的條目**：`• **倒數 2 週**（現在 → 開學前）｜ 見信號：確認供應鏈；啟動招募並發專屬碼`
- **問題 → 對策的條目**：`a. **時間窗口只有兩週，錯過就沒了**：現在備貨＋招募，開學週集中引爆，不拖`（**注意：交付稿裡寫根因白話，不寫「模式 NN」**）
- 每條控制在 **1–3 行**；超過就拆子項
- **粗體只用在錨點**（時間／風險名／關鍵數字），不要整句加粗
- **段落之間留空行**（Word 裡就是段後間距）—— 不要擠成一片

**⛔ 五條硬規則**：
1. ⛔ **全部塞表格**（表格轟炸 = 讀起來累，重點反而不見）
2. ⛔ 表格超過 **5 列** 或單元格文字超過 **30 字**
3. ⛔ 段落超過 **80 字**
4. ⛔ **只寫結論，不寫推導** —— 終稿只放「**是什麼／怎麼做**」；「**為什麼**」留支撐稿
   （診斷不要寫「他怎麼做／他的強點」；組合策略不要寫「設計理由」專欄）
5. **判斷句要短而銳** —— 例：「他們截流的是『學校』這個泛顆粒度，不是『宿舍樓』」
6. ⛔ **可抄案例只留編號**（2026-09-16 用戶糾正）—— 「可抄案例」列只放索引，**表後必須用文字展開「別人怎麼做的」**：品牌／他面對什麼問題／具體做了什麼動作／結果數字／**我們怎麼用**。**案例說明一律敘述，不用表格**——只有卡片號等於沒寫（客戶不會去翻案例庫）。

### ⛔ 篇幅配比：**權重 ≥ 40% 給「策略 ＋ 診斷」（與總長度無關）**

> **用戶定調（2026-09-14）**：「**是權重，甚麼長度都是把權重增加**」
> 意思是：不管交付多長（一頁紙／5 頁／20 頁／不限），**策略與診斷的篇幅占比都要 ≥40%**。
> 權重是**獨立於長度**的維度 —— 不是「寫長一點就更重點」，而是**同一個長度裡誰占的份額大**。

| 章節群 | 篇幅占比 | 寫法 |
|---|---|---|
| **策略 ＋ 診斷**（收窄／對手軟肋／節奏／選品／組合／機制） | **≥40%** | **重點章節**。診斷要逐條拆對手軟肋；策略每節寫清「怎麼做＋為什麼這樣取捨」 |
| **執行類**（行動清單／KPI／預算／物料清單／話術） | **≤40%** | 能表格就表格、一格一句話；寫到「能照着做」即可，**不展開解釋** |
| 其他（TL;DR／結論卡／定位口徑／觸達／風險） | 其餘約 20% | — |

**做法**：先定總長度 → 按 **40 / 40 / 20** 分配篇幅 → 執行類寫到夠用就收手，**把省下的額度全給策略與診斷**。

⛔ **最常見錯誤**：把篇幅**平均分**給各章 → 策略與診斷被稀釋成一頁表格，方案就失去判斷力（這正是本 skill 被用戶連續糾正的地方）。

### 每個打法的寫法要求
**禁止寫「做私域運營」「加強內容營銷」這類概括。** 每條必須是：
> 做什麼（具體動作）｜誰負責｜什麼時候｜花多少｜怎麼驗收

例：
> ❌ 用包裹卡做私域引流
> ✅ 印 500 張包裹卡（A5，300 元），正面寫「加店長微信領 ¥5 券」，背面印店長二維碼；由店員在每天 18:00–20:00 高峰期對每單外帶客戶遞卡；目標：2 週加 150 人；驗收：掃碼率 ≥ 30%

---

## 四、參考文件索引

> **選檔看第 -1 步那張表**（含「明確不要讀」）。本節是**細節補充**：每份檔「必須引用什麼、內容是什麼」。
> 目錄級導航：`references/cases/README.md`（50 個行業檔只讀 1 個；含固定標題名與硬規則）。

### 生成時必須引用的三份（⛔ 不是「參考」，是「必須引用並標出處」）
| 文件 | 什麼時候讀／怎麼用 |
|---|---|
| `references/00-打法库.md` | **生成時最先讀**。104 條打法，按狀況查（§0 總表一頁速查）；**每條打法必須照它的「怎麼做」展開**（交付稿裡不寫 §X.X 編號） |
| `references/cases/{行業}.md` | 要找同行案例。**先讀「案例清單」（每條寫清了「做了什麼／結果／可抄的點」），再跳「深度拆解」** |
| `references/03-方法论操作手册.md` | **策略篇必引用 ≥3 個模型**（GROW／NPS／超級符號／AISAS／ECIRM…）並標出處；**111** 個模型（A–M 十三大類）都寫了怎麼填 |

### 案例庫：51 個大類 / **649 張深度案例卡**（01–45 行業 411 張 ＋ 46–51 機構與方法論 238 張）

> ⛔ **案例集硬規則**（2026-09-16 由項目記憶寫進本檔 —— 原本只在內部筆記裡，模型看不到）：
> 1. **嚴禁公司背景**：創始人、成立年份、股權、融資、營收、人事 —— 對做營銷毫無用處。
>    （例外：`cases/46–51` **機構／書籍類**可保留「理解其方法所必需」的背景；行業卡 `01–45` 只留動作與結果。）
> 2. **唯一允許的數字是「這次營銷動作造成的結果」**（銷量／曝光／門店／復購）；公司財報數字不算。
> 3. 清單必須寫**具體動作**（例：「用包裹卡把線下客導到企微、朋友圈按 3:2:1 發」），
>    禁止只寫「私域運營」「品類創新」「超級符號」。
> 4. 每張卡要有「**解決了什麼問題**」＋「**得到了什麼結果**」，且**至少 1 處可信度標記**（【已核實】等）。
> ⚠️ **已知現狀**：早期入庫的案例卡仍含大量背景資訊（營收／融資／創始人等），與規則 1 有落差；
> **新寫或改寫的卡一律按規則 1**，引用時也只挑「動作＋結果」。
`references/cases/` 01–45 號文件，每個含：行業概覽／門禁清單／8–16 個深度案例卡（誰做的／做了什麼／結果數字／可抄的招／坑）／對接實測清單。

| 號段 | 覆蓋 |
|---|---|
| 01–18 | 餐飲茶飲、食品飲料酒、美妝個護、服飾運動戶外、3C數碼家電、汽車出行、互聯網平台、遊戲泛娛樂、AI產品、B2B工業與區域品牌、醫藥健康（含合規紅線專節）、母嬰寵物、家居建材、零售商超便利店（含**羅森百嶼森**）、教育文旅本地生活、金融保險（含合規對照表）、奢侈品高端、酒店度假 |
| 19–30 | 城市區域公共品牌、跨境電商出海、企業服務SaaS、新能源綠色消費、銀發經濟養老、二手循環消費、潮玩文創IP、影視綜藝內容IP、茶飲咖啡烘焙、寵物服務、農業生鮮供應鏈、體育賽事運動俱樂部 |
| 31–45 | 眼鏡視光、珠寶鐘表、箱包皮具配飾、香氛氣味品牌、美容儀個護小家電、健身運動健康、醫美輕醫美、口腔專科連鎖、餐飲供應鏈調味品、預製菜方便速食、文具文創辦公、攝影影像服務、婚慶人生儀式、招聘雇主品牌、物流快遞服務 |
| 46–49 | **機構與方法論類**（乙方案例，每戰役一卡）：國際4A與傳播集團、戰略諮詢、本土創意與營銷服務商；營銷書籍與作者（每書一卡） |
| 50 | **機構出版物與觀點庫**（每機構一卡）：麥肯錫季刊／BCG／貝恩購物者報告／奧美／電通／WPP／W&K／華與華／和君／年度演講 IP／行業數據源（含「動機—用法」速查） |
| 51 | **本土數字營銷與 MCN**（每機構／每模型一卡）：省廣／利歐／引力／天下秀／微播易／分眾（媒介代理集團）、無憂／遙望／蜂群／Papitube（MCN）、新消費操盤複盤、平台官方模型（AIPL／FAST／JD GOAL／O-5A／5R） |

> 全量逐卡清單見 Obsidian《營銷案例庫總索引》（51 類 649 張）。

### 其他支撐文件
| 文件 | 什麼時候讀 |
|---|---|
| ⭐ `references/08-操作流程SOP.md` | **一接案就讀**：從收資料到出稿**每一步的 輸入／命令／產出／校驗**（配合 `composer.py`，精確到每一步） |
| `references/04-失败归因总库.md` | 寫完方案做風險自檢（**12 個失敗模式 + 34 條預演清單**） |
| `references/05-小企业与新品牌从零打造.md` | 客戶是小企業／新品牌。§〇 路由表先定位，再進五階段、各賽道起盤、零預算打法 55 條、失敗歸因 16 模式、騙局識別、對接清單 |
| `references/cases/51-本土数字营销与MCN.md` | 要用平台官方模型（AIPL／FAST／5A／DEEPLINK／MCN 打法） |
| `references/01-接案输出模板.md` | **寫方案時必讀**：逐章寫作指南（目的／必含／寫法要點／範例段落／常見錯誤）＋ 附件模板（體檢表／Brief／報價／驗收指標／一頁紙／項目卡／復盤） |
| `references/02-多Agent分工简报.md` | **多 Agent 分工時必讀**：五份可直接複製的角色簡報 ＋ 三種環境執行方式 ＋ **§二之一 產出規格（深度下限 —— 寫不到就是不合格，不是「精簡」）** ＋ 主理人裁決規則 |
| `references/07-质量范式-便利店开学季案.md` | ⭐ **質量標尺**：實測逆向出的完整範式（角色產出規格／16 骨架模板／可校驗指標／好方案的三個特徵）。**不知道「做到什麼程度算夠」時讀它** |
| `references/范例/` | 📄 **交付稿樣張＋輸入文件樣張**：`便利店开学季战役-交付稿.md/.docx`（v3，可作模板抄）、`-原版.md`（早期版對照）、`-任务规则表.json`（門禁 13 項填好長什麼樣／`build_docx --rules` 的輸入）、`-预算表.json`（`budget_check.py` 的輸入）。**樣張一律簡體；內部文檔才是繁體** |
| `references/06-选流派矩阵与对照表.md` | 要判斷某件事該外包還是自己幹 |
| `references/09-完整策划标准与评分表.md` | **出稿前對標**：大賽（大廣賽 100 分評分表）／B 端商業策劃／G 端政府項目／投標響應 四類各自「必須有的章節」＋ 諮詢公司級維度（MECE／金字塔／SCQA／假設驅動／issue tree）＋ 自評表與缺口清單。composer 的 `--tier 大賽/B端/G端/投標` 場景章節即據此生成 |
| `references/10-文案与物料样本库.md` | **寫文案／物料時直接抄**：海報母版、短視頻分鏡腳本、詳情頁結構、社群與一線話術、提案 PPT 骨架、G 端公文段落、商務報價與偏離表——每條附「為什麼這麼寫」，不是只喊「寫張海報」 |
| `references/11-防返工交付协议.md` | ⛔ **交付物是「要直接遞交給他人的長文檔」就必讀**（方案／報告／標書／投標書／參賽作品），尤其對方給了**模板、大綱或提交清單**時。6 條鐵律（先交目錄／標題一字不改／素材可見不溶解／禁帶內部物／改結構必改引用編號／數字必有來源）＋ 交付前自檢 8 項 ＋ 4 條結構性根因。**與 SKILL 頂部 P0 條款配套** |
| ⭐ `references/12-范式库.md` | **動手填骨架前必讀**：**六檔（速覽／標準＝C 端品牌／大賽／B 端／G 端／投標）各自的範式** —— 每檔 ＝ 骨架（章節樹）＋ **逐節填寫指引（該寫什麼／寫幾句／必須含哪幾個數）** ＋ **可直接改寫套用的關鍵句片段**。共 169 節、3.4 萬字。**範式與模板的區別：模板只給空表，範式連「這節寫什麼算合格」一起給**。由 `build_paradigm.py` 機械生成，勿手改 |
| ~~`archive/机构档案/`~~ | **已退役（2026-09-16）**：機構與方法論材料全部併入 `cases/46-50`（含機構出版物與觀點庫），機構檔案不再單獨維護 |

### 交付執行：腳本（**有代碼執行能力時必用 —— 這是真正的強制層**）

本 skill 附帶 **39 個腳本**（`scripts/`；其中 **33 支有 CLI 入口**，其餘為資料/函式庫模組），把「流程遵守」從**靠自覺**變成**機械校驗**。**交付物一律走唯一入口 `run_pipeline.py`**：

| 腳本 | 什麼時候跑 | 不通過的後果 |
|---|---|---|
| **`run_pipeline.py`** | ⛔ **出稿一律走這個** | **唯一入口**：依序跑 ①門禁 ②預算 ③自檢 ④深度診斷 ⑤出稿，**任一關不過就中止、不生成 `.docx`**。想拿到交付物，只能從這條鏈走 |
| **`flow.py`** | **一接案就跑**（流程嚮導） | 看一眼工作目錄，告訴你「**現在第幾步、下一步跑哪條命令**」——把 `08-操作流程SOP.md` 的流程變成可機械檢查的狀態機。`python scripts/flow.py [--dir 案子目錄]` |
| **`composer.py`** | ⛔ **動筆前第一步**（真正的「skill」引擎） | **方案組裝器**：`--rules 規則表.json --out skeleton.md --tier 速览\|标准\|大赛\|B端\|G端\|投标`。按 `§二 路由表` 機械匹配 3–7 條打法 → 自動拉取具體動作／驗收／可抄案例 → **注入「理論依據」（03 模型的「解決什麼問題＋前幾步」原文）** → 輸出骨架。**2026-09-17 起「可抄案例」注入的是真實卡片內容（品牌＋做了什麼＋結果），不再是檔案路徑占位符**；連不出結果的打法會列進骨架的 **§2.0.2 知識庫缺口**（**要求補，禁止編**）。映射表在 `scripts/knowledge_map.json`。**`--tier 速览` 即「小客戶／快速預覽標準」——現有骨架本身就是各類型客戶（含小企業／大客戶）的標準，不另造標準文件**。**六檔定位（2026-09-17 補）**：速覽＝小客戶／快速預覽（只留決策要素）；**標準＝C 端品牌（消費品牌／零售，場景章為貨盤・內容矩陣・會員復購・渠道控價）**；大賽＝比賽提案；B 端＝企業／工業品／渠道；G 端＝政府／政企；投標＝招投標響應 |
| **⭐ `build_paradigm.py`** | **改骨架或改指引後** | **範式庫生成器**：把 `composer.py` 的骨架（唯一真相）＋ `scripts/paradigm_data.py` 的逐節指引，機械拼成 `references/12-范式库.md`，並供 `composer --internal` 注入「本檔填寫指引」。**拼裝前先做一致性校驗**：composer 實際吐出的標題清單 ≠ 宣告清單就直接退出 1（防兩份真相漂移）。`python scripts/build_paradigm.py [--dry-run]` |
| **⭐ `structure_fix.py`** | **結構一改就立刻跑**（改章節順序／合併模板／映射大綱之後） | **交付稿結構體檢 ＋ 重編號**：① 體檢章號重複／倒序、子編號重複／非遞增、**懸空引用**；② `--renumber` 按**正文出現順序**重編章號與子編號（**標題文字一字不動**，對應協議 2「合併不取捨」）；③ 引用同步改寫 —— 帶部分號的可靠機械映射，**不帶部分號的裸引用（如「（4.3 节）」）一律不猜**，列出來要求人用 `--refmap` 指認。附錄自有編號（附錄九的 9.1–9.6）原樣保留。`python scripts/structure_fix.py plan.md -o fixed.md --renumber [--refmap refs.json] [--report 报告.md]` |
| **⭐ `docx_footnote.py`** | **交付稿要標註引用出處時**（官方常寫「引用須用**腳註**標明」） | **給 .docx 裝**真**Word 腳註**（python-docx 原生沒有腳註 API，這裡直接改寫 OOXML：建 `word/footnotes.xml` ＋ 把佔位 run 換成 `<w:footnoteReference/>` ＋ 補 Content_Types 與 rels）。**Markdown 寫法**：正文 `……大盘约 400 亿元[^1]，`，出處單獨一行 `[^1]: 弗若斯特沙利文《…》2025, p.12`（定義行不進正文）。`build_docx.py` 已自動接入；`python scripts/docx_footnote.py 方案.docx --list --verify` 可事後核驗裝配 |
| **⭐ `delivery_check.py`** | **出稿後、提交前（管線第 ⑦ 關）** | **交付形態核對**：① **必交附件核對表（可勾選）** —— `--init` 生成模板，人可勾 `"已交": true`，或按 glob 自動認檔；② **封面要素**（官方常要求「僅保留項目名稱＋團隊名稱」，多出日期／賽道／身份欄都報）；③ **文件名規範**（如「項目名稱-團隊名稱-隊長姓名」，並專門抓 `【隊長姓名】` 這種**佔位符沒填**）；④ **佔位符殘留掃描**（`【填】`／`{FILL}`／TODO 為硬錯誤；`待补`／`待填` 為警告，因為它們在行文裡也可能是正常詞）。`python scripts/delivery_check.py --dir 交付目录 [--init] [--cover-allow 项目名称,团队名称]` |
| **⭐ `promise_check.py`** | **改完承諾性文檔／新增關卡後** | **「文檔承諾 ↔ 實際執行」對賬關**：① 字段消費（composer 解析了哪些字段、每個是否進了輸出 —— 專抓「解析進內存又丟掉」）② 參考文件消費（references/ 每檔是否有腳本真讀；區分「僅供人工」與「該消費」）③ 判據對賬（curated 承諾→判據關鍵詞表，回歸守衛）。**它堵的是本倉庫最高頻的一類病：能力寫在文檔裡、沒有一條鏈路真的執行它。** `python scripts/promise_check.py [--report 對賬.md]` |
| **⭐ `optimize_scan.py`** | **每輪自檢時（與 verify_all 配套）** | **每輪產出 10 條「可優化點」**，帶位置／現象／為什麼算問題／怎麼改／嚴重度。**它與 `verify_all` 是兩把不同的尺子**：`verify_all` 證明「沒有漂移」，它指出「哪裡還能更好」——50 遍全綠證明不了沒有改進空間。10 個維度：靜默失敗／文檔漂移／重複常量／退出碼語義／異常兜底／遺留標記／硬編碼路徑／可用性／冪等隱患／參數一致性。**修完本輪重跑會露出下一批**，這才是「每輪 10 條」的正確形態。`python scripts/optimize_scan.py [-n 10]` |
| **維護／輔助腳本**（不常跑，但別忘了它們存在） | `t2s_data.py`（繁→簡對照表生成）／`case_clean.py`·`case_relabel.py`（案例集清理與重標號）／`relocate_06.py`（06 選流派遷移，一次性）／`_common.py`（**跨腳本共用常量，唯一真相** —— `OK/NG/WARN` 等符號一律從這裡 import，不要在各自文件裡重定義） | 對應的維護動作發生時才跑 |
| **⭐ `verify_all.py`** | **改完任何腳本或知識庫後（最後一關）** | **全鏈路驗證器**：A 介面（33 支 CLI 腳本 `--help`；純資料模組不計）／**B 冪等壓測 ×50**（第 2 輪起倉庫哈希必須零漂移——專抓「每次跑都改一點點」的非冪等 bug）／C 只讀腳本 ×50 輸出逐字一致／D 端到端真的走完 gate→role→budget→composer→selfcheck→depth→build_docx→run_pipeline **產出一份 .docx**／E 呼叫 `kb_audit`。`python scripts/verify_all.py [-n 50]` |
| **⭐ `kb_audit.py`** | **每次改完知識庫後（全量回歸）** | **連通性審計**：量 **7 條跨檔鏈路** —— 檔案引用可解析／打法→案例可達（**含「引用行＝實際取得到的卡」一致性**）／打法×行業全組合覆蓋率／**交付物注入（實跑 composer，數真實內容 vs 占位符）**／模型死條目 **＋ 可達率**／既有 6 腳本全跑／**私有痕跡掃描（公開倉庫紅線）**。**前 6 個腳本查「檔案內性質」，這支查「跨檔案連通性」——缺了它就會「零件全綠、傳動軸斷了」**（2026-09-17 用戶質問後建立） |
| **`fix_play_cases.py`** | 打法庫案例行過時時 | **批量修 `**案例**` 行**：只接受「品牌關鍵詞 @ 檔號」，由腳本解析**真實卡片**再生成引用（不可能寫出空引用）；解析失敗就整批不寫；硬不變式＝只改案例行 |
| `play_case_candidates.py` | 修案例行時輔助 | 按 bigram 給每條打法的候選卡短名單（**只是排序器，不做決定**） |
| **`case_play_index.py`** | 維護案例庫／打法庫後 | **案例卡 ↔ 打法 雙向索引**：從 `00-打法库` 每條的 `**案例**` 行反查出「哪幾張卡能抄這條打法」，寫 `scripts/case_play_index.json`（供 `composer.py` 消費）並在卡片標題下打 `> **可抄打法**：§1.1 超級符號 ｜ …`。**實測：全庫只有 32/104 條打法的案例行指名了品牌 → 未標記 ≠ 不適用** |
| **`file_meta.py`** | 改了章節或卡片後 | **檔頭自解釋塊**（這是什麼／什麼時候讀／讀完你能／目錄／規模）**全機械生成**，改完重跑即同步，永不與內容漂移 |
| **`case_sections.py`** | 改過案例檔標題後 | **章節標題收斂**到固定槽位（56 種寫法 → 9 個），序號按檔內實際順序重編；不符就報錯 |
| **`case_gap_fill.py`** | 案例庫缺口巡檢 | 檢查 01–45 是否都具備「本行業常見死法」「查不到的部分」兩節（缺就補，帶逐行還原不變式） |
| **`rename_tidy.py`** | 改了頂層檔名／序號後 | 檔名與 H1 統一 + **全庫引用同步**，硬不變式＝舊檔名不得殘留 |
| **`top_titles.py`** | 改了 05／06 頂層標題後 | 頂層工作檔的 `## ` 標題統一（層級修正＋序號連續），**只動標題行** |
| **`reformat_to_template.py`** | **客戶/比賽給了格式範本時** | **按範本重排**：解析範本骨架 → 按映射填入 → **硬要素核對**（丟一件就退出碼 1）。範本要而源稿沒有的章節會標【待補】；源稿多出來的整節移入附錄（協議 2：合併，不取捨） |
| `gate_check.py` | 門禁問完後、**動筆前** | 缺項 → **禁止產出任何方案內容**（協議 1） |
| `budget_check.py` | 寫完預算表 | 分項加總 ≠ 合計 → 不準交付（協議 3 第 7 項） |
| `selfcheck.py` | 交付前 | 缺章節／缺自檢單／內部文檔洩漏 → **不準交付**（協議 3） |
| `depth_check.py` | 交付前**跑一次、看報告** | **只診斷、不阻攔**（用戶定調 2026-09-14）。查 9 個維度：競品／風險四件套／KPI 預警線／物料／禁用詞／**重複度**／空話詞… 報告給你看，**改不改由你決定**。便利店開學季案原版同樣會有提示 —— 那是正常的 |
| `build_docx.py` | 最終出稿 | **內部跑 selfcheck（結構門檻：缺章節／缺自檢單／內部文檔洩漏 → 拒絕）**；depth_check 只印報告、**不阻攔** |
| **`role_check.py`** | 出稿前（**唯一入口的第 ② 關**） | 校驗《多 Agent 分工記錄》：5 個角色產出 ＋ 裁決記錄 → **缺就不出稿**（SKILL：分工每次必走） |
| **`case_lint.py`** | 維護案例庫時 | 體檢案例卡的「公司背景」違規（規則寫了、以前沒腳本查）；`--max` 設容忍上限（**預設 900**）；`--strict` 關閉身份欄／歸因欄豁免回到裸計數。規則與兩項例外見 `references/cases/README.md` §六 |
| **`case_upgrade.py`** | 維護案例庫時 | 體檢每張卡「五要素齊不齊 ＋ 字數夠不夠」（`--min`／`--list`／`--file NN`） |
| **`case_order.py`** | 改完案例卡後 | **要素順序規範化**（①→誰做的→②→洞察→③→④→落地拆解→⑤→可抄的點→適配／常見錯誤／本檔關聯→局限）；**零刪除**，帶「非空行 multiset 不變」硬不變式 |
| **`case_upkit.py`** | **批量升級案例卡時** | **工具包**（import 用，不是命令）：`blocks()` 定位卡 → `fill()` 按字段補塊 → `run()` 跑修復＋不變式校驗＋寫盤。**新寫一檔升級腳本只需寫「內容 dict ＋ 修復清單」，框架不用重寫** |
| **`case_digest.py`** | 升級案例卡前 | **卡片骨架摘要**：`python scripts/case_digest.py 27,28 120` —— 只印 ①／②／洞察／③／⑤／局限，**一屏看完一檔，不用讀整檔**（決定這檔是「補尾塊」還是「寫正文」） |
| **`case_scan.py`** | 升級案例卡前 | **斷句損壞掃描**：`python scripts/case_scan.py 27` —— 按「粗體後缺冒號／句尾被截／括號不成對／替代符殘留／粗體不成對」五類，打印**完整損壞行**，便於一次寫完精準修復 |
| **`smoke_test.py`** | 改完任何腳本後 | 一條命令跑 12 項冒煙檢查（該過的過、該攔的攔） |

**這是本 skill 最強的強制點**：交付物只能由 **`run_pipeline.py`** 產出（它串起門禁→預算→自檢→診斷→出稿）—— **拿不到 `.docx` ＝ 沒完成**，AI 繞不過。

```bash
# ⛔ 唯一出稿路徑（任何一關不過就中止）
python scripts/run_pipeline.py --rules rules.json --budget budget.json \
    --plan plan.md -o 方案.docx --title "客戶名 營銷方案" --date 2026-09-16
```

```bash
# 完整流程（依序，前一步不過就不准往下）
python scripts/gate_check.py gate.json        # ① 門禁放行（13 項 + 規則表已確認）— 單獨跑用於除錯
python scripts/budget_check.py budget.json    # ② 預算校驗（含盈虧線測算）
python scripts/selfcheck.py plan.md           # ③ 交付前自檢（結構/自檢單/洩漏/禁用詞）
python scripts/build_docx.py plan.md -o 方案.docx \
    --title "客戶名 營銷方案" --date 2026-09-14   # ④ 出稿（自動先跑③）
```

**依賴**：Python 3 + `python-docx`（`pip install python-docx`）。

**沒有代碼執行能力時**（純對話環境）→ 退回 Markdown 協議：
- 手工對照 §六 清單逐項核對
- 把 12 項《交付自檢單》**原樣輸出**在回覆中並標 ✅/❌
- 附「樣式對照說明」（哪一級是標題幾、哪些是表格），讓用戶貼進 Word 一鍵套樣式

> 不論走哪條路，交付物的**內容結構**必須符合 §三 的八篇 + 附件規範。

---

## 五、對接實測清單（門禁用 / 見客戶照着問）

> 用法：對接時按這張表問，當場填。**答不上來的先留空，不猜不編。**

### A. 生意基本盤
| # | 要問到什麼 | 怎麼問（原話） |
|---|---|---|
| A1 | 賣什麼、客單價 | 「你們主要賣什麼？平均一單多少錢？」 |
| A2 | 毛利 | 「一單大概賺多少？成本主要在哪些地方？」 |
| A3 | 賣給誰、什麼場景 | 「來買的主要是什麼人？什麼情況下會想到你們？」 |
| A4 | 現在規模 | 「一個月大概做多少？幾家店／多少用戶？」 |
| A5 | 復購頻次 | 「老客戶一般多久來一次？回頭客佔多少？」 |

### B. 目標與卡點
| # | 要問到什麼 | 怎麼問 |
|---|---|---|
| B1 | 生意目標 | 「這段時間最想解決的一件事是什麼？做到什麼算成功？」 |
| B2 | 自述卡點 | 「你覺得現在最大的問題是什麼？」 |
| B3 | **真實卡點**（追問） | 「這個問題是什麼時候開始的？之前試過什麼？為什麼沒成？」 |
| B4 | 時間要求 | 「希望多久看到效果？有沒有必須卡住的時間點？」 |

### C. 預算與決策
| # | 要問到什麼 | 怎麼問 |
|---|---|---|
| C1 | 預算區間 | 「這件事打算投入多少？是一次性還是按月？」 |
| C2 | 誰出錢 | 「這筆錢是你自己出還是有其他來源？」 |
| C3 | 決策人 | 「這個決定是你拍板還是要跟誰商量？」 |
| C4 | 付款習慣 | 「之前跟外面合作，付款方式一般怎麼談？」 |

### D. 資源與能力
| # | 要問到什麼 | 怎麼問 |
|---|---|---|
| D1 | 團隊 | 「現在有幾個人？誰能騰出手做內容／跑物料？」 |
| D2 | 私域基礎 | 「有沒有加過客人微信？有沒有群／會員？」 |
| D3 | 數據基礎 | 「收銀／訂單數據能導出來嗎？有沒有後台？」 |
| D4 | 老闆出鏡意願 | 「你自己願不願意出鏡拍視頻？」 |
| D5 | 供應鏈 | 「產品是自己做的還是進貨？能不能改規格／包裝？能不能退？」 |

### E. 競爭與市場
| # | 要問到什麼 | 怎麼問 |
|---|---|---|
| E1 | 對手 | 「生意最好的是誰？他們憑什麼？」 |
| E2 | 差異點 | 「客人為什麼選你不選他？」（追問：有客人原話嗎？） |
| E3 | 價格水位 | 「同行大概賣多少錢？」 |
| E4 | 對手軟肋 | 「他們有什麼做不到的？」 |

### F. 過去做過什麼
| # | 要問到什麼 | 怎麼問 |
|---|---|---|
| F1 | 做過什麼營銷 | 「之前做過哪些推廣？花了多少？」 |
| F2 | 有效的 | 「哪次效果最好？好在哪？」 |
| F3 | 無效的 | 「哪次完全沒用？你覺得為什麼？」 |
| F4 | 被誰坑過 | 「之前有沒有找過外面的人做？結果怎樣？」 |

### G. 硬約束
| # | 要問到什麼 | 怎麼問 |
|---|---|---|
| G1 | 行業監管 | 「你們這行廣告有沒有什麼不能說的？」（醫藥／金融／教育／房產重點問） |
| G2 | 場地/場域限制 | 「場地有什麼規矩？（能不能外擺、能不能貼、能不能進）」 |
| G3 | 品牌授權 | 「能不能用品牌方的 LOGO／VI？」 |
| G4 | 資金狀況 | 「現金流緊不緊？能承受多久不回本？」 |
| G5 | 老闆偏好／忌諱 | 「有沒有什麼是你一定不想做的？」 |

### H. 交付期待
| # | 要問到什麼 | 怎麼問 |
|---|---|---|
| H1 | 要什麼 | 「你要的是一份方案，還是要人幫你執行？」 |
| H2 | 驗收方式 | 「你怎麼判斷這件事做成了？」 |
| H3 | 對接人 | 「日常對接找誰？多久對一次？」 |
| H4 | 給誰看 | 「這份東西最後給誰看？」 |
| H5 | **目標字數／頁數** | 「這份東西有字數或頁數要求嗎？」（比賽、招標、客戶模板經常會規定）→ 沒要求就按 **5,000–10,000 字**預設 |
| H6 | 有沒有參考格式 | 「有沒有指定的模板或提交清單要我照着寫？」（→ 觸發協議 2：合併不取捨） |

---

## 六、交付前自檢清單（每份方案必跑）

> **與協議 3 的關係（別搞混）**：
> - **本節 = 完整內部核對清單**（更細），用來逐條檢查、發現問題。
> - **協議 3 的《交付自檢單》= 必須原樣輸出給用戶的摘要版**（12 項，標 ✅/❌）。
> - 順序：**先跑本節逐條核對 → 改完 → 再把協議 3 的摘要單輸出在交付回覆中。** 兩者不是二選一。

出稿前逐條核對，**任一不過就回去改**：

**數字類**
- [ ] 預算表分項加總 = 合計？
- [ ] 盈虧線/保本點算得對不對（動銷率 × 毛利 − 固定成本）？
- [ ] 引用的案例數據都有來源標註？【未核實】的數據有沒有從對外交付物裡剔除？
- [ ] KPI 目標是否可測（有基準值、有觀測方式）？

**執行類**
- [ ] 每條動作都寫了「誰做」？
- [ ] 每個負責人的工作量加起來，他一個人有時間做完嗎？（**執行人力檢查**）
- [ ] 如果人力不夠，刪減順序寫了嗎？
- [ ] 所有時間節點對得上日曆嗎（有沒有撞上假期/週末/已定事件）？

**風險類**
- [ ] 對照 `04-失败归因总库.md` 過一遍，本方案最可能死在哪？
- [ ] 關鍵假設列出來了嗎？每條有驗證方式與成本嗎？
- [ ] 假設不成立時的備用方案有嗎？
- [ ] 合規紅線（廣告法／行業監管／平台規則）過了嗎？
- [ ] 與客戶的硬約束有沒有衝突？

**表達類**
- [ ] 有沒有出現「做私域」「加強內容」「提升品牌力」這類沒有動作的廢話？
- [ ] 結論有沒有前置（第一頁就能看到做什麼）？
- [ ] 禁用詞與對外口徑寫清了嗎？

**方法論類（2026-09-16 新增，對應 §三 硬要求 4、5）**
- [ ] **打法組合表**在嗎？8 列齊嗎（打法／為什麼用它／具體動作／誰做／花多少／多久見效／驗收指標／可抄案例）？
- [ ] 每條打法的「**為什麼用它**」寫的是說服邏輯（差異化／成本／對手做不到），不是形容詞？
- [ ] **可抄案例**列引用了 `cases/` 具體卡片嗎？
- [ ] 診斷章有 **A–H 問題類型標籤**嗎？
- [ ] 卡點寫成了**重構句**（「不是 X——是 Y」）並給了**核心動作一句**嗎？
- [ ] 風險每條寫全四件套了嗎（為什麼會發生／預警信號／兜底預案／預防動作）？**有沒有把「模式 NN」這種內部編號寫進稿子**？
- [ ] 有沒有寫「**不承諾的事**」（防期待錯位）與「**下一步只寫一件**」（先導閥）？

---

## 七、知識合併步驟（把線下拿到的知識寫回 skill）

> **每次對接／項目結束後執行。** 操作原則：**增量續寫，不整篇覆蓋**；不新建重複頁。

| 你拿到的東西 | 合併到 | 具體位置 |
|---|---|---|
| 一個新的**營銷打法** | `references/00-打法库.md` | 對應打法分類下新增條目 |
| 一個新的**案例** | `references/cases/{行業}.md` | 「案例清單」加一條；值得深挖的寫進「深度拆解」 |
| 一個**行業常識／潛規則** | `references/cases/{行業}.md` | 「本行業打法地圖」或新增「行業概覽」節 |
| 一次**失敗** | `references/04-失败归因总库.md` | 對應失敗模式下的實例；新模式則整節新增 |
| 一個**新的問法** | 本文件 §五 | 對應分組加一行 |
| 一個**新的方法論／工具** | `references/03-方法论操作手册.md` | 對應類別下按七段結構新增 |
| 一個**新的行業** | `references/cases/` | 新建行業文件，沿用五節結構 |
| 一份**完整的交付方案** | `references/cases/{行業}.md` 或新建 `projects/` | 作為範例存檔（去識別化後） |

**合併紅線（❌ 不寫進 skill）**：
- 公司背景：創始人、董事長、成立年份、股權、融資、員工數
- 機構的營收／人事／獎項數量（除非它是這個案例的效果數據）
- 與營銷動作無關的行業八卦
- 未經核實的數據
- 客戶隱私：真實客戶名、聯絡方式、報價底線

**合併後要做**：更新本文件 §四 索引（若新增文件）→ 更新 §八 變更記錄 → 舊內容過時則**升級替換**。

### 改案例庫後的腳本鏈（順序很重要）

```bash
python scripts/file_meta.py                      # ⓪ 檔頭自解釋塊（改了章節後重跑，目錄行自動同步）
python scripts/case_sections.py --fix            # ① 章節標題規範化（改標題後必跑）
python scripts/case_order.py --fix               # ② 卡片要素順序（零刪除，帶不變式）
python scripts/case_upgrade.py --list            # ③ 看還差多少（五要素齊 ＋ ≥2500 字）
python scripts/case_lint.py                      # ④ 查「公司背景」違規（--strict 裸計數）
python scripts/case_gap_fill.py                  # ⑤ 檢查 01–45 的「常見死法／查不到」兩節齊不齊
python scripts/case_play_index.py --fix          # ⑥ 重建「案例卡 ↔ 打法」索引 ＋ 打「可抄打法」標
python scripts/kb_audit.py                       # ⑦ **連通性審計（全量回歸，七條鏈路）**
python scripts/verify_all.py -n 50               # ⑧ **全鏈路驗證 ＋ 50 遍冪等壓測（最後一關）**
python scripts/smoke_test.py                     # ⑨ 改過任何腳本後
```

**改了 `00-打法库` 的 `**案例**` 行之後**，**必須**跑 ⑥ —— 那是「打法 ↔ 案例」兩邊唯一的連線來源；
不跑，骨架裡的「可抄案例」就會退回占位符（或用戶端直接看不到卡片內容）。

> ⚠️ **改案例卡後必須跑 ① + ② + ③**。歷史上多次批量改造把「③ 做了什麼」搬到卡片最後、
> 或把 `### 3.2` 標題的換行吞掉（變成 `---### 3.2`，讓它不再是一級標題）——**這類損壞肉眼很難發現**，
> 靠 `case_order.py`／`case_sections.py` 的機械校驗 ＋ 硬不變式才能保證不丟內容。

**⛔ 不寫進來的**：公司背景（創始人／成立年份／股權／融資／營收／人事）、未核實數據、客戶隱私。
**原則**：增量續寫，不整篇覆蓋；同名迭代走**升級替換**，不版本堆疊。

> 新增或改名任何檔 → **必須同步三處**：本文件 §四 索引、`references/cases/README.md`（若涉及案例檔）、
> `AGENTS.md` §五 檔案地圖。

---

## 八、變更記錄

- 2026-09-14 初版：以「機構檔案」為組織方式。
- 2026-09-14 二次擴充：補國際廣告集團與中國創意熱店。
- 2026-09-14 三次擴充：補其他咨詢公司。
- 2026-09-14 **方向糾正（第一次）**：用戶指出內容主體應是「乙方為甲方做的案例」與「能直接調用的營銷方法」，而非公司介紹；機構檔案移至 `archive/`。
- 2026-09-14 **方向糾正（第二次）**：定位為「商業對接後快速生成營銷方法」的生成器；新建 18 個行業案例集（條目式清單 + 深度卡）。
- 2026-09-14 **第三次修正（本版）**：以便利店開學季案（事實底稿 ＋ 交付稿）為質量基準，新增 ——
  1. **第 0 步門禁**：12 項必問清單，沒問清不出方案；
  2. **六步工作流**：事實底稿 → 現狀分析 → **多 Agent 分工（每次必走）** → 主理人裁決 → Word 交付；
  3. **交付物規範**：一個 `.docx`，八篇 + 附件，簡體（篇幅規則見下方第四次修正，**「≥ 2 萬字」已廢除**）；
  4. 新增必寫章節：禁用詞與口徑、執行人力檢查、關鍵假設與驗證、待解決問題清單；
  5. 新增 **§六 交付前自檢清單**（數字／執行／風險／表達四類）。
- 2026-09-14 **第四次修正（強制執行協議版）**：針對「執行者把 skill 當靈感庫而非驗收標準」的復盤——
  1. 頂部新增 **⚡ 強制執行協議** 五條：啟動即立《任務規則表》（門禁一次問全＋糾偏即升級）、結構衝突「合併不取捨」、**交付前 12 項自檢單必須原樣輸出在交付回覆中**、工具盤點先於路徑（連接器優先、禁默認爬蟲、付費生成先報價）、交付形態紅線（內部過程文檔不得入交付稿）；
  2. **篇幅規則修改**：刪除「正文 ≥2 萬字」硬編碼，改為「門禁先問用戶目標字數，未指定時預設 5,000–10,000 字」，深度以硬要素齊全衡量、不以字數注水；
  3. `references/04-失败归因总库.md` 新增附錄「AI 執行者使用本生成器的失敗模式」5 條（骨架式使用/跳過自檢/門禁漏項/工具慣性繞路/結構衝突做減法）。

- 2026-09-16 **架構整理版**：
  1. 頂層文件按流程順序重排：`00-打法库`（引擎）→ `01-接案输出模板` → `02-多Agent分工简报` → `03-方法论操作手册` → `04-失败归因总库` → `05-小企业与新品牌从零打造` → `06-选流派矩阵与对照表`（選型入口）；全部交叉引用同步更新（SKILL/AGENTS/README/内部引用，共 9 文件 115 处）；
  2. **機構案例庫併入 cases/**：4 冊轉為 `cases/46-49`，統一為案例卡格式——機構冊**每戰役一卡**（誰幫誰做了什麼＋結果＋可抄的招）、書籍冊**每書一卡**，共 192 卡；導言保留選冊判斷邏輯並改寫為門禁清單與對接實測清單；
  3. 案例庫規模（**2026-09-17 最終口徑**）：**51 類 / 649 張深度卡** ＝ 01–45 行業檔 411 張 ＋ 46–51 機構與方法論檔 238 張。
     （口徑統一為「編號深度卡」數；歷史數字 419 案 → 611 案 → 628 案 → 620 卡均已作廢，勿再引用。）

- 2026-09-16 **質量門檻增補（用戶以《中百羅森新生開學引流方案》為基準糾偏）**：
  1. 新增**硬門檻 4：打法組合表**（8 列，缺「為什麼用它」或「可抄案例」＝不合格；`selfcheck.py`【7a】機械攔截）；
  2. 新增**硬門檻 5：診斷三件套**（A–H 問題類型＋卡點重構句＋核心動作一句）；
     ⚠️ 同一批還加了「風險掛『模式 NN』編號」—— **該條已於 2026-09-17 廢除**（那是內部座標，
     selfcheck 第【10】關會硬攔）。保留這行只為說明來龍去脈，**不要照做**。
  3. **A–H 問題類型分類首次明確定義**（第 2 步，此前 SKILL/模板兩處引用卻無定義 —— 完整性缺口已補）；
  4. `selfcheck.py` 新增【7】核心方法論要素四項校驗；`01-接案输出模板.md` 新增「2.0 打法組合表」完整規格與範例；范式增補第七章；
  5. §六 交付前自檢清單新增「方法論類」7 條。

- 2026-09-16 **公開倉庫整理版**：
  1. **範例集中化**：新建 `references/范例/`，收入 `便利店开学季战役-交付稿.md/.docx`（v3 實測版）與 `-原版.md`；
  2. **去識別化**：範例與質量範式中的具名主體全部替換（某連鎖便利店／某高校／本地批發市場），
     保留全部數字與方法論；`cases/14` 的公開商業案例（百嶼森）不受影響；
  3. **命名統一**：`範本-罗森案范式.md` → `07-质量范式-便利店开学季案.md`（接入 00–08 編號體系）；
     `範本-罗森案范例（用户认可原版）.md` → `范例/便利店开学季战役-原版.md`；「羅森案」代稱 → 「便利店開學季案」；
  4. **語言統一**：內部文檔一律繁體（cases/44–50 已轉正）；**範例樣張一律簡體**（交付物語言）。

- 2026-09-17 **結構與可用性整理版（五件事一次做完）**：
  1. **融 README**：`references/README.md` 零獨有內容（維護規則已在 SKILL §七）→ 移除，3 個 README 收成 2 個（根 ＋ `cases/README.md`）；
  2. **補 16 檔缺口**：`20 21 23 24 26 27 28 29 32 33 40 41 42 43 44 45` 各補「本行業常見死法」＋「查不到的部分」
     （新增 `scripts/case_gap_fill.py`，帶「刪掉插入塊須逐行還原原文」硬不變式）；
  3. **06 進 cases**：頂層 06（本土数字营销与MCN）→ **`references/cases/51-本土数字营销与MCN.md`**（29 卡，
     甲乙丙丁四組：本土媒介代理集團／MCN／新消費操盤複盤／平台官方模型），補齊六槽結構；
     **05／07 只統一標題**（`scripts/top_titles.py`：05 的「階段一～五／路徑一～三」由 H2 降 H3，H2 序號 〇～十二 連續）；
  4. **重新梳理命名**：頂層序號補空 **07／08／09 → 06／07／08**；H1 統一為「名稱（＋語義限定詞）」；
     全庫引用同步 68 處（`scripts/rename_tidy.py`，硬不變式＝舊檔名不得殘留）；
  5. **讓知識庫真的被用上**（修的是機制，不是文字）：
     a. `composer.py` 的「可抄案例」**注入真實卡片內容**（品牌＋做了什麼＋結果），不再印檔案路徑占位符；
     b. 「理論依據」**注入 03 模型的「解決什麼問題＋前幾步」原文**，不再只給編號；
     c. 新增 **§2.0.2 知識庫缺口**：連不出案例的打法**逐條列出並要求補**——**寧可空著，禁止編一個案例**；
     d. 新增 `scripts/case_play_index.py`：從 `00-打法库` 的案例行**反查**「案例卡 ↔ 打法」雙向索引
        （`scripts/case_play_index.json`），並在卡片標題下打 `> **可抄打法**：…`。
  - ⚠️ **本版留下的最大待辦（已於同日修完）**：`00-打法库` 的 104 條 `**案例**` 行是**早期 6 檔時代寫的**
    ——只有 **32/104** 指名了品牌，且只覆蓋 `01–06` 六個檔；全庫 649 張卡裡**只有 14 張**能自動對上打法。
    這就是「庫裡的東西沒被用進交付物」的**唯一機制根因**。

- 2026-09-17 **連通性修復版（用戶質問「为什么还是有这么多问题」後建立）**：
  **根因**：既有 6 個校驗腳本（`file_meta`／`case_sections`／`case_order`／`case_upgrade`／
  `case_lint`／`smoke_test`）查的**全是「檔案內性質」**，沒有一個查**「跨檔案連通性」** ——
  所以 60/60 檔頭合規、51/51 章節一致、409/409 卡達標、12/12 冒煙全綠，
  而唯一的產出引擎 `composer.py` 還在印「來源 cases/xx.md，請展開…」。
  **零件全合格，機器的傳動軸是斷的，而沒有一支儀器量傳動軸。**
  1. **新增 `scripts/kb_audit.py`（連通性審計）**：量 6 條鏈路 —— L1 檔案引用可解析／
     L2 打法→案例可達／L3 案例→打法覆蓋率／L4 交付物注入（實跑 composer，數真實內容 vs 占位符）／
     L5 模型死條目／L6 既有 6 腳本全量回歸。**斷鏈類（L1/L2/L4/L6）才影響退出碼，覆蓋率類只報數**。
  2. **修復 `00-打法库` 全部 104 條案例行**（新增 `scripts/fix_play_cases.py`）：
     只接受「品牌關鍵詞 @ 檔號前綴」，由腳本從 `cases/` 解析出**真實卡片**再生成引用 ——
     因此不可能寫出空引用。**修復中發現 `農夫山泉`、`星巴克` 在庫裡根本沒有卡片**（舊行引的是不存在的卡）。
     硬不變式：每條只改 `**案例**` 一行，其餘逐行不變。
  3. **修 `composer.parse_cards` 的三格式兼容 bug**：第一版只認 2 種案例清單寫法，
     `29／32／33／51` 用的第三種（`N. **品牌**｜…`）靜默回 0 張卡 —— **這個 bug 是 kb_audit 抓出來的**。
     同時支援機構檔（46–51 無案例清單，卡片即 `### 3.N 標題`）。
  4. **效果**：
     | 指標 | 修復前 | 修復後 |
     |---|---|---|
     | L1 檔案引用斷鏈 | 3 | **0** |
     | L2 打法→案例 可達 | 19/104（18%） | **104/104（100%）** |
     | L4 交付物占位符 | 3 | **0**（真實卡片內容 6 → 18 處） |
     | 卡片「可抄打法」標記 | 14 張 | **75 張 / 27 檔** |
     | 既有 6 腳本 | 全綠 | 全綠（未被破壞） |
  5. 新增 `scripts/play_case_candidates.py`（候選卡排序器，修案例行時輔助）。**腳本表 26 → 29**。
  6. **方法論教訓（寫進 `AGENTS.md` §四 降級表旁）**：
     ① 校驗要分「零件合格」與「鏈路可通」兩類，**只查前者必然漏掉後者**；
     ② 指標不要自證 —— 上限／閾值由自己定，達標率就永遠好看；
     ③ **每次改動都要全量回歸**，不能「改哪查哪」，否則早期產物會被庫的增長甩成斷鏈。

- 2026-09-17 **L3／L5 修復 ＋ 全鏈路 50 遍自檢版**：
  1. **L3 先修「設計」再修「數字」**：查下去發現真問題不是覆蓋率低，而是
     `pick_cards` **完全沒用行業檔** —— 銀發養老的客戶會被配上美妝／茶飲的例子，**「可抄案例」不可抄**。
     改成兩段式：**1 張精準對標（打法案例行指名）＋ 1 張本行業可抄**。
     結果：(打法 × 行業) **5304 組全部有卡（空手 0）**，其中 **2375 組（45%）含本行業卡**；
     其餘是「該行業確實沒有語義匹配的卡」——**不硬湊**，只回跨行業標桿。
  2. **卡片宇宙口徑統一**：`kb_audit` 原本數「深度卡 609」，而 picker 操作的是
     「案例清單條目 ∪ 深度卡 983」——**兩個分母對不上，覆蓋率算出來是假的**。
  3. **`parse_cards` 取「案例清單 ∪ 深度卡」並集**：實測 25 個檔裡有 **48 張深卡不在自己的案例清單裡**，
     只讀清單的 Agent 永遠看不到、composer 也引用不到。
  4. **L5 修到底（兩層）**：第一層「映射表引用但手冊裡不存在」＝死條目 → 0；
     第二層才是真問題 ——**「被 JSON 引用」≠「挑得到」**：接入 33 個模型後仍只有 14 個能被
     `theory_for` 選中。修法：① `theory_for` 的 override 由「整組替換」改**可累加**；
     ② override 由 `[:3]` 截斷改為**人手指定的全部給出**；③ 逐條把 49 個模型綁到它真正該管的打法。
     **結果：111/111 模型全部可達（0 死條目、0 未接線、0 挑不到）。**
  5. **新增 `scripts/verify_all.py`（全鏈路驗證 ＋ 50 遍冪等壓測）**：
     A 介面（33 支 CLI 腳本 `--help`；純資料模組不計）／B **冪等 ×50**（第 2 輪起倉庫哈希必須零漂移）／
     C 只讀腳本 ×50 輸出逐字一致／D **端到端真的產出一份 .docx**（gate→role→budget→composer→
     selfcheck→depth→build_docx→run_pipeline）／E 呼叫 `kb_audit`。
     **實測 50 遍：全綠、倉庫零漂移（5 分 54 秒）。** 腳本表 29 → 30。
  6. **本輪又抓到 4 個真 bug**（全部由新儀器發現，非事後諸葛）：
     · `fix_play_cases.resolve` 用「標題最短」判定 → 選中副卡（王老吉被選成「禮品場景」那張）
     · `hint_candidates` 丟掉「品牌↔檔」對應 → 同一品牌跨檔重複抽出（淄博燒烤、石頭科技）
     · `play_overrides` 有個 2 字符舊鍵 `'VI'` **短鍵遮蔽長鍵** → 改為最長鍵優先
     · `verify_all` 自己的 CLI 寫錯（gate_check／role_check 收位置參數）、哈希基線取早了
  7. **新增不變式「引用行＝實際取得到的卡」**（L2 的一部分）：案例行寫的品牌，
     composer 必須真的取得到 —— 這條一上線就暴露 11 條舊的「描述式」案例行，已一併重寫。
