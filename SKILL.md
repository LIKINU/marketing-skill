---
name: marketing-playbook
description: 营销方案生成器（**强制流程 skill，违反＝本次使用失败**）。商业对接后把客户情况喂进来 → 产出一份完整 Word 交付文档（现状分析 + 策划方案 + 预算 + 行动清单）。**启用后第一件事必须是执行 §0 门禁（13 项一次问全）并输出《任务规则表》等用户确认 —— 确认前不得产出任何方案内容；交付前必须原样输出 12 项自检单。** 若用户已给足资料，仍须先出《任务规则表》确认再动笔。触发场景：见完客户要出方案、接到新案子、要诊断客户卡点、要比稿。
agent_created: true
规范依赖: AI协作总规范 §2.1 硬规则 · §2.4 写文档前三问 Gate · §2.6 语言约定 · **§4.8 质量工程（本 skill 是它的实战来源）**
---


> ⛔ **开新会话第一步**：先跑 `python scripts/start_here.py --client "客户情况"`。
> 它输出「**该读哪几份、读哪一节**」＋「**明确不要读什么**」—— 仓库已 263 万字，
> **通读是最大的浪费**。核心思路：**用「查」代替「读」**（`--grep` 也支持）。
> 派 subagent 时改给 **`AGENT-BRIEF.md`**（约 3500 字的仓库快照）＋ 1–2 个目标档的那一节。
# 营销方案生成器

## 📍 第 -1 步：你要做什么 → **只读这几份**（本 skill 唯一导航）

> ⛔ **本表是全库唯一的导航来源。** `README.md`／`AGENTS.md` 里的文件地图只是**给人看的镜像**，**与本表冲突时以本表为准**。
> **一次最多同时打开 3 份文件。** 本 skill 是为「按需加载」设计的 —— **通读 = 读不完 + 抓不到重点**。

| 你现在要… | 只读这些 | 明确**不要**读 |
|---|---|---|
| **第一次接入／安装到某平台** | 本档（SKILL.md）全文 ＋ `AGENTS.md` | — |
| **接了一个新案**（拿到客户资料那一刻） | `references/08-操作流程SOP.md`（S0–S8 逐步：输入／命令／产出／校验） | 所有 `cases/` |
| **动笔前出骨架** | `references/00-打法库.md`（§0 总表一页速查 → 挑中的条目） | `cases/46–51` |
| **写方案正文** | `references/01-接案输出模板.md`（逐章）＋ `references/03-方法论操作手册.md`（挑 ≥3 个模型）＋ `references/07-质量范式-便利店开学季案.md`（**深度标尺**） | `cases/46–51` |
| **找同行参照案例** | **先查下方〈反向索引〉拿到档号** → `references/cases/{档号}-{文件名}.md`（进档先读「案例清单」，再跳「深度拆解」） | **其余 44 个行业档**、`cases/46–51` |
| **选乙方／看某机构做过什么** | `references/cases/46–51`（机构与方法论类） | `cases/01–45` |
| **多 Agent 分工** | `references/02-多Agent分工简报.md` | — |
| **交付前风险自检** | `references/04-失败归因总库.md` | — |
| **客户是小企业／新品牌** | `references/05-小企业与新品牌从零打造.md`（**这是主入口**，不是补充） | — |
| **要用平台官方模型** | `references/cases/51-本土数字营销与MCN.md` | — |
| **判断某件事该外包还是自己干** | `references/06-选流派矩阵与对照表.md` | — |
| **想看成品长什么样** | `references/范例/便利店开学季战役-交付稿.md` | `-原版.md`（仅供对照） |
| **维护／扩充本 skill** | 本档 §七 ＋ `references/08-操作流程SOP.md` §S8 | — |
| **改脚本** | `scripts/` 对应脚本文件头注解 ＋ 改完跑 `python scripts/smoke_test.py` | `references/` 全部 |

**⛔ 执行时明确不要读**（读了浪费上下文，还会误导）：
- `README.md` —— 给**人**看的说明书，执行方案时不需要
- `AGENTS.md` —— 只在**接入新平台**时读一次
- `references/cases/` 里**与客户行业无关**的档（50 个文件只有 1 个跟你的案子有关）
- `references/cases/46–51` —— **只有要选乙方／引用方法论时才读**，跟行业案例无关
- `references/范例/` 内的 `-原版.md`、`references/cases/*` 的「本行业对接实测清单」（与 SKILL §五 重复）

**不确定读哪个 → 读 `references/08-操作流程SOP.md`。** 它把每一步该读什么、该跑什么命令都写死了。

### 🔎 反向索引：拿到客户 → 直接查档号（**不要在 45 个文件名里猜**）

> 这张表存在的唯一理由：第 -1 步原本只写「你客户所属的那 1 个文件」，**Agent 得自己列 45 个文件名再猜**——
> 这才是「找不准」的真因。查到档号后，路径是 `references/cases/{档号}-{文件名}.md`。

| 客户是什么 | 档号 |
|---|---|
| **吃 · 喝** | 餐饮／茶饮／快餐／中式连锁 **01**｜现制茶饮咖啡烘焙 **27**｜餐饮供应链与调味品 **39**｜预制菜与方便速食 **40**｜饮料乳品酒零食 **02**｜农业生鲜与供应链 **29** |
| **穿 · 美 · 戴** | 服饰运动户外 **04**｜美妆个护 **03**｜珠宝钟表 **32**｜箱包皮具配饰 **33**｜眼镜与视光 **31**｜香氛与气味 **34**｜奢侈品与高端消费 **17** |
| **住 · 行** | 家居建材家装 **13**｜汽车与出行 **06**｜酒店与度假 **18**｜物流与快递 **45** |
| **零售 · 渠道** | 零售商超与便利店 **14**｜二手与循环消费 **24**｜跨境电商与出海 **20**｜文具文创与办公 **41**｜母婴与宠物 **12** |
| **人 · 服务** | 教育文旅与本地生活 **15**｜健身与运动健康 **36**｜口腔与专科连锁 **38**｜银发经济与健康养老 **23**｜宠物服务 **28**｜婚庆与人生仪式 **43**｜摄影与影像 **42**｜招聘与雇主品牌 **44**｜医疗美容与轻医美 **37**｜医药健康与器械 **11** |
| **科技 · 工业 · 能源** | 3C 数码与家电 **05**｜互联网平台与 APP **07**｜游戏与泛娱乐 **08**｜AI 产品与营销 **09**｜企业服务与 SaaS **21**｜B2B 工业品与区域品牌 **10**｜新能源与绿色消费 **22**｜美容仪与个护小家电 **35** |
| **内容 · IP · 赛事** | 潮玩文创 IP **25**｜影视综艺与内容 IP **26**｜体育赛事与运动俱乐部 **30** |
| **公共 · 区域 · 金融** | 城市与区域公共品牌 **19**｜金融与保险 **16** |

**查不到精确匹配时**（如「社区药店」「眼镜批发商」）：
1. 先找**最近的 1 档**（药店 → **11** 医药健康与器械；眼镜批发 → **31** 眼镜与视光），
2. **在方案里明说档位不完全对应**，并补一句「差异在哪」——
   ⛔ **不要因为不完全匹配就不引用**，也不要硬套（引错比不引更伤）。

---

## ⛔ 停 —— 执行者先读完这一段（任何情况下不得跳过）

**你不是在「参考」这份文件，你是在执行一套流程。** 四条不可谈判：

**0. 动手前必读质量标尺：`references/07-质量范式-便利店开学季案.md`。**
   它定义了「做到什么程度算够」——**83 项量化指标 + 16 个骨架模板 + 每个角色的产出规格**。
   **不读它 = 不知道深度标准 = 必然产出空壳。** 这是执行失败率最高的一环（最常见的症状是：产出看起来完整，但没有一个判断站得住）。

1. **不得先写方案后补流程。** 门禁没问完、任务规则表没经用户确认 → **一个字的方案内容都不准写。**
2. **每一轮回复都必须以一行《流程状态》开头**（格式见下）。**缺了这一行，代表你已经跑偏。**
3. **发现自己跳过了任何一步 → 立即停手，回到该步重做**，并在回复中明说「我发现跳过了 X，现在回到该步」。

### 每轮必须输出的状态行（固定格式，纯文字，任何平台都能输出）

```
【营销方案 · 流程状态】门禁 13 项：{已问全数}/13（缺：{逐项列出}）｜ 任务规则表：{未出 / 待确认 / 已确认}｜ 当前阶段：{收集中 / 分析 / 分工生成 / 裁决 / 自检 / 已交付}｜ 减法自检：{未做 / 已做（主线：{一句话}｜反对意见：{有／无}）}
```

**为什么要这一行**：它把「有没有遵守流程」从**承诺**变成**可被外部检查的事实** —— 用户看一眼就知道你走到哪一步、有没有跳步。没有它，所有「我会遵守」都是空话。
> 末栏「**减法自检**」只在进入《自检／已交付》阶段时填：**主线**能不能压成一句话、**有没有**一条反对客户既有判断的意见（见 §三 减法规则第 3、4 条）。这两件无法机械校验，但正是决定方案成败的两件。
> 非营销任务时，本行不输出。

**最常见的失败方式**：执行者把本文件当「灵感库」而非「验收标准」—— 跳过门禁、跳过自检、用自创的简化检查替代本文件的清单。**本文件的清单是唯一合法的检查标准。**

---

## ⛔ 最高优先级条款（P0 · 优先于本文件一切其他章节，含下方协议 1–8）

> 这四条由 2026-09-17 一次真实返工事故反推（一个校园大赛项目书 1.5 万字**重做三次**）。
> **根因不是模型偷懒，是「默认以自己的结构生成全文」凌驾了「用户给定的结构」。**
> 完整协议（6 条铁律 ＋ 交付前自检 8 项 ＋ 4 条结构性根因）见 `references/11-防返工交付协议.md`。

**P0-1 · 用户给定的结构 > 本 skill 的一切默认结构与默认流程**
- 当用户／主办方／招标方提供了模板、大纲、目录、提交清单时，**它的优先级高于本 skill 的八篇结构、门禁顺序、自检清单等一切默认**。
- 一级与二级标题**必须逐字沿用**：不替换成「更好听」的说法、不合并、不拆分、不重排、不自造层级；模板自带的板块名（哪怕平淡）一个字都不动。
- 模板未覆盖但必须有的内容 → 作为**新增同级／下级章节**补进去，而不是改造原有标题。
- 模板写「仅供参考、可调整」时，**默认仍原样保留**，除非用户明确要求改。

**P0-2 · 用户给的素材要「看得见地插进去」，不能溶解**
- 用户给的大纲／章节清单，必须作为**文档中真实存在的可见章节**出现，**保留原本的章节名与编号**（如「一、活动背景」「4.2 核心活动模块」）。
- ⛔ 禁止把用户大纲的内容打散、揉进别人的模板小节里——那等于用户的大纲消失了。位置可按逻辑排，但**章节名与编号要在正文里看得见**。

**P0-3 · 「skill 自检通过」≠「交付达成」**
- 本 skill 的自检单（协议 3）通过，**只代表满足了本 skill 的默认标准，不代表满足了用户的验收项**。
- 用户自定义的验收项（主办方格式、字数、匿名、双格式、附件齐全…）**必须另行逐条核对，并原样输出结果**。
- ⛔ 禁止用「我们的自检全绿」作为「可以交了」的证明。

**P0-4 · 形状类任务先交 1 页目录，确认后再写正文**
- 当任务涉及**结构／粒度／组织方式**（要不要用模板、用几章、章节怎么排）：**第一版只交一页目录**（只写标题层级），附一句「确认目录后我再填正文；你也可以直接改标题」。
- **收到确认前，禁止生成正文。** 目录改一次是分钟级，正文改一次是小时级。
- 唯一例外：用户明确说「直接给完整版」。
- 一句话含**两个约束**时（如「用官方模板＋也要有大纲十章」）：动手前**分别复述两个约束**，确认都听懂了再动手。

---

## ⚡ 强制执行协议（先读这里；违反任何一条＝本次使用失败）

> 本节是硬约束，优先级高于本文件其他所有章节的「建议」。执行者（AI）必须机械式遵守，不得「参考精神」。

### 协议 1 · 启动即立规（门禁答案钉死）
- 第一轮对话：按 §五 门禁清单**一次性**问全（映射到实际场景），同时确认「交付物定义」「**目标字数**（先问用户；未指定时预设 **5,000–10,000 字**）」与用户特殊要求。
- 问完后**必须产出《任务规则表》**：**13 项**门禁答案＋交付结构＋红线＋工具路径，请用户确认；确认前不得产出任何方案内容。
- 用户此后的**每一次纠偏 = 规则表升级**：先复述「规则表已更新为 …」并征得确认再动手；禁止把纠偏当一次性修补。

### 协议 2 · 结构冲突时「合并，不取舍」
- 当用户要求的文档结构（比赛提交清单、客户模板等）与 §三 八篇结构不一致：**默认把八篇内容按映射装进用户结构，一个硬要素都不许丢**（五要素行动、禁用词口径、预算能对上、KPI、人力检查、假设验证）。
- 映射规则：用户结构的每个模块 → 对应八篇章节；八篇中用户结构没有对应位的内容 → 放入附件，不得删除。
- **禁止「为了贴合用户格式而砍深度」——这是本 skill 历史上最大的翻车点。**

### 协议 3 · 交付前自检单（必须原样输出在交付回复中）
宣告交付前，**必须在回复中逐项输出下表并标 ✅/❌**；任何一项 ❌ → 回去改，不得声称完成：

| # | 自检项 | 结果 |
|---|---|---|
| 1 | 门禁 13 项已问全（含目标字数）并写入《任务规则表》 | |
| 2 | 未经证实的假设已在文首单独标注 | |
| 3 | 文档结构完整（八篇＋附件，或按协议 2 映射后无缺项） | |
| 4 | 字数达标（以《任务规则表》确认的目标字数为准；未指定时预设 5,000–10,000 字） | |
| 5 | 每条打法/动作五要素齐全（做什么/谁/何时/花多少/怎么验收） | |
| 6 | 禁用词与口径章节存在，物料文案已逐字对照 | |
| 7 | 预算分项加总＝合计；引用数字全部有来源 | |
| 8 | KPI 可测（有基准值与观测方式）＋决策节奏已写 | |
| 9 | 执行人力检查＋删减顺序已写 | |
| 10 | 关键假设＋验证方式＋Plan B 已写 | |
| 11 | 无内部过程章节（事实底稿/多Agent分工/裁决记录）泄入交付稿 | |
| 12 | 交付回复中已附本自检单 | |

### 协议 4 · 工具盘点先于路径
- 需要读取外部资料/平台内容时，**第一步先盘点**：已连接的连接器、MCP、现成 API。连接器可达 → 禁止用浏览器爬虫硬绕（浪费用户额度的最大来源）。
- 涉及付费生成（图片/视频/模型）→ 先报预估消耗并征得用户同意。

### 协议 5 · 交付形态红线
- 对外交付一律简体中文 `.docx`（除非用户另有约定）。
- 内部过程文档（事实底稿、Agent 分工、裁决记录）**不得出现在交付稿中**——用户要的是作品，不是工作过程。

### 协议 6 · 每轮输出《流程状态》行（**跨平台唯一的「强制力」来源**）
- **每轮回复的第一行**必须是固定格式的流程状态行（格式见本文件首屏）。
- 缺这一行 **＝ 已经跑偏**，用户可据此直接指出，执行者必须补上并回到正确阶段。
- 阶段推进时同步更新：`收集中 → 分析 → 分工生成 → 裁决 → 自检 → 已交付`。
- **为什么这条最重要**：本 skill 是纯 Markdown，任何平台都没有强制执行机制。**把流程状态变成用户可见的固定输出**，是唯一能在所有平台上生效的约束方式 —— 做不到就是没遵守，一目了然。

### 协议 7 · 跨平台一致性（本 skill 要在任何 Agent 上跑通）
- **禁止依赖平台专有能力**（特定工具名、专有语法、特定 hook）。所有流程必须是**纯文字可执行**的。
- 能力不足时按 `AGENTS.md` §四 的能力对照表降级，**降级不等于跳步**（例如派不出多 Agent 就顺序扮演，但五个角色仍要一一产出）。
- 支持 hook 的平台（如 Claude Code／WorkBuddy）**可选**加装硬约束，配置方法见 `AGENTS.md` §九；**但不得以「没有 hook」为由降低执行标准**。

### 协议 8 · 卡死处理（**强制不得变成卡死**）

> **最高优先级是「用户拿到东西」。校验是保障，不是障碍。**
> 一个把人卡死的流程，比一个宽松的流程更糟。

**三种卡死与处置**：

| 情况 | 处置 |
|---|---|
| **同一项校验连续 2 次不过** | **停止重试** —— 把「卡在哪／试过什么／需要你决定什么」摊给用户，并给 2–3 个选项让用户选（改做法／跳过该项／换方案方向） |
| **脚本报错或环境不可用**（没装套件、没代码权限、文件读不到、编码异常） | **降级到 Markdown 协议继续产出**，不要中断任务；在交付说明中注明「本稿未经脚本校验」 |
| **用户明说「跳过／先出稿」** | **允许跳过**（`build_docx.py --force`），但必须：① 声明「本稿未通过交付前校验」② 列出未通过项 ③ 不得声称已完成 |

**禁止的行为**：
- 同一个错误反复重试超过 2 次（烧用户额度）
- 因为校验不过就迟迟不交付（超过合理轮次必须上报，而不是自己死磕）
- **静默跳过校验**（跳过一律留痕，让用户知道哪里没检查）

**用户侧的救援话术**（见 README §一点八）：卡住时说「**卡在哪了？把错误原文贴出来**」或「**跳过这项，先出稿**」。

> **交付物定义**：一次完整的营销项目 = **一份 Word 文档**，内含现状分析报告 + 营销策划方案 + 预算明细 + 行动清单 + 风险与假设。
> 不是 Markdown，不是聊天里的文字。**最终必须产出 `.docx` 文件。**
>
> **跨工具可用（重要）**：本 skill 是纯 Markdown、无任何专有语法，**任何 Agent 平台都能用**（ChatGPT／Claude／Claude Code／豆包／本地 Agent／其他）。
> - 接入方式（各平台怎么装）→ 见根目录 `AGENTS.md`
> - 环境没有多 Agent 能力 → 有降级方案（见 `references/02-多Agent分工简报.md` §二）
> - 环境没有文件系统 → 产出结构化 Markdown ＋ 样式对照说明（见 §四「交付执行：脚本」）

---

## ⛔ 第 0 步：门禁（强制，不可跳过、不可猜测）

**任何要产出交付稿的任务，都必须先问清楚。没问清之前 —— 不准出方案、不准往下走、不准用「一般情况」代替。**

> ⚠️ **门禁覆盖范围（2026-09-14 补）**：**不只「新项目」** ——
> **重新生成、改结构（例如改成精炼版）、改版、换量级，这些任务同样要先问。**
>
> **教训**：用户要求「再生成一个罗森范例」时，执行 AI 因为「素材现成」就直接开跑，
> 没问结构／量级／字数，违反了门禁第 13 项。**素材现成 ≠ 需求明确。**
>
> **机械强制（已落地）**：`build_docx.py` 现在要求提供《任务规则表》（`--rules`），
> **没有它就直接拒绝出稿** —— 「先问用户」不再是自觉，而是**出稿的硬前提**。

### 执行方式
1. 对照下面 13 项清单逐项核对，**列出还缺哪些**
2. 把缺项**一次性**问完（不要一项一项挤牙膏）
3. **等用户回答**。用户没答完之前，停止，不要产出任何方案内容
4. 用户答完 → **产出《任务规则表》请用户确认**（见协议 1）→ 确认后才进第 1 步

### 13 项门禁清单（全部拿到才放行）

> ⚠️ **门禁补充项（不计入 13 项，但建议一并问 —— 漏了会让下游整章凭猜）**：
>
> | 补充项 | 为什么要问 | 不问的后果 |
> |---|---|---|
> | **渠道结构**（直营／加盟占比 ＋ 门店数） | 直营与加盟的落地方式完全不同 | 给加盟体系写的方案，门店不会照做 |
> | **现有私域存量**（好友／群／会员数 ＋ 谁运营） | 复购率与加微目标都要从存量推 | 加微目标只能凭空写 |
> | **客户可动用现金／账期天数** | 预算是费用表，客户要的是现金流 | 最大垫资缺口算不出来（B端／门店最致命） |
> | **客户行业 ＋ 已持资质证照**（2026-09-19 新增） | 受监管行业（餐饮食品／医美医疗／药品器械／保健食品／化妆品／教育／金融／房地产／酒类／加盟招商／农资／烟草）要挂 **`3.5 行业资质与宣称边界`**，内容由 `scripts/industry_rules.py` 按行业注入 | 医美客户拿到茶饮客户的骨架；资质文号与行业禁语一栏都没有，方案过不了法务 |
>
> 这几项是 R5／R6 等视角分别独立提出的同类问题：
> **门禁不问 → 下游整章没有输入 → 只能编**。
> ⚠️ **「客户行业」填了就是权威**：`composer` 的行业判定**显式声明优先**于关键词计数
> （实测：医美客户被模板里残留的「茶饮」字样带走、SaaS 客户被判成餐饮 —— 比漏判更坏的是错判）。

| # | 要拿到什么 | 为什么是门槛 |
|---|---|---|
| 1 | **卖什么**（产品/服务、客单价、毛利） | 不知道毛利就没法定价与选品 |
| 2 | **卖给谁**（人群、场景、什么情况下会买） | 决定内容与渠道 |
| 3 | **现在什么规模**（营收／门店数／用户数／订单量） | 判断方案量级 |
| 4 | **卡在哪**（客户**原话**怎么说） | 决定问题类型（A–H） |
| 5 | **预算多少**（总额、是否一次性、谁出钱） | 没有预算 = 没有方案 |
| 6 | **有什么现成资源**（人手、渠道、私域、内容能力、老板能否出镜） | 决定动作能不能落地 |
| 7 | **时间要求**（死线、关键节点、日历） | 决定节奏与取舍 |
| 8 | **硬约束**（合规、场地、授权、人力上限、不能做什么） | 撞了就是白做 |
| 9 | **对接人与决策人**（谁拍板、决策链） | 决定方案给谁看 |
| 10 | **竞争对手**（谁在抢、凭什么抢） | 决定差异点 |
| 11 | **过去试过什么**（有效的、无效的、被谁坑过） | 避免重复失败 |
| 12 | **怎么算成功**（验收标准） | 决定 KPI |
| 13 | **交付结构 ＋ 内容量级 ＋ 字数**（三件事一起问） | **先问用户，不要自己替他决定**。下限＝便利店开学季案水平（要素齐全＋有判断力＋有主线）、**上限不设限** |

> **第 13 项的问法（⛔ 给选项让用户选，不要开放式地问）**
>
> **① 结构**（给两个选项）
>
> - **精炼版**（像便利店开学季案）：终稿 = **结论与决策** ＋ 能执行的动作。**必须包含**：
>   - **诊断结论**（例：「对抗学长的五条结构性软肋」—— 对手哪里打不动）
>   - **组合策略**（例：「三档套装 ＋ 满减阶梯」—— 产品与价格的组合设计）
>   - **特别专案**（峰值日的加码动作，例：报到日家长专案）
>   - **机制**（例：需求登记机制怎么运作）
>   - 定位口径／触达／文案／KPI（含工具与决策节奏）／预算／行动清单／风险与假设
>   - **留支撑稿的只有**：**推导过程与资料堆砌**（竞品逐环拆解的 79 行、完整禁用词 59 条、现状数据罗列）
>   - ⛔ **最常见的错误**：把「诊断」也当成「分析」砍掉 → 方案变成「只有动作、没有判断依据」的执行清单，**没说服力**。**砍过程，不砍结论。**
>
> - **完整版**：上面全部 ＋ **现状分析的完整展开**与竞品逐环拆解（八篇＋附件）
>
> **② 量级**（⛔ **每个选项都要标预估篇幅** —— 用户才有尺寸感）
>
> | 选项 | 篇幅 | 适合谁看 |
> |---|---|---|
> | **一页纸** | 1–2 页（约 800–1,500 字） | 决策人快速过目 |
> | **精简版** | 约 200–300 行 | 只看决策与执行 |
> | **精炼版** | 约 300–400 行（便利店开学季案原版 371 行） | 要直接拿去执行 |
> | **标准版** | 5,000–10,000 字 | 常规交付 |
> | **完整版** | 不限（含全部推导与附件） | 内部存档／大项目 |
>
> **③ 硬限制** —— 「有没有字数或页数的硬要求？」（比赛、招标、客户模板常有规定）
>
> - **下限＝便利店开学季案水平**（要素齐全 ＋ 有判断力 ＋ 有一条主线）、**上限不设限**。
> - ⛔ **不许开放式地问「你要多长」** —— 用户没有尺寸感，答不出来。
>   **必须给具体选项 ＋ 预估篇幅**，让用户勾选。
> - 用户没特别说 → 预设「**精炼版／标准版**」，并写进《任务规则表》。
>
> 详细问法见 §五 对接实测清单（40+ 条原话问法，按 8 组分类）。

### 门禁例外
仅当用户明说「先按假设出一版」「我就想看看思路」时，才可跳过门禁；此时**必须在文档首页标注「本方案基于以下未经验证的假设」并列出假设清单**。

---

## 一、工作流（六步 · 内容维度）

> **先分清两条线，别混**：
> - **本节「六步」＝内容工作流**（事实 → 分析 → 生成 → 裁决 → 交付）
> - **「S0–S8」＝操作流程**（工具怎么跑）→ 见 `references/08-操作流程SOP.md`；向导：`python scripts/flow.py`
>
> **对照**：事实底稿 ↔ S2 ｜ 现状分析／生成 ↔ S3–S4 ｜ 裁决 ↔ S4 ｜ 交付 ↔ S7–S8。
> ⛔ **强制层**：`scripts/flow.py → composer.py → selfcheck.py → run_pipeline.py` 一关不过就不出稿。
> ⛔ **内容再多，不经 `composer` 注入知识、不过 `selfcheck`，就不算完成。**

### 第 1 步：产出「事实底稿」（内部文件，先做）
把门禁收集到的一切整理成事实底稿 —— 这是后面所有 Agent 的唯一输入源。内容含：
客户与场所 ｜ 目标与预算 ｜ 时间轴（含日历节点）｜ 战场事实 ｜ **硬约束** ｜ 现有资产 ｜ 竞争格局 ｜ 已确认决策 ｜ 待解决问题。
（此步借用便利店开学季案的「事实底稿」做法：**任何方案变更都需先回到这里核对**。）

### 第 2 步：现状分析（体检）
按 §三 文档结构第一篇写。必须回答：
- 生意现状（数据事实，不是形容词）
- 问题诊断（归到 A–H 哪一类，见下表）
- 真正的卡点（**我的判断**，不是复述客户原话）—— 写法是**重构问题**，不是复述现象
- 已排除的假设（显示判断过程）
- 约束与风险

#### 问题类型 A–H（诊断章的归类骨架，2026-09-16 明确定义）

| 类 | 问题 | 典型客户原话 | 对应打法章节 |
|---|---|---|---|
| **A 认知** | 没人知道我／记不住 | 「没人知道我们」 | §1 认知类、§2 内容类 |
| **B 交易** | 知道但不买／转化低 | 「来的多买的少」 | §8 定价与转化类 |
| **C 渠道** | 想买买不到／铺货与触达断 | 「想买找不到」 | §5 渠道类 |
| **D 信任** | 不敢买／缺证明 | 「怕被坑」 | §7 信任类 |
| **E 复购／私域** | 买了不回头／沉淀不了 | 「回头客少」 | §4 私域类 |
| **F 定价** | 卖不动／价格力弱／毛利薄 | 「只能靠打折」 | §8 定价与转化类 |
| **G 组织** | 人力与执行力跟不上 | 「没人做」 | 02-多Agent分工简报（执行人力检查） |
| **H 合规** | 监管红线约束 | 「这行不能说」 | cases/11 医药、16 金融合规专节 |

> **用法**：主类型写 1 个、并列次要类型（例：「E 复购／私域＋引流（A 认知／B 交易）｜性质：交易问题为主」）。
> **判据**：客户原话只是入口，**归类由你给**——客户说「没人来」，若数据显示到店没跌、成交跌了，那就是 B 不是 A。
> **诊断章三件套（必齐）**：① 问题类型 A–H 标签 ② **卡点重构句**（「不是 X——是 Y」的句式）③ **核心动作一句**（窗口期内做什么、把什么变成什么）。

### 第 3 步：多 Agent 分工生成（**每次都走，不省略**；执行细节见 `references/02-多Agent分工简报.md`）

| 角色 | 负责什么 | 产出 | 必写要素（**要素齐全才算合格 —— 不是字数越多越好**） |
|---|---|---|---|
| **（前置）事实整理** | 把门禁收集到的一切整理成事实底稿（所有角色的唯一输入源）——**非角色，由主理人做** | 事实底稿 | 见第 1 步 |
| **策略 Agent** | 三次收窄、**打法组合表**、选品与货盘、定价与机制、节奏排期（渠道分工／预算／KPI／风险**只给量级摘要**，明细归触达/财务风控） | 策略篇 | **① 打法组合（多段文字，7 要素，见下）②** 选品 **≥8 个 SKU**（各含毛利与走量预期）／定价 **≥3 档**各给依据／时间线**按天列**并标峰值日／机制逐个写清规则 |
| **品牌 Agent** | **竞品扫描**、定位、差异化支点、命名、**禁用词与口径** | 品牌篇 | 头号对手 **≥5 环节四段式**／其他对手 **≥3 个**含威胁等级／禁用词 **≥5 类**并标后果／一致性审核清单 |
| **触达 Agent** | 渠道选择（含为什么**不**用某些）、用户路径、关键词与搜索、内容分发 | 触达篇 | 渠道 **≥4 个**各含理由／预算／预期／执行人；路径**能画出**且标流失风险 |
| **文案 Agent** | 物料清单与逐件完整文案、一线话术、群内话术 | 文案篇 | 物料 **≥12 件**，每件含**位置＋完整原文＋成本**；话术是**能照着念的原话** |
| **财务/风控 Agent** | 预算明细、盈亏线、走量预期、KPI 表、**风险四件套**、关键假设 | 财务风控篇 | 预算 **≥6 项**且加总＝合计／KPI **≥8 个**含预警线／风险 **≥3 条**且四件套齐全 |
| **主理人（我）** | 裁决冲突、合并成稿、跑双校验 | 合并稿 | 依《任务规则表》确认的字数，去重合并（不删要素） |

> ### ⚠️ 关于篇幅：**先问用户要多长，不是越长越好**
>
> - **字数由门禁第 13 项先问用户决定** —— 比赛／招标常有字数页数上限；客户也可能只要**一页纸摘要**；内部讨论稿又可能要完整版。**不问就默认 5,000–10,000 字。**
> - 上表的「≥N」是**内容完整性要求**（该写的要素一个不能少），**不是「字数越多越好」**。
> - **深度的定义**：该有的要素齐全 ＋ 每个要素写到**可执行**（有动作、有负责人、有数字、有验收）。
> - **反面教材**：把「深度」做成「字数竞赛」→ 方案变长但空话变多，客户不买单。**宁短而实，不长而虚。**
> - 若客户只要摘要：**要素照样齐全，但每个要素压缩到最精炼** —— 压缩的是论述，不是要素。

>
> **角色数量按项目规模匹配**（便利店开学季案实际动用了 **7 个参与者**＝5 个角色＋事实整理＋主理人）：
> - **大项目**（多战场／预算 ≥10 万或周期 ≥1 个月）→ **5 个执行角色全上**，一个不能少
> - **小项目**（单店／单一目标／预算 <5 万）→ 可合并为 3 个（策略＋品牌／触达＋文案／风控），但**每个角色的必写要素不变**
> - **不变的原则**：**角色可以合并，要素不可以缺失。**

**分工规则**：
1. 各 Agent **只读事实底稿**，不互读彼此的草稿（避免互相迁就、越写越像）
2. 出现冲突（例如品牌 Agent 说不能用某词、文案 Agent 却写了）→ **主理人裁决**，并在文档里记录裁决理由
3. 全部完成后，主理人跑 §六 自检清单

**环境不同，做法不同（本 skill 不依赖特定工具）**：
- **能派独立 Agent** → 五个角色各自独立执行，互不看草稿
- **只有一个对话** → 顺序扮演，每次明确宣告「现在我是 X 角色」，只读事实底稿、不回顾前序输出，五个都写完才进裁决
- **只能复制粘贴** → 把 `references/02-多Agent分工简报.md` §三 的五份角色简报逐一贴进去跑

### 第 4 步：主理人裁决与合并
把五篇合成一份文档，冲突处按**五级优先序**裁决（客户硬约束 → 事实底稿 → 财务可行性 → 品牌一致性 → 创意表达），记录裁决理由，补齐过渡与索引。

### 第 4.5 步：按客户格式范本重排（**客户／比赛／招标给了范本时必走**）

客户常自带**提交格式范本**（章节清单、模板文件）。**不要为了贴合格式砍深度**（协议 2）——做法是**映射，不是重写**：

```bash
# ① 先看自动匹配建议（不写档）
python scripts/reformat_to_template.py --source 方案.md --template 客户范本.docx --dry-run
# ② 确认映射后重排（映射表 JSON：范本章节 → 源稿章节列表）
python scripts/reformat_to_template.py --source 方案.md --template 客户范本.docx \
    --map 映射.json -o 方案-按范本.md --report 要素核对.md
# ③ 走唯一入口出稿
python scripts/run_pipeline.py --rules rules.json --plan 方案-按范本.md \
    -o 方案-按范本.docx --title "客户名 营销方案"
```

**三道保证（不丢东西）**：

| 情况 | 处理 |
|---|---|
| 范本要、源稿没有的章节 | 标【待补】＋列进核对报告（该新写的新写） |
| 源稿有、范本没对应位的章节 | **整节移入附录**（协议 2：合并，不取舍） |
| **硬要素核对** | 执行摘要／问题类型／卡点重构句／打法组合／禁用词／KPI／预算／行动清单／风险兜底／关键假设／自检单 —— **丢一件就退出码 1，不准出稿** |

> **映射由你（AI）判断**：工具只做标题粗筛建议；语义映射要你决定
> （例：「二、市场与竞争分析」 ← 源稿「一 · 诊断」＋「五 · 定位与口径」）。
> 范本与映射示例：`references/范例/格式范本示例-比赛提交清单.md` ＋ `references/范例/格式范本映射示例.json`。
> （⚠️ 引用一律写**完整相对路径**；只写裸文件名时，简繁写错不会被 L1 抓到 —— 2026-09-18 已补上这一关）

### 第 4.7 步：治理节奏与变更控制（**交付前把「后面怎么改」定下来**）

方案不是交出去就结束。**交付前**必须跟客户说定三件事，并落进交付稿的 **8.14 变更控制与范围边界**：

1. **变更走什么口** —— 谁提、**谁批**、几个工作日内答复、影响哪几节、**算范围内还是新增工作量**
2. **评审节奏** —— 初稿／定稿各在哪一天、谁参加、**这次只裁决什么**（⛔ 不带日期的「初稿评审」等于没有节奏）
3. **范围边界与争议升级** —— 本方案**不做什么**；双方不一致时谁裁、几个工作日内给结论

每次变更都要回到 **8.13 变更记录**留一行（日期／改了什么／为什么／谁批的）。**8.13 是日志，8.14 是规则** —— 只有日志没有规则，等于「事后都记着，事中没人拦」。

**为什么必须放在交付前**：交付后再谈「改一次多少钱」，客户会认为在坐地起价；交付前写清，双方都有依据。

**规则表里带上治理块就会一并校验**（缺字段即拦；速览类快案可整块省略）：

```bash
python scripts/gate_check.py rules.json
# 可选块："governance": {"变更审批人": "…", "答复时限": "…", "评审节奏": "…",
#                       "范围边界": "…", "交付物清单": "…", "争议升级": "…"}
```

### 第 5 步：交付 Word
按 §三 规范生成 `.docx`。**有代码执行能力时必须走 `scripts/build_docx.py`** —— 它会先跑自检，不过就拒绝生成（强制点）；没有代码能力则退回 Markdown + 样式对照说明。详见 §四「交付执行：脚本」。

---

## 二、状况 → 打法 路由表

**这张表是引擎。** 左边是对接中听到的状况，右边直接给打法方向。

| 听到的状况 | 打法方向 | 预算量级 | 见效周期 |
|---|---|---|---|
| 没人知道我们（线下店） | 门头即广告 + 招牌菜/爆品命名 + 3 公里地推 + 熟客群 | 5 千–5 万 | 2–6 周 |
| 没人知道我们（线上） | 符号／口号 + 单平台内容打透 + 场景化搜索词 | 1–10 万 | 1–3 个月 |
| 来了不买 | 详情页/菜单重做 + 价格锚定 + 首单钩子 + 话术 | 0–2 万 | 1–4 周 |
| 买了不回头 | 私域收口 + 分层发券 + 会员阶梯 + 唤醒 SOP | 1–5 万（含工具） | 1–3 个月 |
| 投流亏钱 | 先做全链路数据表找断点 → 修转化 → 再放量 | 0（先诊断） | 2–4 周 |
| 想开新品牌／第二曲线 | 先判能不能延伸 → 品牌架构设计 → 冷启动 | 20 万起 | 6–12 个月 |
| 想涨价 | 加价值要素保溢价 + 分层定价 + 套餐设计 | 0–3 万 | 1–2 个月 |
| 想出海 | 先选一个市场 + 产品本地化重做 + 本地帐号矩阵 + 自播 | 50 万起 | 3–12 个月 |
| 短期战役（开学/开业/大促） | 定主战场日 + 造势期/引爆期/收口期三段 + 峰值日专案 | 按预算 | 2–4 周 |
| 老板要做个人 IP | 内容日历 + 不写脚本 + 前三秒给结论 + 高管矩阵 | 人力为主 | 3–6 个月 |

> 详细打法（怎么做、步骤、预算、风险）见 `references/00-打法库.md`（104 条）。

---

## 三、交付物规范（Word 文档）

> ⭐ **五块质量硬要求**（缺任何一块＝方案不合格。前三块标准来自用户认可的「便利店开学季案」，后两块来自 2026-09-16 用户以《中百罗森新生开学引流方案》为基准的纠偏）：
>
> 1. **多维度** —— 现状分析要做满：生意现状／问题诊断（含 **A–H 问题类型标签**）／**已排除的假设**／**竞争与关联品牌扫描**／约束与风险底线。不是只写一个「现状」了事。
> 2. **多品牌** —— 竞争扫描必须**逐个对手拆**：头号对手按业务环节全链条拆（每环四件事：他怎么做／强点／致命弱点／我们怎么打）；其余对手逐个列（他抢什么／威胁等级／我们的应对）；最后落到**「对手无法复制的资产」**。
> 3. **风险把控** —— 每个风险必写**四件套**：排序理由／**预警信号（具体数字 + 检查时间点）**／**按触发顺序的兜底预案（含最坏兜底）**／**预防性动作**。只列风险不给预案的，等于没写。
> 4. **打法组合（2026-09-16 新增硬门槛，同日修正为「多段文字」形式）** —— 策略篇**必须**有打法组合：每条打法写齐 7 个要素——**为什么用它**／具体动作／谁做／花多少／多久见效／验收指标／**可抄案例**。⛔ **形式用多段文字，不要用宽表**：8–9 列的表格在 Word 里会挤成一条竖线（用户实测反馈），根本读不清。缺「为什么用它」＝只有清单没有说服力；缺「可抄案例」＝ 628 张案例卡白做。`selfcheck.py` 机械校验 7 个要素标签（形式不限）。
> 5. **诊断三件套 ＋ 风险挂编号（2026-09-16 新增）** —— 诊断章须齐：**问题类型（A–H）** ＋ **卡点重构句**（「不是 X——是 Y」）＋ **核心动作一句**；风险章每条须写全**四件套**（为什么会发生／预警信号／兜底预案／预防动作）；**写作时内部对照 `04-失败归因总库.md` 的 12 个模式与 34 条预演，但交付稿里只写根因白话，不写「模式 NN」编号**（见下方协议 5 与 selfcheck 第【10】关）。

### ⛔ 减法规则（比加法更重要 —— 这份 skill 历史上最大的翻车点）

1. **同一件事只说一遍** —— 全文不允许逐字重复的段落／表格／清单。
   同一内容需要多处引用时，用「见 §X」而不是再抄一遍。
2. **禁止为凑数而重复／灌水** —— **上限不设限**：要写多长由**用户决定**（门禁第 13 项先问）。
   但**同一件事不许说两遍** —— 重复是**缺陷**，长不是。
   判断标准：「这句话删掉会不会少信息？」不会 → 删。**宁短而实，不长而虚。**
3. **必须有一条反对意见** —— 如果客户已有判断（例如「主战场就是 X 日」），
   至少有一个角色要**明确提出反对并给理由**；主理人裁决后**保留这条痕迹**
   （原版靠这一条赢了：品牌角色开篇否定了客户已拍板的战场判断）。
   **只会附和客户的方案是废方案。**
4. **必须有一条主线** —— 整份方案能被压成一句话（例：原版「他们卖到校前的一包货，
   我们卖到校后 15 天里随时能兑现的确定性」）。压不出来就是还没想清。

> 校验：`depth_check.py` 的【9】重复度维度 + 【2】风险决策推理，会把第 1、2 条机械拦下；
> 第 3、4 条需人工确认，但执行者必须在《流程状态》行里声明是否已写。
>
> ⚠️ **没有「字数上限」这种东西** —— 要素数量多少算合适，取决于**用户要的量级**（门禁第 13 项先问），不由执行者替他决定。
>
> **一次实测的代价**（2026-09-14 对比诊断）：同一份内稿，旧标准产出 1,998 行、新标准（把
> 要素下限全填满）产出后仍输给 371 行的原版 —— 因为原版有主线、有取舍、有反对意见，
> 而且每个判断只说一遍。**要素齐全不等于方案成立。**

### 基本设定
- **格式**：`.docx`（不是 markdown、不是聊天文字）
- **份数**：**一个文件**装完（现状分析 + 策划方案 + 预算 + 行动清单）
- **语言**：**简体中文**（全仓库统一；知识库、脚本、文档、交付物都是简体 —— 2026-09-18 用户裁定）
- **篇幅基准**：**以门禁确认的目标字数为准——先问用户；未指定时预设 5,000–10,000 字**。深度以「硬要素齐全」衡量（五要素行动、禁用词口径、预算能对上、KPI、人力检查、假设验证），**不以字数注水、也不为省字砍硬要素**；附件（物料文案、话术表）另计
- **必备元素**：封面（项目名／日期／版本）｜目录｜页码｜表格化呈现（能表格就不要长段）

### 文档结构（八篇 + 附件）

```
封面
目录
执行摘要（TL;DR）          —— 1 页，结论先行：做什么、花多少、预期什么结果
核心结论卡片               —— 一张表讲完关键数字：目标／投入／预期回报／保本点／最大风险／谁执行

第一篇 现状分析
  1.1 生意现状（数据事实）
  1.2 问题诊断（归类 + 我的判断）
  1.3 已排除的假设

〇 · 议题树与假设台账         —— ⭐ 2026-09-17 新增，**放在现状分析之后**
  0.1 MECE 分解（营收＝流量×转化率×客单价×复购次数，找漏钱最多的那一层）
  0.2 假设台账（H1–H3，**每条都要可证伪**：说得出「若拿到什么，就说明我错了」）
  · 一/二/三 章开头各写一行 `> 本章回应：H【填】（证实／证伪）` —— 写在台账里却不被回应＝没做假设驱动

二·九 · 洞察萃取             —— ⭐ 2026-09-17 新增，**从数据到定位之间那一跳**
  观察（数据或原话）→【洞察】人心深处＿＿（≤25 字，**主语必须是「人／用户」**）
  → 连接句 → 共鸣测试（念给 3 个人，几人说「啊，我也是」）→ 不合格版（自照反例改写）
  1.4 ⭐ 竞争与关联品牌扫描（**多品牌逐个拆** —— 本 skill 的质量分水岭之一）
      · **头号对手**：按业务环节全链条拆解（如「获客→信任建立→成交→履约→复购」），
        每一环写四件事：【他怎么做／他的强点／他的**致命弱点**／**我们怎么打**】
      · **其他对手**：**逐个**列（含渠道品牌、平台、个人卖家），每个写
        【他抢的是什么价值点／威胁等级（高/中/低）／我们的判断与应对】
      · **核心差异点**：先压成一句话，再落到**「对手无法复制的可核实资产」**（用对比表列）
  1.5 约束与风险底线（硬约束逐条列，标明「撞了就是白做」）

第二篇 策略
  2.0 ⭐ **打法组合（必写，多段文字形式）** —— 本 skill 的核心产物：
      · **每条打法一段**：`**打法 N｜名称**（定位语）`，下面逐个要素一行：
        `- **为什么用它**：…`（说服逻辑：差异化锚点／成本结构／对手做不到的那点，不是形容词）
        `- **具体动作（精准到每一步）**：…`（⛔ 拆成**编号步骤**，每步写齐「动作／时间／谁做／物料·话术／产出」，≥3 步；只写一句话＝不合格，`selfcheck` 第【7】关判硬错误。用户 2026-09-16：「策划具体操作流程要详细精准到每一步——指策划案的打法和实操」）
        `- **谁做｜花多少｜多久见效**：…｜…元｜…`（三要素合一行）
        `- **验收指标**：…`
        `- **可抄案例**：…（展开见 2.0.1）`
        `- **为什么这么做**：…`（把理论/观点**写成内容**：这套动作背后是什么道理、照著改为什么不会跑偏。⛔ 只写一串理论名字＝没写）
      · ⛔ **每条打法的做法必须来自 `references/00-打法库.md`**（含「怎么做」步骤），不许自己编一套
      · ⛔ **不要用宽表**：8–9 列的表格在 Word 里会挤成一条竖线（用户 2026-09-16 实测反馈）
      · 「可抄案例」列只放索引，**表后必须另有 2.0.1 文字展开**（别人怎么做的＋我们怎么用）
      · 3–7 条为宜；一页纸／摘要版可压缩，但不得整节省略
  > ⛔ **知识要「写成内容」，不是「标出处」**（selfcheck 第【8】关机械校验）：
  > - **策略篇**要真的用到 **≥3 个理论**（用中文名写进做法里）＋ **≥1 个书籍观点**（《书名》＋它的主张）。
  > - **每条打法的「为什么这么做」必须写开**（≥25 实字）—— 讲清楚这套动作背后是什么道理，
  >   **不是列一串理论名称**。
  > - 「可抄案例」必须是「**品牌** —— 他做了什么 ▶ 结果：数字」；只有品牌没结果不算可抄。
  > - **第三篇 定位与口径**要点明用的是哪一套定位理论，并说清楚它用在这一步的哪个决定上。
  >
  > ⛔⛔ **交付稿不得出现任何内部坐标**（selfcheck 第【10】关硬错误、不出稿）：
  > **写作指南见 `references/12-范式库.md`**（六档骨架＋逐节填写指引＋关键句片段）；
  > 交付稿的**骨架唯一真相在 `scripts/composer.py`**，宣告在 `scripts/paradigm_data.py`，
  > 两者由 `scripts/build_paradigm.py` 对账、交付稿再由 `selfcheck` 第【1b】关对账 ——
  > **改骨架就要三处同步**，否则会出现「文档承诺了、实际不生成」。

  > `§X.X`／`（03 §C1）`／`（作者49）`／`cases/xx.md`／`references/`／`打法库`／`模式 NN`／
  > `composer.py`／`SKILL.md`。
  > **客户不需要知道我们内部怎么编号、文件放哪、用了哪个脚本** —— 那些写进
  > `composer.py --internal` 那份文件里，只有你和维护者看。
  > 注意这不是「少写」：理论照用、案例照引、**而且要写得更详尽**，只是不挂编号。
  > 用户 2026-09-17 原话：「不是拿了案例就行了，说有什么理论是没有任何意义的 ——
  > 要根据这些案例、这些理论、这些观点写具体的操作、具体的做法。」
  2.1 ⭐ **三次收窄**（先说清：这次**不做什么**、战场缩到多小、为什么 ——
      收窄是方案成立的前提，不做取舍的方案等于没方案）
  2.2 方案成立的前提
  2.3 被低估的资产（现有资源里被忽略的 2–3 个）
  2.4 节奏排期（按天/按周，标出**峰值日与黄金窗口**）
  2.5 产品/服务与货盘（选品逻辑、定价、套装、满减）
  2.6 特别专案（针对峰值日的加码动作）

第三篇 定位与口径
  3.1 定位与差异化支点（3 条）
  3.2 ⚠️ 禁用词与红线（**什么话绝不能说**）
  3.3 对不同人说什么（客户／用户／家长／监管／合作方，各一套口径）
  3.4 命名与记忆点

第四篇 触达与渠道
  4.1 渠道选择（为什么用这些、为什么不用那些）
  4.2 路径设计（用户从哪知道你 → 怎么到店/下单）
  4.3 内容与投放节奏
  4.4 触达的硬风险

第五篇 落地文案与物料
  5.1 物料清单（每件：放在哪、写什么、多少钱）
  5.2 话术（一线人员照着说）
  5.3 需要客户核实的事实

第六篇 KPI 与追踪机制
  6.1 追踪工具（土办法也要能落地，含成本）
  6.2 每日/每周只看这几个数
  6.3 决策节奏（什么数出现就调整、怎么调）

第七篇 预算明细
  7.1 预算表（分项 + 合计，**必须能对上**）
  7.2 盈亏线测算（保本点／健康线）
  7.4 追投与止损规则

第八篇 执行与风控
  8.1 行动清单（每条：做什么／谁做／什么时候／花多少／验收）
  8.2 ⚠️ 执行人力检查（**每个动作的负责人是否有时间做**；人力不足时的删减顺序）
  8.3 ⭐ **风险清单 —— 每个风险必写「四件套」**（本 skill 的质量分水岭之二）
      · **先说清为什么会发生在我们身上**（内部对照 `04-失败归因总库.md` 找同类死法；
        ⛔ 交付稿**不写**「模式 NN」这种编号 —— 客户看不懂，selfcheck 第【10】关会拦），
        例：「**时机错误**：两周窗口没抓住 → 必须现在备货＋招募，开学周集中引爆，不拖」
      · **排序理由**：为什么这个最紧急／最可能（并说明其他风险为何排在后面）
      · **预警信号**：**具体数字 + 检查时间点**（例：「9/12 中午 12:00 复盘：大件动销 < 25%」）
      · **兜底预案**：**按触发顺序分步**（第一步做什么 → 第二步 → … → **最坏兜底**），
        并说明**为什么先做这个而不是直接降价**（先试免费的，再花钱的）
      · **预防性动作**：比兜底更重要 —— 能在事前把风险消除掉的动作（如「只跟可退货的档口合作」）
      > **只列风险不给预案的，等于没写。**（此标准来自便利店开学季案，那份的风险章节是本 skill 的质量基准）
  8.4 关键假设与验证（每条假设 + 验证方式 + 验证成本 + 不成立时的 Plan B）
  8.5 待解决问题清单（我还没搞清的事）
  8.6 不做什么（明确划界，防期待错位）

附件
  A 物料文案全文
  B 话术表
  C 客户原始资料与约束（客户告知的原始情况、原始数据、硬约束）
     > 正文**不得**出现「事实底稿／Agent 分工／裁决记录」这类内部过程字样；本附件是唯一例外
  D 参考案例（同行做过的、可抄的）
  E 交付自检单（12 项，逐项 ✅/❌）—— **同时必须在聊天回复中原样输出**（协议 3）
```

### ⛔ 表达格式原则（**决定「读得下去」还是「看得头疼」**）

> **用户定调（2026-09-14，对比两版排版）**：
> ① **表格轰炸**（什么都塞表格）→ 用户：「有点混乱、**不能一眼看出重点**」
> ② **列表式**（粗体锚点 ＋ 一句话一条 ＋ 适度留白）→ 用户：「排版比较**舒服、清晰**」
>
> ⛔ **不要一律用表格，也不要一律用列表 —— 按信息类型选。**
> 表格在需要时**照用**（对比型**必用**表格，它比列表清晰得多）；不要矫枉过正把该用表格的地方也改成列表。
> 判断标准：读者要「**对照比较**」→ 表格；读者要「**顺序阅读**」→ 列表。

| 信息类型 | 用什么 | 例 |
|---|---|---|
| **对比型**（≥2 个对象逐项比较） | **表格**（列 ≤5） | 对手软肋 vs 我们打法；套装档位 vs 内容 vs 价格 |
| **说明型／步骤型／清单型** | **列表**（`•` 或 `a.` `b.` `c.`） | 排期按阶段；风险逐条；机制步骤；渠道说明 |
| **案例说明**（别家怎么做的） | **叙述＋列表**（绝不用表格） | 品牌 → 面对什么 → 做了什么 → 结果 → 我们怎么用 |
| **要素多、文字长的组合**（打法组合／商业模式拆解） | **多段文字**（一条一段，`**打法 N｜名称**` ＋ 要素标签） | 每要素一行（`- **为什么用它**：…`）；⛔ 宽表在 Word 会挤成竖线 |
| **假设与对策**（条件→动作） | **列表**（`a.` `b.`） | 每条：假设 → 验证方式 → 不成立时怎么办 |
| **多字段并列**（物料清单／渠道清单／预算／指标表） | **表格**（列 ≤5、格 ≤30 字） | 物料／位置／内容／成本；渠道／理由／人力／预算／预期 |

> ⛔ **多列清单不要用列表**（2026-09-14 用户纠正）：「**物料用表格吧，太乱了**」——
> 像物料这种「名称／位置／内容／成本」多列并排的信息，**列表会破坏列的对齐关系，读起来乱**；
> 表格才看得清。**判断：只要一条信息有 2 个以上并列字段 → 用表格。**

**列表的标准写法（照着抄）**：
- **带条件／时间的条目**：`• **倒数 2 周**（现在 → 开学前）｜ 见信号：确认供应链；启动招募并发专属码`
- **问题 → 对策的条目**：`a. **时间窗口只有两周，错过就没了**：现在备货＋招募，开学周集中引爆，不拖`（**注意：交付稿里写根因白话，不写「模式 NN」**）
- 每条控制在 **1–3 行**；超过就拆子项
- **粗体只用在锚点**（时间／风险名／关键数字），不要整句加粗
- **段落之间留空行**（Word 里就是段后间距）—— 不要挤成一片

**⛔ 五条硬规则**：
1. ⛔ **全部塞表格**（表格轰炸 = 读起来累，重点反而不见）
2. ⛔ 表格超过 **5 列** 或单元格文字超过 **30 字**
3. ⛔ 段落超过 **80 字**
4. ⛔ **只写结论，不写推导** —— 终稿只放「**是什么／怎么做**」；「**为什么**」留支撑稿
   （诊断不要写「他怎么做／他的强点」；组合策略不要写「设计理由」专栏）
5. **判断句要短而锐** —— 例：「他们截流的是『学校』这个泛颗粒度，不是『宿舍楼』」
6. ⛔ **可抄案例只留编号**（2026-09-16 用户纠正）—— 「可抄案例」列只放索引，**表后必须用文字展开「别人怎么做的」**：品牌／他面对什么问题／具体做了什么动作／结果数字／**我们怎么用**。**案例说明一律叙述，不用表格**——只有卡片号等于没写（客户不会去翻案例库）。

### ⛔ 篇幅配比：**权重 ≥ 40% 给「策略 ＋ 诊断」（与总长度无关）**

> **用户定调（2026-09-14）**：「**是权重，甚么长度都是把权重增加**」
> 意思是：不管交付多长（一页纸／5 页／20 页／不限），**策略与诊断的篇幅占比都要 ≥40%**。
> 权重是**独立于长度**的维度 —— 不是「写长一点就更重点」，而是**同一个长度里谁占的份额大**。

| 章节群 | 篇幅占比 | 写法 |
|---|---|---|
| **策略 ＋ 诊断**（收窄／对手软肋／节奏／选品／组合／机制） | **≥40%** | **重点章节**。诊断要逐条拆对手软肋；策略每节写清「怎么做＋为什么这样取舍」 |
| **执行类**（行动清单／KPI／预算／物料清单／话术） | **≤40%** | 能表格就表格、一格一句话；写到「能照着做」即可，**不展开解释** |
| 其他（TL;DR／结论卡／定位口径／触达／风险） | 其余约 20% | — |

**做法**：先定总长度 → 按 **40 / 40 / 20** 分配篇幅 → 执行类写到够用就收手，**把省下的额度全给策略与诊断**。

⛔ **最常见错误**：把篇幅**平均分**给各章 → 策略与诊断被稀释成一页表格，方案就失去判断力（这正是本 skill 被用户连续纠正的地方）。

### 每个打法的写法要求
**禁止写「做私域运营」「加强内容营销」这类概括。** 每条必须是：
> 做什么（具体动作）｜谁负责｜什么时候｜花多少｜怎么验收

例：
> ❌ 用包裹卡做私域引流
> ✅ 印 500 张包裹卡（A5，300 元），正面写「加店长微信领 ¥5 券」，背面印店长二维码；由店员在每天 18:00–20:00 高峰期对每单外带客户递卡；目标：2 周加 150 人；验收：扫码率 ≥ 30%

---

## 四、参考文件索引

> **选档看第 -1 步那张表**（含「明确不要读」）。本节是**细节补充**：每份文件「必须引用什么、内容是什么」。
> 目录级导航：`references/cases/README.md`（50 个行业档只读 1 个；含固定标题名与硬规则）。

### 生成时必须引用的三份（⛔ 不是「参考」，是「必须引用并标出处」）
| 文件 | 什么时候读／怎么用 |
|---|---|
| `references/00-打法库.md` | **生成时最先读**。104 条打法，按状况查（§0 总表一页速查）；**每条打法必须照它的「怎么做」展开**（交付稿里不写 §X.X 编号） |
| `references/cases/{行业}.md` | 要找同行案例。**先读「案例清单」（每条写清了「做了什么／结果／可抄的点」），再跳「深度拆解」** |
| `references/03-方法论操作手册.md` | **策略篇必引用 ≥3 个模型**（GROW／NPS／超级符号／AISAS／ECIRM…）并标出处；**111** 个模型（A–M 十三大类）都写了怎么填 |

### 案例库：51 个大类 / **649 张深度案例卡**（01–45 行业 411 张 ＋ 46–51 机构与方法论 238 张）

> ⛔ **案例集硬规则**（2026-09-16 由项目记忆写进本档 —— 原本只在内部笔记里，模型看不到）：
> 1. **严禁公司背景**：创始人、成立年份、股权、融资、营收、人事 —— 对做营销毫无用处。
>    （例外：`cases/46–51` **机构／书籍类**可保留「理解其方法所必需」的背景；行业卡 `01–45` 只留动作与结果。）
> 2. **唯一允许的数字是「这次营销动作造成的结果」**（销量／曝光／门店／复购）；公司财报数字不算。
> 3. 清单必须写**具体动作**（例：「用包裹卡把线下客导到企微、朋友圈按 3:2:1 发」），
>    禁止只写「私域运营」「品类创新」「超级符号」。
> 4. 每张卡要有「**解决了什么问题**」＋「**得到了什么结果**」，且**至少 1 处可信度标记**（【已核实】等）。
> ⚠️ **已知现状**：早期入库的案例卡仍含大量背景资讯（营收／融资／创始人等），与规则 1 有落差；
> **新写或改写的卡一律按规则 1**，引用时也只挑「动作＋结果」。
`references/cases/` 01–45 号文件，每个含：行业概览／门禁清单／8–16 个深度案例卡（谁做的／做了什么／结果数字／可抄的招／坑）／对接实测清单。

| 号段 | 覆盖 |
|---|---|
| 01–18 | 餐饮茶饮、食品饮料酒、美妆个护、服饰运动户外、3C数码家电、汽车出行、互联网平台、游戏泛娱乐、AI产品、B2B工业与区域品牌、医药健康（含合规红线专节）、母婴宠物、家居建材、零售商超便利店（含**罗森百屿森**）、教育文旅本地生活、金融保险（含合规对照表）、奢侈品高端、酒店度假 |
| 19–30 | 城市区域公共品牌、跨境电商出海、企业服务SaaS、新能源绿色消费、银发经济养老、二手循环消费、潮玩文创IP、影视综艺内容IP、茶饮咖啡烘焙、宠物服务、农业生鲜供应链、体育赛事运动俱乐部 |
| 31–45 | 眼镜视光、珠宝钟表、箱包皮具配饰、香氛气味品牌、美容仪个护小家电、健身运动健康、医美轻医美、口腔专科连锁、餐饮供应链调味品、预制菜方便速食、文具文创办公、摄影影像服务、婚庆人生仪式、招聘雇主品牌、物流快递服务 |
| 46–49 | **机构与方法论类**（乙方案例，每战役一卡）：国际4A与传播集团、战略咨询、本土创意与营销服务商；营销书籍与作者（每书一卡） |
| 50 | **机构出版物与观点库**（每机构一卡）：麦肯锡季刊／BCG／贝恩购物者报告／奥美／电通／WPP／W&K／华与华／和君／年度演讲 IP／行业数据源（含「动机—用法」速查） |
| 51 | **本土数字营销与 MCN**（每机构／每模型一卡）：省广／利欧／引力／天下秀／微播易／分众（媒介代理集团）、无忧／遥望／蜂群／Papitube（MCN）、新消费操盘复盘、平台官方模型（AIPL／FAST／JD GOAL／O-5A／5R） |

> 全量逐卡清单见 Obsidian《营销案例库总索引》（51 类 649 张）。

### 其他支撑文件
| 文件 | 什么时候读 |
|---|---|
| ⭐ `references/08-操作流程SOP.md` | **一接案就读**：从收资料到出稿**每一步的 输入／命令／产出／校验**（配合 `composer.py`，精确到每一步） |
| `references/04-失败归因总库.md` | 写完方案做风险自检（**12 个失败模式 + 34 条预演清单**） |
| `references/05-小企业与新品牌从零打造.md` | 客户是小企业／新品牌。§〇 路由表先定位，再进五阶段、各赛道起盘、零预算打法 55 条、失败归因 16 模式、骗局识别、对接清单 |
| `references/cases/51-本土数字营销与MCN.md` | 要用平台官方模型（AIPL／FAST／5A／DEEPLINK／MCN 打法） |
| `references/01-接案输出模板.md` | **写方案时必读**：逐章写作指南（目的／必含／写法要点／范例段落／常见错误）＋ 附件模板（体检表／Brief／报价／验收指标／一页纸／项目卡／复盘） |
| `references/02-多Agent分工简报.md` | **多 Agent 分工时必读**：五份可直接复制的角色简报 ＋ 三种环境执行方式 ＋ **§二之一 产出规格（深度下限 —— 写不到就是不合格，不是「精简」）** ＋ 主理人裁决规则 |
| `references/07-质量范式-便利店开学季案.md` | ⭐ **质量标尺**：实测逆向出的完整范式（角色产出规格／16 骨架模板／可校验指标／好方案的三个特征）。**不知道「做到什么程度算够」时读它** |
| `references/范例/` | 📄 **交付稿样张＋输入文件样张**：`便利店开学季战役-交付稿.md`（v3，可作模板抄；**要看 Word 版就自己生成**：`python scripts/build_docx.py references/范例/便利店开学季战役-交付稿.md`）、`-原版.md`（早期版对照）、`-任务规则表.json`（门禁 13 项填好长什么样／`build_docx --rules` 的输入）、`-预算表.json`（`budget_check.py` 的输入）。**样张一律简体**（现在全仓都是简体） |
| `references/06-选流派矩阵与对照表.md` | 要判断某件事该外包还是自己干 |
| `references/09-完整策划标准与评分表.md` | **出稿前对标**：大赛（大广赛 100 分评分表）／B 端商业策划／G 端政府项目／投标响应 四类各自「必须有的章节」＋ 咨询公司级维度（MECE／金字塔／SCQA／假设驱动／issue tree）＋ 自评表与缺口清单。composer 的 `--tier 大赛/B端/G端/投标` 场景章节即据此生成 |
| `references/10-文案与物料样本库.md` | **写文案／物料时直接抄**：海报母版、短视频分镜脚本、详情页结构、社群与一线话术、提案 PPT 骨架、G 端公文段落、商务报价与偏离表——每条附「为什么这么写」，不是只喊「写张海报」 |
| `references/11-防返工交付协议.md` | ⛔ **交付物是「要直接递交给他人的长文档」就必读**（方案／报告／标书／投标书／参赛作品），尤其对方给了**模板、大纲或提交清单**时。6 条铁律（先交目录／标题一字不改／素材可见不溶解／禁带内部物／改结构必改引用编号／数字必有来源）＋ 交付前自检 8 项 ＋ 4 条结构性根因。**与 SKILL 顶部 P0 条款配套** |
| ⭐ `references/12-范式库.md` | **动手填骨架前必读**：**六档（速览／标准＝C 端品牌／大赛／B 端／G 端／投标）各自的范式** —— 每档 ＝ 骨架（章节树）＋ **逐节填写指引（该写什么／写几句／必须含哪几个数）** ＋ **可直接改写套用的关键句片段**。共 169 节、3.4 万字。**范式与模板的区别：模板只给空表，范式连「这节写什么算合格」一起给**。由 `build_paradigm.py` 机械生成，勿手改 |
| ~~`archive/机构档案/`~~ | **已退役（2026-09-16）**：机构与方法论材料全部并入 `cases/46-50`（含机构出版物与观点库），机构档案不再单独维护 |

### 交付执行：脚本（**有代码执行能力时必用 —— 这是真正的强制层**）

本 skill 附带 **46 个脚本**（`scripts/`；其中 **40 支有 CLI 入口**，其余为资料/函数库模块），把「流程遵守」从**靠自觉**变成**机械校验**。**交付物一律走唯一入口 `run_pipeline.py`**：

| 脚本 | 什么时候跑 | 不通过的后果 |
|---|---|---|
| **`run_pipeline.py`** | ⛔ **出稿一律走这个** | **唯一入口**：依序跑 ①门禁 ②预算 ③自检 ④深度诊断 ⑤出稿，**任一关不过就中止、不生成 `.docx`**。想拿到交付物，只能从这条链走 |
| **`flow.py`** | **一接案就跑**（流程向导） | 看一眼工作目录，告诉你「**现在第几步、下一步跑哪条命令**」——把 `08-操作流程SOP.md` 的流程变成可机械检查的状态机。`python scripts/flow.py [--dir 案子目录]` |
| **`composer.py`** | ⛔ **动笔前第一步**（真正的「skill」引擎） | **方案组装器**：`--rules 规则表.json --out skeleton.md --tier 速览\|标准\|大赛\|B端\|G端\|投标`。按 `§二 路由表` 机械匹配 3–7 条打法 → 自动拉取具体动作／验收／可抄案例 → **注入「理论依据」（03 模型的「解决什么问题＋前几步」原文）** → 输出骨架。**2026-09-17 起「可抄案例」注入的是真实卡片内容（品牌＋做了什么＋结果），不再是文件路径占位符**；连不出结果的打法会列进骨架的 **§2.0.2 知识库缺口**（**要求补，禁止编**）。映射表在 `scripts/knowledge_map.json`。**`--tier 速览` 即「小客户／快速预览标准」——现有骨架本身就是各类型客户（含小企业／大客户）的标准，不另造标准文件**。**六档定位（2026-09-17 补）**：速览＝小客户／快速预览（只留决策要素）；**标准＝C 端品牌（消费品牌／零售，场景章为货盘・内容矩阵・会员复购・渠道控价）**；大赛＝比赛提案；B 端＝企业／工业品／渠道；G 端＝政府／政企；投标＝招投标响应 |
| **⭐ `build_paradigm.py`** | **改骨架或改指引后** | **范式库生成器**：把 `composer.py` 的骨架（唯一真相）＋ `scripts/paradigm_data.py` 的逐节指引，机械拼成 `references/12-范式库.md`，并供 `composer --internal` 注入「本档填写指引」。**拼装前先做一致性校验**：composer 实际吐出的标题清单 ≠ 宣告清单就直接退出 1（防两份真相漂移）。`python scripts/build_paradigm.py [--dry-run]` |
| **⭐ `structure_fix.py`** | **结构一改就立刻跑**（改章节顺序／合并模板／映射大纲之后） | **交付稿结构体检 ＋ 重编号**：① 体检章号重复／倒序、子编号重复／非递增、**悬空引用**；② `--renumber` 按**正文出现顺序**重编章号与子编号（**标题文字一字不动**，对应协议 2「合并不取舍」）；③ 引用同步改写 —— 带部分号的可靠机械映射，**不带部分号的裸引用（如「（4.3 节）」）一律不猜**，列出来要求人用 `--refmap` 指认。附录自有编号（附录九的 9.1–9.6）原样保留。`python scripts/structure_fix.py plan.md -o fixed.md --renumber [--refmap refs.json] [--report 报告.md]` |
| **⭐ `docx_footnote.py`** | **交付稿要标注引用出处时**（官方常写「引用须用**脚注**标明」） | **给 .docx 装**真**Word 脚注**（python-docx 原生没有脚注 API，这里直接改写 OOXML：建 `word/footnotes.xml` ＋ 把占位 run 换成 `<w:footnoteReference/>` ＋ 补 Content_Types 与 rels）。**Markdown 写法**：正文 `……大盘约 400 亿元[^1]，`，出处单独一行 `[^1]: 弗若斯特沙利文《…》2025, p.12`（定义行不进正文）。`build_docx.py` 已自动接入；`python scripts/docx_footnote.py 方案.docx --list --verify` 可事后核验装配 |
| **⭐ `delivery_check.py`** | **出稿后、提交前（管线第 ⑦ 关）** | **交付形态核对**：① **必交附件核对表（可勾选）** —— `--init` 生成模板，人可勾 `"已交": true`，或按 glob 自动认档；② **封面要素**（官方常要求「仅保留项目名称＋团队名称」，多出日期／赛道／身份栏都报）；③ **文件名规范**（如「项目名称-团队名称-队长姓名」，并专门抓 `【队长姓名】` 这种**占位符没填**）；④ **占位符残留扫描**（`【填】`／`{FILL}`／TODO 为硬错误；`待补`／`待填` 为警告，因为它们在行文里也可能是正常词）。`python scripts/delivery_check.py --dir 交付目录 [--init] [--cover-allow 项目名称,团队名称]` |
| **⭐ `impact.py`** | **改完任何东西，立刻跑** | **影响面分析**：读 `git status` → 算出「**你现在必须重跑什么 ＋ 必须同步改什么**」。**它取代的是「靠人记住跑哪几条」** —— 依赖边写在脚本的 `RULES` 里（改骨架→要同步 paradigm_data；改案例卡→要跑 5 支；改 README→结构表是生成物不要手改…）。`--verify` 会直接把该跑的跑一遍。**每碰到一条没建模的边，就补进 RULES —— 补一条，下次少一次盲目重跑。** |
| **⭐ `start_here.py`** | **开新会话第一步** | 输入客户情况 → 输出「**该读哪几份、读哪一节**」＋「**明确不要读什么**」；`--grep` 跨库检索（**用查代替读**）。仓库已 263 万字，通读是最大浪费 |
| **⭐ `agent_brief.py`** | **派 subagent 前** | 生成 `AGENT-BRIEF.md`（约 3500 字的仓库快照）。**每个 agent 都是全新上下文，没有它就重读整个仓库一遍** —— 已挂在幂等压测里，改架构会自动更新 |
| **⭐ `promise_check.py`** | **改完承诺性文档／新增关卡后** | **「文档承诺 ↔ 实际执行」对账关**：① 字段消费（composer 解析了哪些字段、每个是否进了输出 —— 专抓「解析进内存又丢掉」）② 参考文件消费（references/ 每档是否有脚本真读；区分「仅供人工」与「该消费」）③ 判据对账（curated 承诺→判据关键词表，回归守卫）。**它堵的是本仓库最高频的一类病：能力写在文档里、没有一条链路真的执行它。** `python scripts/promise_check.py [--report 对账.md]` |
| **⭐ `optimize_scan.py`** | **每轮自检时（与 verify_all 配套）** | **每轮产出 10 条「可优化点」**，带位置／现象／为什么算问题／怎么改／严重度。**它与 `verify_all` 是两把不同的尺子**：`verify_all` 证明「没有漂移」，它指出「哪里还能更好」——50 遍全绿证明不了没有改进空间。10 个维度：静默失败／文档漂移／重复常量／退出码语义／异常兜底／遗留标记／硬编码路径／可用性／幂等隐患／参数一致性。**修完本轮重跑会露出下一批**，这才是「每轮 10 条」的正确形态。`python scripts/optimize_scan.py [-n 10]` |
| **⭐ `backlog_cleanup.py`** | **存量清理时** | **案例库存量清理（只标注、不批量改写）**：`--report` 导全量清单（空卡／「严禁公司背景」命中／无洞察栏的卡／数据复述型洞察），`--annotate` 给「无深度卡的清单行」补**纯标记**（`（仅清单·无深度卡）`）。**为什么不批量改写**：这三类都是**内容问题**，批量改写会让教材失真，而教材失真会直接污染下游交付稿 —— 所以脚本只做机械安全的标注，改写留给人。`python scripts/backlog_cleanup.py --report 优化轮次/存量清理清单.md` |
| **⭐ `repo_hygiene.py`** | **整理／清理仓库时** | **冗余／卫生扫描**：既有的校验脚本＋`kb_audit.py` 查的都是「内容对不对」，**没有一支查「这个文件该不该存在」** —— 于是慢慢长出三类冗余：① 孤儿档（全仓零入链引用）② 派生物（`.docx` 等可由源档＋脚本再生，还会**比源档旧**）③ 本机垃圾（`.DS_Store`／`实测-*`／`__pycache__`）。三类都不影响「知识库是否连通」，断链审计永远看不到。`python scripts/repo_hygiene.py`（只读报告）／`--clean`（把**本机垃圾**移入废纸篓）／`--strict`（有可清理项就 exit 1）／`--json`。**只标注、不代删已入库的文件** —— 那是 git 历史的一部分，要人拍板 |
| **维护／辅助脚本**（不常跑，但别忘了它们存在） | `t2s_data.py`（繁→简对照表生成）／`case_clean.py`·`case_relabel.py`（案例集清理与重标号）／`relocate_06.py`（06 选流派迁移，**一次性已完成**）／`_common.py`（**跨脚本共用常量，唯一真相** —— `OK/NG/WARN` 等符号一律从这里 import，不要在各自文件里重定义）／`sync-to-obsidian.sh`（镜像到 Obsidian 离线存档，pre-push hook 触发；**存档说明里的数字一律现算，不手写** —— 手写的数字一定会过期）／`lang_unify.py`（**全仓语言统一**：报还有什么繁体／`--fix` 就地转／`--strict` 当门槛。⚠️ 判据不是内容：`t2s_data.py` 的 `T2S_PAIRS`、`_common.py` 的 `TRAD_HINT`、`smoke_test.py` 的繁体负向夹具**必须保留繁体**，行尾带 `# lang-keep-trad` 护栏） | 对应的维护动作发生时才跑 |
| **⭐ `verify_all.py`** | **改完任何脚本或知识库后（最后一关）** | **全链路验证器**：A 接口（40 支 CLI 脚本 `--help`；纯资料模块不计）／**B 幂等压测 ×50**（第 2 轮起仓库哈希必须零漂移——专抓「每次跑都改一点点」的非幂等 bug）／C 只读脚本 ×50 输出逐字一致／D 端到端真的走完 gate→role→budget→composer→selfcheck→depth→build_docx→run_pipeline **产出一份 .docx**／E 呼叫 `kb_audit`／**F 呼叫 `promise_check`（文档承诺↔实际执行，判失败）＋ `optimize_scan`（可优化点，只报数）＋ `repo_hygiene`（仓库冗余，只报数——**刻意不加 `--clean`**：回归验证不该有副作用）** —— **六条自检收成一个入口，跑一次就知道全部**。`python scripts/verify_all.py [-n 50]` |
| **⭐ `kb_audit.py`** | **每次改完知识库后（全量回归）** | **连通性审计**：量 **7 条跨文件链路** —— 文件引用可解析／打法→案例可达（**含「引用行＝实际取得到的卡」一致性**）／打法×行业全组合覆盖率／**交付物注入（实跑 composer，数真实内容 vs 占位符）**／模型死条目 **＋ 可达率**／既有 6 脚本全跑／**私有痕迹扫描（公开仓库红线）**。**前 6 个脚本查「文件内性质」，这支查「跨文件连通性」——缺了它就会「零件全绿、传动轴断了」**（2026-09-17 用户质问后建立） |
| **`fix_play_cases.py`** | 打法库案例行过时时 | **批量修 `**案例**` 行**：只接受「品牌关键词 @ 档号」，由脚本解析**真实卡片**再生成引用（不可能写出空引用）；解析失败就整批不写；硬不变式＝只改案例行 |
| `play_case_candidates.py` | 修案例行时辅助 | 按 bigram 给每条打法的候选卡短名单（**只是排序器，不做决定**） |
| **`case_play_index.py`** | 维护案例库／打法库后 | **案例卡 ↔ 打法 双向索引**：从 `00-打法库` 每条的 `**案例**` 行反查出「哪几张卡能抄这条打法」，写 `scripts/case_play_index.json`（供 `composer.py` 消费）并在卡片标题下打 `> **可抄打法**：§1.1 超级符号 ｜ …`。**实测：全库只有 32/104 条打法的案例行指名了品牌 → 未标记 ≠ 不适用** |
| **`file_meta.py`** | 改了章节或卡片后 | **文件头自解释块**（这是什么／什么时候读／读完你能／目录／规模）**全机械生成**，改完重跑即同步，永不与内容漂移 |
| **`case_sections.py`** | 改过案例档标题后 | **章节标题收敛**到固定槽位（56 种写法 → 9 个），序号按档内实际顺序重编；不符就报错 |
| **`case_gap_fill.py`** | 案例库缺口巡检 | 检查 01–45 是否都具备「本行业常见死法」「查不到的部分」两节（缺就补，带逐行还原不变式） |
| **`rename_tidy.py`** | 改了顶层文件名／序号后 | 文件名与 H1 统一 + **全库引用同步**，硬不变式＝旧文件名不得残留 |
| **`top_titles.py`** | 改了 05／06 顶层标题后 | 顶层工作档的 `## ` 标题统一（层级修正＋序号连续），**只动标题行** |
| **`reformat_to_template.py`** | **客户/比赛给了格式范本时** | **按范本重排**：解析范本骨架 → 按映射填入 → **硬要素核对**（丢一件就退出码 1）。范本要而源稿没有的章节会标【待补】；源稿多出来的整节移入附录（协议 2：合并，不取舍） |
| `gate_check.py` | 门禁问完后、**动笔前** | 缺项 → **禁止产出任何方案内容**（协议 1） |
| `budget_check.py` | 写完预算表 | 分项加总 ≠ 合计 → 不准交付（协议 3 第 7 项） |
| `selfcheck.py` | 交付前 | 缺章节／缺自检单／内部文档泄漏 → **不准交付**（协议 3） |
| `depth_check.py` | 交付前**跑一次、看报告** | **只诊断、不阻拦**（用户定调 2026-09-14）。查 9 个维度：竞品／风险四件套／KPI 预警线／物料／禁用词／**重复度**／空话词… 报告给你看，**改不改由你决定**。便利店开学季案原版同样会有提示 —— 那是正常的 |
| `build_docx.py` | 最终出稿 | **内部跑 selfcheck（结构门槛：缺章节／缺自检单／内部文档泄漏 → 拒绝）**；depth_check 只印报告、**不阻拦** |
| **`role_check.py`** | 出稿前（**唯一入口的第 ② 关**） | 校验《多 Agent 分工记录》：5 个角色产出 ＋ 裁决记录 → **缺就不出稿**（SKILL：分工每次必走） |
| **`case_lint.py`** | 维护案例库时 | 体检案例卡的「公司背景」违规（规则写了、以前没脚本查）；`--max` 设容忍上限（**预设 900**）；`--strict` 关闭身份栏／归因栏豁免回到裸计数。规则与两项例外见 `references/cases/README.md` §六 |
| **`case_upgrade.py`** | 维护案例库时 | 体检每张卡「五要素齐不齐 ＋ 字数够不够」（`--min`／`--list`／`--file NN`） |
| **`case_order.py`** | 改完案例卡后 | **要素顺序规范化**（①→谁做的→②→洞察→③→④→落地拆解→⑤→可抄的点→适配／常见错误／本档关联→局限）；**零删除**，带「非空行 multiset 不变」硬不变式 |
| **`case_upkit.py`** | **批量升级案例卡时** | **工具包**（import 用，不是命令）：`blocks()` 定位卡 → `fill()` 按字段补块 → `run()` 跑修复＋不变式校验＋写盘。**新写一档升级脚本只需写「内容 dict ＋ 修复清单」，框架不用重写** |
| **`case_digest.py`** | 升级案例卡前 | **卡片骨架摘要**：`python scripts/case_digest.py 27,28 120` —— 只印 ①／②／洞察／③／⑤／局限，**一屏看完一档，不用读整档**（决定这档是「补尾块」还是「写正文」） |
| **`case_scan.py`** | 升级案例卡前 | **断句损坏扫描**：`python scripts/case_scan.py 27` —— 按「粗体后缺冒号／句尾被截／括号不成对／替代符残留／粗体不成对」五类，打印**完整损坏行**，便于一次写完精准修复 |
| **`smoke_test.py`** | 改完任何脚本后 | 一条命令跑 12 项冒烟检查（该过的过、该拦的拦） |

**这是本 skill 最强的强制点**：交付物只能由 **`run_pipeline.py`** 产出（它串起门禁→预算→自检→诊断→出稿）—— **拿不到 `.docx` ＝ 没完成**，AI 绕不过。

```bash
# ⛔ 唯一出稿路径（任何一关不过就中止）
python scripts/run_pipeline.py --rules rules.json --budget budget.json \
    --plan plan.md -o 方案.docx --title "客户名 营销方案" --date 2026-09-16
```

```bash
# 完整流程（依序，前一步不过就不准往下）
python scripts/gate_check.py gate.json        # ① 门禁放行（13 项 + 规则表已确认）— 单独跑用于调试
python scripts/budget_check.py budget.json    # ② 预算校验（含盈亏线测算）
python scripts/selfcheck.py plan.md           # ③ 交付前自检（结构/自检单/泄漏/禁用词）
python scripts/build_docx.py plan.md -o 方案.docx \
    --title "客户名 营销方案" --date 2026-09-14   # ④ 出稿（自动先跑③）
```

**依赖**：Python 3 + `python-docx`（`pip install python-docx`）。

**没有代码执行能力时**（纯对话环境）→ 退回 Markdown 协议：
- 手工对照 §六 清单逐项核对
- 把 12 项《交付自检单》**原样输出**在回复中并标 ✅/❌
- 附「样式对照说明」（哪一级是标题几、哪些是表格），让用户贴进 Word 一键套样式

> 不论走哪条路，交付物的**内容结构**必须符合 §三 的八篇 + 附件规范。

---

## 五、对接实测清单（门禁用 / 见客户照着问）

> 用法：对接时按这张表问，当场填。**答不上来的先留空，不猜不编。**

### A. 生意基本盘
| # | 要问到什么 | 怎么问（原话） |
|---|---|---|
| A1 | 卖什么、客单价 | 「你们主要卖什么？平均一单多少钱？」 |
| A2 | 毛利 | 「一单大概赚多少？成本主要在哪些地方？」 |
| A3 | 卖给谁、什么场景 | 「来买的主要是什么人？什么情况下会想到你们？」 |
| A4 | 现在规模 | 「一个月大概做多少？几家店／多少用户？」 |
| A5 | 复购频次 | 「老客户一般多久来一次？回头客占多少？」 |

### B. 目标与卡点
| # | 要问到什么 | 怎么问 |
|---|---|---|
| B1 | 生意目标 | 「这段时间最想解决的一件事是什么？做到什么算成功？」 |
| B2 | 自述卡点 | 「你觉得现在最大的问题是什么？」 |
| B3 | **真实卡点**（追问） | 「这个问题是什么时候开始的？之前试过什么？为什么没成？」 |
| B4 | 时间要求 | 「希望多久看到效果？有没有必须卡住的时间点？」 |

### C. 预算与决策
| # | 要问到什么 | 怎么问 |
|---|---|---|
| C1 | 预算区间 | 「这件事打算投入多少？是一次性还是按月？」 |
| C2 | 谁出钱 | 「这笔钱是你自己出还是有其他来源？」 |
| C3 | 决策人 | 「这个决定是你拍板还是要跟谁商量？」 |
| C4 | 付款习惯 | 「之前跟外面合作，付款方式一般怎么谈？」 |

### D. 资源与能力
| # | 要问到什么 | 怎么问 |
|---|---|---|
| D1 | 团队 | 「现在有几个人？谁能腾出手做内容／跑物料？」 |
| D2 | 私域基础 | 「有没有加过客人微信？有没有群／会员？」 |
| D3 | 数据基础 | 「收银／订单数据能导出来吗？有没有后台？」 |
| D4 | 老板出镜意愿 | 「你自己愿不愿意出镜拍视频？」 |
| D5 | 供应链 | 「产品是自己做的还是进货？能不能改规格／包装？能不能退？」 |

### E. 竞争与市场
| # | 要问到什么 | 怎么问 |
|---|---|---|
| E1 | 对手 | 「生意最好的是谁？他们凭什么？」 |
| E2 | 差异点 | 「客人为什么选你不选他？」（追问：有客人原话吗？） |
| E3 | 价格水位 | 「同行大概卖多少钱？」 |
| E4 | 对手软肋 | 「他们有什么做不到的？」 |

### F. 过去做过什么
| # | 要问到什么 | 怎么问 |
|---|---|---|
| F1 | 做过什么营销 | 「之前做过哪些推广？花了多少？」 |
| F2 | 有效的 | 「哪次效果最好？好在哪？」 |
| F3 | 无效的 | 「哪次完全没用？你觉得为什么？」 |
| F4 | 被谁坑过 | 「之前有没有找过外面的人做？结果怎样？」 |

### G. 硬约束
| # | 要问到什么 | 怎么问 |
|---|---|---|
| G1 | 行业监管 | 「你们这行广告有没有什么不能说的？」（医药／金融／教育／房产重点问） |
| G2 | 场地/场域限制 | 「场地有什么规矩？（能不能外摆、能不能贴、能不能进）」 |
| G3 | 品牌授权 | 「能不能用品牌方的 LOGO／VI？」 |
| G4 | 资金状况 | 「现金流紧不紧？能承受多久不回本？」 |
| G5 | 老板偏好／忌讳 | 「有没有什么是你一定不想做的？」 |

### H. 交付期待
| # | 要问到什么 | 怎么问 |
|---|---|---|
| H1 | 要什么 | 「你要的是一份方案，还是要人帮你执行？」 |
| H2 | 验收方式 | 「你怎么判断这件事做成了？」 |
| H3 | 对接人 | 「日常对接找谁？多久对一次？」 |
| H4 | 给谁看 | 「这份东西最后给谁看？」 |
| H5 | **目标字数／页数** | 「这份东西有字数或页数要求吗？」（比赛、招标、客户模板经常会规定）→ 没要求就按 **5,000–10,000 字**预设 |
| H6 | 有没有参考格式 | 「有没有指定的模板或提交清单要我照着写？」（→ 触发协议 2：合并不取舍） |

---

## 六、交付前自检清单（每份方案必跑）

> **与协议 3 的关系（别搞混）**：
> - **本节 = 完整内部核对清单**（更细），用来逐条检查、发现问题。
> - **协议 3 的《交付自检单》= 必须原样输出给用户的摘要版**（12 项，标 ✅/❌）。
> - 顺序：**先跑本节逐条核对 → 改完 → 再把协议 3 的摘要单输出在交付回复中。** 两者不是二选一。

出稿前逐条核对，**任一不过就回去改**：

**数字类**
- [ ] 预算表分项加总 = 合计？
- [ ] 盈亏线/保本点算得对不对（动销率 × 毛利 − 固定成本）？
- [ ] 引用的案例数据都有来源标注？【未核实】的数据有没有从对外交付物里剔除？
- [ ] KPI 目标是否可测（有基准值、有观测方式）？

**执行类**
- [ ] 每条动作都写了「谁做」？
- [ ] 每个负责人的工作量加起来，他一个人有时间做完吗？（**执行人力检查**）
- [ ] 如果人力不够，删减顺序写了吗？
- [ ] 所有时间节点对得上日历吗（有没有撞上假期/周末/已定事件）？

**风险类**
- [ ] 对照 `04-失败归因总库.md` 过一遍，本方案最可能死在哪？
- [ ] 关键假设列出来了吗？每条有验证方式与成本吗？
- [ ] 假设不成立时的备用方案有吗？
- [ ] 合规红线（广告法／行业监管／平台规则）过了吗？
- [ ] 与客户的硬约束有没有冲突？

**表达类**
- [ ] 有没有出现「做私域」「加强内容」「提升品牌力」这类没有动作的废话？
- [ ] 结论有没有前置（第一页就能看到做什么）？
- [ ] 禁用词与对外口径写清了吗？

**方法论类（2026-09-16 新增，对应 §三 硬要求 4、5）**
- [ ] **打法组合表**在吗？8 列齐吗（打法／为什么用它／具体动作／谁做／花多少／多久见效／验收指标／可抄案例）？
- [ ] 每条打法的「**为什么用它**」写的是说服逻辑（差异化／成本／对手做不到），不是形容词？
- [ ] **可抄案例**列引用了 `cases/` 具体卡片吗？
- [ ] 诊断章有 **A–H 问题类型标签**吗？
- [ ] 卡点写成了**重构句**（「不是 X——是 Y」）并给了**核心动作一句**吗？
- [ ] 风险每条写全四件套了吗（为什么会发生／预警信号／兜底预案／预防动作）？**有没有把「模式 NN」这种内部编号写进稿子**？
- [ ] 有没有写「**不承诺的事**」（防期待错位）与「**下一步只写一件**」（先导阀）？

---

## 七、知识合并步骤（把线下拿到的知识写回 skill）

> **每次对接／项目结束后执行。** 操作原则：**增量续写，不整篇覆盖**；不新建重复页。

| 你拿到的东西 | 合并到 | 具体位置 |
|---|---|---|
| 一个新的**营销打法** | `references/00-打法库.md` | 对应打法分类下新增条目 |
| 一个新的**案例** | `references/cases/{行业}.md` | 「案例清单」加一条；值得深挖的写进「深度拆解」 |
| 一个**行业常识／潜规则** | `references/cases/{行业}.md` | 「本行业打法地图」或新增「行业概览」节 |
| 一次**失败** | `references/04-失败归因总库.md` | 对应失败模式下的实例；新模式则整节新增 |
| 一个**新的问法** | 本文件 §五 | 对应分组加一行 |
| 一个**新的方法论／工具** | `references/03-方法论操作手册.md` | 对应类别下按七段结构新增 |
| 一个**新的行业** | `references/cases/` | 新建行业文件，沿用五节结构 |
| 一份**完整的交付方案** | `references/cases/{行业}.md` 或新建 `projects/` | 作为范例存档（去识别化后） |

**合并红线（❌ 不写进 skill）**：
- 公司背景：创始人、董事长、成立年份、股权、融资、员工数
- 机构的营收／人事／奖项数量（除非它是这个案例的效果数据）
- 与营销动作无关的行业八卦
- 未经核实的数据
- 客户隐私：真实客户名、联系方式、报价底线

**合并后要做**：更新本文件 §四 索引（若新增文件）→ 更新 §八 变更记录 → 旧内容过时则**升级替换**。

### 改案例库后的脚本链（顺序很重要）

```bash
python scripts/file_meta.py                      # ⓪ 文件头自解释块（改了章节后重跑，目录行自动同步）
python scripts/case_sections.py --fix            # ① 章节标题规范化（改标题后必跑）
python scripts/case_order.py --fix               # ② 卡片要素顺序（零删除，带不变式）
python scripts/case_upgrade.py --list            # ③ 看还差多少（五要素齐 ＋ ≥2500 字）
python scripts/case_lint.py                      # ④ 查「公司背景」违规（--strict 裸计数）
python scripts/case_gap_fill.py                  # ⑤ 检查 01–45 的「常见死法／查不到」两节齐不齐
python scripts/case_play_index.py --fix          # ⑥ 重建「案例卡 ↔ 打法」索引 ＋ 打「可抄打法」标
python scripts/kb_audit.py                       # ⑦ **连通性审计（全量回归，七条链路）**
python scripts/verify_all.py -n 50               # ⑧ **全链路验证 ＋ 50 遍幂等压测（最后一关）**
python scripts/smoke_test.py                     # ⑨ 改过任何脚本后
```

**改了 `00-打法库` 的 `**案例**` 行之后**，**必须**跑 ⑥ —— 那是「打法 ↔ 案例」两边唯一的连接来源；
不跑，骨架里的「可抄案例」就会退回占位符（或用户端直接看不到卡片内容）。

> ⚠️ **改案例卡后必须跑 ① + ② + ③**。历史上多次批量改造把「③ 做了什么」搬到卡片最后、
> 或把 `### 3.2` 标题的换行吞掉（变成 `---### 3.2`，让它不再是一级标题）——**这类损坏肉眼很难发现**，
> 靠 `case_order.py`／`case_sections.py` 的机械校验 ＋ 硬不变式才能保证不丢内容。

**⛔ 不写进来的**：公司背景（创始人／成立年份／股权／融资／营收／人事）、未核实数据、客户隐私。
**原则**：增量续写，不整篇覆盖；同名迭代走**升级替换**，不版本堆叠。

> 新增或改名任何档 → **必须同步三处**：本文件 §四 索引、`references/cases/README.md`（若涉及案例档）、
> `AGENTS.md` §五 文件地图。

---

## 八、变更记录

- 2026-09-14 初版：以「机构档案」为组织方式。
- 2026-09-14 二次扩充：补国际广告集团与中国创意热店。
- 2026-09-14 三次扩充：补其他咨询公司。
- 2026-09-14 **方向纠正（第一次）**：用户指出内容主体应是「乙方为甲方做的案例」与「能直接调用的营销方法」，而非公司介绍；机构档案移至 `archive/`。
- 2026-09-14 **方向纠正（第二次）**：定位为「商业对接后快速生成营销方法」的生成器；新建 18 个行业案例集（条目式清单 + 深度卡）。
- 2026-09-14 **第三次修正（本版）**：以便利店开学季案（事实底稿 ＋ 交付稿）为质量基准，新增 ——
  1. **第 0 步门禁**：12 项必问清单，没问清不出方案；
  2. **六步工作流**：事实底稿 → 现状分析 → **多 Agent 分工（每次必走）** → 主理人裁决 → Word 交付；
  3. **交付物规范**：一个 `.docx`，八篇 + 附件，简体（篇幅规则见下方第四次修正，**「≥ 2 万字」已废除**）；
  4. 新增必写章节：禁用词与口径、执行人力检查、关键假设与验证、待解决问题清单；
  5. 新增 **§六 交付前自检清单**（数字／执行／风险／表达四类）。
- 2026-09-14 **第四次修正（强制执行协议版）**：针对「执行者把 skill 当灵感库而非验收标准」的复盘——
  1. 顶部新增 **⚡ 强制执行协议** 五条：启动即立《任务规则表》（门禁一次问全＋纠偏即升级）、结构冲突「合并不取舍」、**交付前 12 项自检单必须原样输出在交付回复中**、工具盘点先于路径（连接器优先、禁默认爬虫、付费生成先报价）、交付形态红线（内部过程文档不得入交付稿）；
  2. **篇幅规则修改**：删除「正文 ≥2 万字」硬编码，改为「门禁先问用户目标字数，未指定时预设 5,000–10,000 字」，深度以硬要素齐全衡量、不以字数注水；
  3. `references/04-失败归因总库.md` 新增附录「AI 执行者使用本生成器的失败模式」5 条（骨架式使用/跳过自检/门禁漏项/工具惯性绕路/结构冲突做减法）。

- 2026-09-16 **架构整理版**：
  1. 顶层文件按流程顺序重排：`00-打法库`（引擎）→ `01-接案输出模板` → `02-多Agent分工简报` → `03-方法论操作手册` → `04-失败归因总库` → `05-小企业与新品牌从零打造` → `06-选流派矩阵与对照表`（选型入口）；全部交叉引用同步更新（SKILL/AGENTS/README/内部引用，共 9 文件 115 处）；
  2. **机构案例库并入 cases/**：4 册转为 `cases/46-49`，统一为案例卡格式——机构册**每战役一卡**（谁帮谁做了什么＋结果＋可抄的招）、书籍册**每书一卡**，共 192 卡；导言保留选册判断逻辑并改写为门禁清单与对接实测清单；
  3. 案例库规模（**2026-09-17 最终口径**）：**51 类 / 649 张深度卡** ＝ 01–45 行业档 411 张 ＋ 46–51 机构与方法论档 238 张。
     （口径统一为「编号深度卡」数；历史数字 419 案 → 611 案 → 628 案 → 620 卡均已作废，勿再引用。）

- 2026-09-16 **质量门槛增补（用户以《中百罗森新生开学引流方案》为基准纠偏）**：
  1. 新增**硬门槛 4：打法组合表**（8 列，缺「为什么用它」或「可抄案例」＝不合格；`selfcheck.py`【7a】机械拦截）；
  2. 新增**硬门槛 5：诊断三件套**（A–H 问题类型＋卡点重构句＋核心动作一句）；
     ⚠️ 同一批还加了「风险挂『模式 NN』编号」—— **该条已于 2026-09-17 废除**（那是内部座标，
     selfcheck 第【10】关会硬拦）。保留这行只为说明来龙去脉，**不要照做**。
  3. **A–H 问题类型分类首次明确定义**（第 2 步，此前 SKILL/模板两处引用却无定义 —— 完整性缺口已补）；
  4. `selfcheck.py` 新增【7】核心方法论要素四项校验；`01-接案输出模板.md` 新增「2.0 打法组合表」完整规格与范例；范式增补第七章；
  5. §六 交付前自检清单新增「方法论类」7 条。

- 2026-09-16 **公开仓库整理版**：
  1. **范例集中化**：新建 `references/范例/`，收入 `便利店开学季战役-交付稿.md/.docx`（v3 实测版）与 `-原版.md`；
  2. **去识别化**：范例与质量范式中的具名主体全部替换（某连锁便利店／某高校／本地批发市场），
     保留全部数字与方法论；`cases/14` 的公开商业案例（百屿森）不受影响；
  3. **命名统一**：`范本-罗森案范式.md` → `07-质量范式-便利店开学季案.md`（接入 00–08 编号体系）；
     `范本-罗森案范例（用户认可原版）.md` → `范例/便利店开学季战役-原版.md`；「罗森案」代称 → 「便利店开学季案」；
  4. **语言统一**：内部文档一律繁体（cases/44–50 已转正）；**范例样张一律简体**（交付物语言）。

- 2026-09-17 **结构与可用性整理版（五件事一次做完）**：
  1. **融 README**：`references/README.md` 零独有内容（维护规则已在 SKILL §七）→ 移除，3 个 README 收成 2 个（根 ＋ `cases/README.md`）；
  2. **补 16 档缺口**：`20 21 23 24 26 27 28 29 32 33 40 41 42 43 44 45` 各补「本行业常见死法」＋「查不到的部分」
     （新增 `scripts/case_gap_fill.py`，带「删掉插入块须逐行还原原文」硬不变式）；
  3. **06 进 cases**：顶层 06（本土数字营销与MCN）→ **`references/cases/51-本土数字营销与MCN.md`**（29 卡，
     甲乙丙丁四组：本土媒介代理集团／MCN／新消费操盘复盘／平台官方模型），补齐六槽结构；
     **05／07 只统一标题**（`scripts/top_titles.py`：05 的「阶段一～五／路径一～三」由 H2 降 H3，H2 序号 〇～十二 连续）；
  4. **重新梳理命名**：顶层序号补空 **07／08／09 → 06／07／08**；H1 统一为「名称（＋语义限定词）」；
     全库引用同步 68 处（`scripts/rename_tidy.py`，硬不变式＝旧文件名不得残留）；
  5. **让知识库真的被用上**（修的是机制，不是文字）：
     a. `composer.py` 的「可抄案例」**注入真实卡片内容**（品牌＋做了什么＋结果），不再印文件路径占位符；
     b. 「理论依据」**注入 03 模型的「解决什么问题＋前几步」原文**，不再只给编号；
     c. 新增 **§2.0.2 知识库缺口**：连不出案例的打法**逐条列出并要求补**——**宁可空著，禁止编一个案例**；
     d. 新增 `scripts/case_play_index.py`：从 `00-打法库` 的案例行**反查**「案例卡 ↔ 打法」双向索引
        （`scripts/case_play_index.json`），并在卡片标题下打 `> **可抄打法**：…`。
  - ⚠️ **本版留下的最大待办（已于同日修完）**：`00-打法库` 的 104 条 `**案例**` 行是**早期 6 档时代写的**
    ——只有 **32/104** 指名了品牌，且只覆盖 `01–06` 六个文件；全库 649 张卡里**只有 14 张**能自动对上打法。
    这就是「库里的东西没被用进交付物」的**唯一机制根因**。

- 2026-09-17 **连通性修复版（用户质问「为什么还是有这么多问题」后建立）**：
  **根因**：既有 6 个校验脚本（`file_meta`／`case_sections`／`case_order`／`case_upgrade`／
  `case_lint`／`smoke_test`）查的**全是「文件内性质」**，没有一个查**「跨文件连通性」** ——
  所以 60/60 文件头合规、51/51 章节一致、409/409 卡塔尔标、12/12 冒烟全绿，
  而唯一的产出引擎 `composer.py` 还在印「来源 cases/xx.md，请展开…」。
  **零件全合格，机器的传动轴是断的，而没有一支仪器量传动轴。**
  1. **新增 `scripts/kb_audit.py`（连通性审计）**：量 6 条链路 —— L1 文件引用可解析／
     L2 打法→案例可达／L3 案例→打法覆盖率／L4 交付物注入（实跑 composer，数真实内容 vs 占位符）／
     L5 模型死条目／L6 既有 6 脚本全量回归。**断链类（L1/L2/L4/L6）才影响退出码，覆盖率类只报数**。
  2. **修复 `00-打法库` 全部 104 条案例行**（新增 `scripts/fix_play_cases.py`）：
     只接受「品牌关键词 @ 档号前缀」，由脚本从 `cases/` 解析出**真实卡片**再生成引用 ——
     因此不可能写出空引用。**修复中发现 `农夫山泉`、`星巴克` 在库里根本没有卡片**（旧行引的是不存在的卡）。
     硬不变式：每条只改 `**案例**` 一行，其余逐行不变。
  3. **修 `composer.parse_cards` 的三格式兼容 bug**：第一版只认 2 种案例清单写法，
     `29／32／33／51` 用的第三种（`N. **品牌**｜…`）静默回 0 张卡 —— **这个 bug 是 kb_audit 抓出来的**。
     同时支持机构档（46–51 无案例清单，卡片即 `### 3.N 标题`）。
  4. **效果**：
     | 指标 | 修复前 | 修复后 |
     |---|---|---|
     | L1 文件引用断链 | 3 | **0** |
     | L2 打法→案例 可达 | 19/104（18%） | **104/104（100%）** |
     | L4 交付物占位符 | 3 | **0**（真实卡片内容 6 → 18 处） |
     | 卡片「可抄打法」标记 | 14 张 | **75 张 / 27 档** |
     | 既有 6 脚本 | 全绿 | 全绿（未被破坏） |
  5. 新增 `scripts/play_case_candidates.py`（候选卡排序器，修案例行时辅助）。**脚本表 26 → 29**。
  6. **方法论教训（写进 `AGENTS.md` §四 降级表旁）**：
     ① 校验要分「零件合格」与「链路可通」两类，**只查前者必然漏掉后者**；
     ② 指标不要自证 —— 上限／阈值由自己定，达标率就永远好看；
     ③ **每次改动都要全量回归**，不能「改哪查哪」，否则早期产物会被库的增长甩成断链。

- 2026-09-17 **L3／L5 修复 ＋ 全链路 50 遍自检版**：
  1. **L3 先修「设计」再修「数字」**：查下去发现真问题不是覆盖率低，而是
     `pick_cards` **完全没用行业档** —— 银发养老的客户会被配上美妆／茶饮的例子，**「可抄案例」不可抄**。
     改成两段式：**1 张精准对标（打法案例行指名）＋ 1 张本行业可抄**。
     结果：(打法 × 行业) **5304 组全部有卡（空手 0）**，其中 **2375 组（45%）含本行业卡**；
     其余是「该行业确实没有语义匹配的卡」——**不硬凑**，只回跨行业标杆。
  2. **卡片宇宙口径统一**：`kb_audit` 原本数「深度卡 609」，而 picker 操作的是
     「案例清单条目 ∪ 深度卡 983」——**两个分母对不上，覆盖率算出来是假的**。
  3. **`parse_cards` 取「案例清单 ∪ 深度卡」并集**：实测 25 个文件里有 **48 张深卡不在自己的案例清单里**，
     只读清单的 Agent 永远看不到、composer 也引用不到。
  4. **L5 修到底（两层）**：第一层「映射表引用但手册里不存在」＝死条目 → 0；
     第二层才是真问题 ——**「被 JSON 引用」≠「挑得到」**：接入 33 个模型后仍只有 14 个能被
     `theory_for` 选中。修法：① `theory_for` 的 override 由「整组替换」改**可累加**；
     ② override 由 `[:3]` 截断改为**人手指定的全部给出**；③ 逐条把 49 个模型绑到它真正该管的打法。
     **结果：111/111 模型全部可达（0 死条目、0 未接线、0 挑不到）。**
  5. **新增 `scripts/verify_all.py`（全链路验证 ＋ 50 遍幂等压测）**：
     A 接口（40 支 CLI 脚本 `--help`；纯资料模块不计）／B **幂等 ×50**（第 2 轮起仓库哈希必须零漂移）／
     C 只读脚本 ×50 输出逐字一致／D **端到端真的产出一份 .docx**（gate→role→budget→composer→
     selfcheck→depth→build_docx→run_pipeline）／E 呼叫 `kb_audit`。
     **实测 50 遍：全绿、仓库零漂移（5 分 54 秒）。** 脚本表 29 → 30。
  6. **本轮又抓到 4 个真 bug**（全部由新仪器发现，非事后诸葛）：
     · `fix_play_cases.resolve` 用「标题最短」判定 → 选中副卡（王老吉被选成「礼品场景」那张）
     · `hint_candidates` 丢掉「品牌↔档」对应 → 同一品牌跨文件重复抽出（淄博烧烤、石头科技）
     · `play_overrides` 有个 2 字符旧键 `'VI'` **短键遮蔽长键** → 改为最长键优先
     · `verify_all` 自己的 CLI 写错（gate_check／role_check 收位置参数）、哈希基线取早了
  7. **新增不变式「引用行＝实际取得到的卡」**（L2 的一部分）：案例行写的品牌，
     composer 必须真的取得到 —— 这条一上线就暴露 11 条旧的「描述式」案例行，已一并重写。

- 2026-09-18 **冗余清理＋仓库卫生版**：
  1. **新增 `scripts/repo_hygiene.py`（仓库冗余／卫生扫描）**：既有的 6 支校验脚本＋`kb_audit.py`
     查的都是「**内容对不对**」（引用通不通、卡片齐不齐、模型可不可达），
     **没有一支查「这个文件该不该存在」** —— 于是仓库会慢慢长出三类冗余：
     ① 孤儿档（零入链引用）② 派生物（`.docx` 等可由源档＋脚本再生）③ 本机垃圾（`.DS_Store`／`实测-*`）。
     三类都不影响「知识库是否连通」，所以断链审计永远看不到。本脚本补的就是这一格。
     用法：`python scripts/repo_hygiene.py`（只读报告）／`--clean`（把本机垃圾移入废纸篓）／`--strict`。
     **只标注、不代删已入库的文件** —— 那要人拍板并写进 commit message。
  2. **清掉两份冗余资产**（皆**已删除**）：
     · **已删除**：`references/范例/便利店开学季战役-骨架示例.md` —— 全仓零引用，
       30 节 vs 现范式的 301 节，严重过时；骨架现在由 `build_paradigm.py` 实时生成。
     · **已删除**：`references/范例/便利店开学季战役-交付稿.docx` —— 陈旧派生物，
       比它的源档 `.md` 早 9.1 小时，且可由 `build_docx.py` 随时再生（`.md` 才是源）。
     四处文档引用同步改为「要看 Word 版就自己生成」。
  3. **`kb_audit.py` L1 新增「裸文件名引用」检查**：原本只认带 `references/|scripts/|cases/` 前缀的引用，
     于是 `SKILL.md` 里写成 `格式范本映射示例.json` 的那一处**是坏引用却查不出来**
     —— 它**已改名**为 `格式范本映射示例.json`（**简体「范」**）。补上 `BARE_PAT` 后
     L1 断链从 51 处降到 **0 处**（其余为模板／命令／通配／已删档／链路写法，按上下文豁免）。
     同批修掉一个**只在错误路径上才显形**的 bug：`bad` 清单混了字符串与二元组两种形状，
     `bad` 一空就相安无事，**一旦真的有断链，印报告时就炸 `ValueError: too many values to unpack`**
     —— 断链越严重它越崩。
  4. **`cases/50-机构出版物与观点库.md` 修 15 处指向已退役机构档的引用**（如 `01-麦肯锡.md` → `47-战略咨询.md` 的麦肯锡段）。
  5. **修掉影响面分析器自身的「短键遮蔽长键」bug**：`impact.py` 原本命中第一条规则就 `break`，
     于是通用规则把专门规则整个遮掉 —— 改 `repo_hygiene.py` 时算出的全是 `scripts/*.py` 的通用建议。
     改为**最具体者胜**：判据是**通配符之前的字面前缀长度**（`references/*.md` 比 `references/范例/` 字符更多，
     但它只锁定 11 个字面前缀、后面全放行 —— 纯比长度会判反）。与 `infer_industry()`／`play_overrides`
     同规则，已在 `AGENTS.md` §四之一 记为**同一类 bug 的第三个实例**。
  6. **教训（写进本版）**：**校验器的盲区不在「查得不细」，而在「查的维度以外」** ——
     连通性审计再密，也照不到「文件该不该存在」；而**错误路径从未被测过**的检查器，
     会在真正出问题的那一天自己先崩掉。

- 2026-09-18 **全仓语言统一（繁 → 简）**：
  1. **取消双轨语言**（用户裁定「**全部都改成简体吧**」）：原先「交付物简体／内部文档繁体」，
     代价是一个永久的转换步骤（`composer` 注入知识库内容时要逐字 t2s），
     而且「哪一份该是哪一种」只能靠人记 —— 记错就是交付稿混进繁体。
     统一之后知识库就是简体 → **注入时不用转，少一个会出错的环节**，规则从两条变一条。
     **实测规模：129 个文件、615,240 个字**（字体层）。
  2. **新增 `scripts/lang_unify.py`**（可复用，不是一次性脚本）：报还有什么繁体／`--fix` 就地转／
     `--strict` 当门槛。引擎优先 `opencc`（词组级），没装则退回**本仓库自带的** `t2s_data.T2S_PAIRS`
     （零依赖），**用了哪个会明示** —— 不静默降级。
  3. ⚠️ **这一轮最重要的发现：判据不是内容。**
     机械转换对「**用某批字去侦测另一批字**」的表，语义方向是**反的**。实测踩到两处：
     · `selfcheck.py` 的 `_trad_hint`、`build_docx.py` 的 `_trad` —— 这两份「繁体字提示集」
       **本身就是繁体字**，被转成简体后，开始**反过来把简体字判成繁体**：
       简体标杆稿当场被判「67 种繁体字」而拦下，**冒烟测试抓到**。
     · `scripts/smoke_test.py` 的**繁体负向夹具** —— 转掉之后「繁体稿应被拦」就变成
       在测一份简体稿（永远该通过）→ **测试静默失效**。
     对策：新增 `# lang-keep-trad` 行级护栏（`lang_unify.py` 会跳过带这个标记的行），
     并把两份已漂移的繁体判据表**合并进 `_common.py` 的 `TRAD_HINT`**
     （原先各写一份，build_docx 那份少了末尾 6 个字 → 少侦测 6 个字）。
     **教训：加「批量转换」这类工具时，先问「这批东西里有没有『尺子』」。**
  4. **词汇层也一起换（港台用词 → 内地说法）**，共 **91 对、1,158 处**：
     · **引擎不可用**：opencc 的 `tw2sp`（台湾正体→大陆用词）拿已转简体的文本去跑会乱套，
       实测「什么」→「什幺」、「小程序」→「小进程」、「核心结论」→「内核结论」。
       **词汇替换只能是人工审定的表** —— 候选取自 opencc 的 `TWPhrasesRev.txt`（518 对）
       ∩ 本仓库实际出现（137 对），再逐条人工核上下文。
     · **否决约 60 条**，理由写进 `lang_unify.py` 的 `VOCAB` 注释（免得下次有人照表全转）。
       最大一类是「**它本来就是内地标准词**」：核心→内核／执行→运行／线上→在线／指标→指针／
       建立→创建／文件→文档／互动→交互／整合→集成／程序→进程／预设→缺省／通道→信道／
       智慧→智能／社群→社区／离线→脱机／启用→激活 …… 照表换会把「执行摘要」「线上线下一体」
       「KPI 指标」「小程序」改成错的。
     · **同形不同义、逐条核过上下文才否决的**：装置（本仓库 32 处多是「艺术装置」）／
       物件（13 处是实体物件）／呼叫（混着「紧急呼叫按钮」）／图示（＝插画，不是 icon）／
       讯息／宣告（内地本来就用）／堆叠（内地营销说「叠加」）。
     · **另设 `VOCAB_KEEP` 例外短语**：「公司档案／机构档案」是**卷宗**义，内地也用「文件」——
       跑「文件→文件」前先给它们上哨兵，跑完还原，免得变成「公司文件」。
  5. **词汇层保留的已知项**：`档` 本身不换（「标准档／速览档／档位／档期」在内地也是对的，
     那是「档位」不是「文件」）；`净利`（内地通用简称）、`客诉`、`社群`、`曝光`、`促销`、
     `盘点`、`落实`、`优化` 都保留 —— 它们不是港台标记。<!-- lang-keep-trad -->
  6. 同步修正所有**语言纪律类规则文字**（那不是转换，是改规则）：README §七点五、AGENTS §六、
     SKILL §三、`references/01`／`03`／`08`、`agent_brief.py` 的 CONVENTIONS、`impact.py` 的 RULES。
     ⛔ **「交付物必须简体」这条没有放松** —— `selfcheck`【9】照旧判硬错误，
     它现在的意义变成「**挡住外部粘贴进来的繁体**」，比以前更需要。
  7. ⚠️ **工具自身的非幂等 bug（写进去，因为这一类只有「反复执行」才会现形）**：
     `lang_unify.py` 第一版把正文改烂了，**只跑一遍时看着全对**。两类：
     · **自我触发**：`厄瓜多` → `厄瓜多尔`，而结果里**仍含** `厄瓜多` → 每跑一遍多一个「尔」。  <!-- lang-keep-trad -->
       修法：键写断言 `厄瓜多(?!尔)`（国名类同：尼日／哈萨克／乌兹别克）。  <!-- lang-keep-trad -->
     · **撞上更大的词**：`产品品质`（本来就对）里的 `品质` 被换 → `产质量量`（实测 14 处）。  <!-- lang-keep-trad -->
       修法：`品质(?!量)`。同类还有 `档案管理／档案室／档案馆`（内地也用「档案」）→ 进 `VOCAB_KEEP`。  <!-- lang-keep-trad -->
     → **对策不是「记得小心」，是加一关**：`apply_vocab(apply_vocab(x)) == apply_vocab(x)` 复查，
       不幂等就**拒绝写入并 exit 2**（跑坏了比不跑更糟）。合成探针抓「自我触发」，
       真实文本复查抓「撞上更大的词」——
       **这正是本仓库反复记的那条：非幂等 bug 只在反复执行时现形，单跑一次必然漏。**
