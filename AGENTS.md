# AGENTS.md · 跨工具接入說明

> **這個資料夾是一套「營銷方案生成器」，純 Markdown 寫成，不依賴任何特定 AI 工具的語法。**
> 任何能讀檔案、或能接受貼上的 Agent 都能用：ChatGPT／Claude／Claude Code／豆包／Cursor／Windsurf／Codex 類工具／本地 Agent／其他。

---

## 一、這是什麼（給 Agent 看的一句話）

商業對接後，把客戶情況餵進來 → 產出一份完整的 Word 營銷方案（現狀分析 + 策劃方案 + 預算 + 行動清單 + 風險與假設）。
**核心是「生成方案」，不是「查資料」。**

**先讀 `SKILL.md`**，它是唯一入口，裡面有完整工作流。

> ⛔ **開新會話第一步**：跑 `python scripts/start_here.py --client "客戶情況一句話"`。
> 倉庫已 263 萬字，**通讀是最大的浪費** —— 它會告訴你「該讀哪幾份、讀哪一節」與「不要讀什麼」；
> 想查某個詞就用 `--grep`，**用「查」代替「讀」**。沒有代碼執行能力時，手動只讀 SKILL.md §0／§二／§六 三節。

---

## 二、先記住這一節：強制執行協議 + 門禁

### 2.1 門禁（第一道硬規則）
**第一次使用時，必須先把客戶情況問清楚（13 項），沒問清不准出方案、不准往下走、不准用「一般情況」代替。**
- 13 項清單見 `SKILL.md` 第 0 步（含**第 13 項：目標字數** —— 沒問就預設 5,000–10,000 字）
- 問完後**必須產出《任務規則表》請用戶確認**，確認前不得產出任何方案內容
- 唯一例外：用戶明說「先按假設出一版」—— 此時文檔首頁必須列出假設清單

### 2.1.5 ⛔ 最高優先級條款（P0 · **優先於下面八條，也優先於 SKILL 的一切默認結構**）

> 由 2026-09-17 一次真實返工事故反推（校園大賽項目書 1.5 萬字**重做三次**）。完整協議（6 條鐵律 ＋ 交付前自檢 8 項 ＋ 4 條結構性根因）見 `references/11-防返工交付协议.md`。
> **根因不是模型偷懶，是「默認以自己的結構生成全文」凌駕了「用戶給定的結構」。**

- **P0-1 · 用戶給定的結構 > 本 skill 的一切默認結構與默認流程** —— 模板／大綱／目錄／提交清單在場時，一級二級標題**逐字沿用**（不替換／不合并／不拆分／不重排／不自造）；模板沒有的內容作為**新增同級／下級章節**補進去。模板寫「僅供參考」也默認原樣保留。
- **P0-2 · 用戶給的素材要「看得見地插進去」，不能溶解** —— 用戶大綱必須作為**文檔中可見章節**出現，保留原名與編號（如「一、活動背景」）；⛔ 禁止打散揉進別的小節。
- **P0-3 · 「skill 自檢通過」≠「交付達成」** —— 自檢單（協議 3）只代表滿足 skill 默認標準；**用戶自定義的驗收項（主辦方格式／字數／匿名／雙格式／附件）必須另行逐條核對並原樣輸出**。
- **P0-4 · 形狀類任務先交 1 頁目錄，確認後再寫正文**（唯一例外：用戶明說「直接給完整版」）；一句話含**兩個約束**時 → 動手前**分別複述**兩個約束。

### 2.2 八條強制執行協議（**跨平台都適用，違反任何一條＝本次使用失敗**）

| # | 協議 | 要點 |
|---|---|---|
| 1 | **啟動即立規** | 門禁一次問全 → 產出《任務規則表》→ 用戶確認。**用戶此後每次糾偏＝規則表升級**，先復述再動手，禁止當一次性修補 |
| 2 | **結構衝突「合併，不取捨」** | 用戶要求的結構（比賽清單／客戶模板）與八篇不一致時，**把八篇映射進去，硬要素一件不丟**；沒有對應位的放附件。**禁止為貼合格式砍深度** |
| 3 | **交付前自檢單必須原樣輸出** | 宣告完成前，在回覆中逐項輸出 12 項自檢單並標 ✅/❌；任何一項 ❌ → 回去改，不得聲稱完成 |
| 4 | **工具盤點先於路徑** | 要讀外部資料，**第一步先盤點已連接的連接器／MCP／API**。連接器可達就**禁止用爬蟲硬繞**（燒額度主因）。涉及付費生成 → 先報預估消耗並徵得同意 |
| 5 | **交付形態紅線** | 對外交付一律**簡體 `.docx`**（除非另有約定）；**內部過程文檔（事實底稿／Agent 分工／裁決記錄）不得出現在交付稿中** —— 用戶要的是作品，不是工作過程 |
| 6 | **每輪輸出《流程狀態》行** | **每輪回覆第一行**必須是：`【營銷方案 · 流程狀態】門禁 13 項：x/13（缺：…）｜ 任務規則表：{未出/待確認/已確認}｜ 當前階段：{…}`。**缺這一行＝已跑偏**。這是純 Markdown 在任何平台唯一有效的「強制力」來源 |
| 7 | **跨平台一致性** | **禁止依賴平台專有能力**（工具名／專有語法／特定 hook）；能力不足按 §四 對照表降級，**降級不等於跳步**；支持 hook 的平台可選加裝（見 §九），**但不得以「沒有 hook」為由降低標準** |
| 8 | **卡死處理** | **強制不得變成卡死**。同一項校驗連續 2 次不過 → **停止重試**，把問題攤給用戶並給 2–3 個選項；腳本報錯／環境不可用 → **降級 Markdown 繼續產出**；用戶說跳過 → 允許（`--force`），但**必須聲明未校驗並列出未通過項**。**禁止**：同一錯誤重試 >2 次／因校驗不過就不交付／靜默跳過 |

> 完整條文見 `SKILL.md` 頂部「⚡ 強制執行協議」。

### 2.3 為什麼有這些條款
執行 AI 最常見的五個失敗模式：**骨架式使用**（最高頻——只抄結構不填硬要素）／跳過交付前自檢／門禁漏項＋糾偏不沉澱／工具慣性繞路／結構衝突做減法。八條協議，每一條對應堵住其中一個。
**P0 四條另堵四個結構性失敗**（`04-失败归因总库.md` 附錄模式 F–I）：用自己的結構替換用戶結構／溶解用戶素材／把自檢綠當成可交／長輸出綁架判斷。完整說明見 `references/04-失败归因总库.md` 附錄與 `references/11-防返工交付协议.md`。

---

## 三、各平台怎麼接入

### A. 有 skill／插件機制的平台
| 平台 | 放哪裡 | 怎麼觸發 |
|---|---|---|
| **Claude Code / Claude.ai** | 專案目錄 `.claude/skills/marketing-playbook/` 或使用者級 `~/.claude/skills/` | 說「出方案」等觸發語；SKILL.md 的 frontmatter（name/description）會被讀取 |
| **WorkBuddy** | 平台的 skills／插件目錄 | 直接說客戶情況即觸發 |
| **Cursor / Windsurf / ZCode 類** | 放在專案根目錄，並在專案的 `AGENTS.md` 或規則檔中寫一行「行銷方案相關任務請先讀 `marketing-playbook/SKILL.md`」 | 依工具的規則載入機制 |
| **其他支援通用 skill 規範的工具** | 依其約定放置；核心是讓 Agent 能讀到 `SKILL.md` | 同左 |

### B. 有知識庫／專案空間的平台
| 平台 | 做法 |
|---|---|
| **ChatGPT Projects** | 把 `SKILL.md` + `README.md` 傳進 Project 知識庫，自訂指令寫「處理行銷方案時，先讀 SKILL.md 並依其流程執行」 |
| **豆包／其他國產 AI** | 上傳檔案或貼上內容；若有「知識庫／智慧體」功能，同樣把 SKILL.md 放進去 |
| **飛書知識庫 / Notion** | 匯入整個資料夾，需要時讓 Agent 讀對應頁面 |

### C. 什麼都沒有（純對話環境）
1. 把 `SKILL.md` **全文貼上**，說「照這個流程幫我出方案」
2. 需要哪個參考檔再貼哪個 —— **本 skill 就是為「按需載入」設計的**，不用一次貼完
3. 常用順序：`SKILL.md` → `references/00-打法库.md` → `references/cases/{行業}.md` → `references/01-接案输出模板.md`

---

## 四、環境能力對照表（沒有某項能力時怎麼辦）

| 能力 | 有 | 沒有 |
|---|---|---|
| **讀檔案** | 按需讀 `references/` 下的檔案 | 請使用者貼上需要的檔 |
| **派出獨立 Agent** | 五個角色（策略／品牌／觸達／文案／財務風控）獨立執行，互不看草稿 | **順序扮演**：每次宣告「現在我是 X 角色」，只讀事實底稿、不回顧前序輸出；五個都寫完才裁決（詳見 `references/02-多Agent分工简报.md` §二） |
| **執行代碼** | 用 `python-docx` 生成 `.docx`（帶標題層級、表格、頁碼、目錄） | 產出結構化 Markdown ＋ **樣式對照說明**（哪一級對應標題幾、哪些是表格），讓使用者貼進 Word 一鍵套樣式 |
| **聯網檢索** | 可核查案例數據與市場事實 | 只用檔案內已有內容，並明確標註「未聯網核實」 |
| **寫入檔案** | 產出後直接落檔 | 在對話中輸出完整內容，提醒使用者自行保存 |

---

## 四之一、質檢方法論（**2026-09-17 用血換來的三條，任何平台都适用**）

背景：這個知識庫曾連續多輪「質檢全部通過」，卻仍然在交付物裡看不到知識庫的內容
（`composer.py` 一直在印「來源 cases/xx.md，請展開…」這種占位符）。原因是**質檢的量錯了東西**：

| 錯誤做法 | 為什麼必然漏 | 正確做法 |
|---|---|---|
| 只查**檔案內性質**（這檔有五節嗎／字夠嗎／標題一致嗎） | 這些性質**全部合格**也不代表知識能從「打法」走到「案例」再走到「交付物」。**零件合合格 ≠ 機器能動** | 加一層**連通性審計**：逐條鏈路問「引用走得到終點嗎」→ `scripts/kb_audit.py`（L1–L7） |
| 用**自證式指標**（上限／閾值自己定） | `case_lint --max` 由 300 調到 900、「≥2500 字」由自己定 —— **達標率當然永遠好看** | 指標要能**否證自己**：優先量「外部可驗證的事實」（引用能否解析、實跑一次產出物裡有幾條真實內容） |
| **改哪查哪**，只回歸當次改動 | 庫從 6 檔長到 51 檔，早期產物（`00-打法库` 的案例行）指向的檔早就變了，**卻從未復檢** → 斷鏈累積 | **每次改完跑全量回歸**（`kb_audit.py` 的 L6 會一次跑完既有 6 個校驗腳本） |
| **只跑一次**就宣稱沒問題 | 有一類 bug 只在「反覆執行」時現形：**非冪等**（每次跑都改一點點）。單跑一次看不出來 | **50 遍冪等壓測**：`scripts/verify_all.py` —— 第 1 輪之後每輪倉庫哈希必須不變 |
| 「引用**檔案**」就算查過 | 引用了檔 ≠ 引得到**內容**：實測 104 條打法的案例行只指向 6 個檔、`農夫山泉`／`星巴克` 在庫裡根本沒有卡片 | 量**可達性**：引用行裡**指名**的品牌，產出引擎要真的取得到（L2 的「引用行＝實取」一致性） |
| 指標的分母與被測對象不一致 | `kb_audit` 原本數「深度卡 609」，picker 操作的是「清單∪深卡 983」→ **覆蓋率算出來是假的** | 指標的**分母必須等於被測函式實際操作的那個集合** |
| 短鍵／模糊匹配**按第一個命中**就停 | ①`play_overrides` 裡一個 2 字符舊鍵 `'VI'` 把 `'VI 一致性'` 擋掉，模型永遠挑不到；②**同一個 bug 還長在影響面分析器自己身上**：`impact.py` 命中第一條規則就 `break`，於是通用規則（`scripts/*.py`）把專門規則（`scripts/repo_hygiene.py`）整個遮掉 —— 改它時算出的全是通用建議 | 模糊匹配一律**最長鍵優先**（越長＝越具體＝越該贏）。⚠️ 「具體度」不是「pattern 字元數」——是**通配符之前的字面前綴長度**（`references/*.md` 比 `references/范例/` 長，但後者才是更具體的那條）。已知三處實作：`infer_industry()`／`play_overrides`／`impact.py` 的 `specificity()` |
| 只查**內容對不對**，不查**檔案該不該存在** | 連通性審計再密也只管「引用通不通」；**孤兒檔／過期的派生物／本機垃圾**三類冗余它照不到，於是倉庫只增不減 | **倉庫衛生掃描**：`scripts/repo_hygiene.py` —— 孤兒檔（零入鏈）／派生物（可再生且會比源檔舊）／本機垃圾（`--clean` 清廢紙簍） |

**落地要求**：改完知識庫或腳本 → **必須**跑
```bash
python scripts/kb_audit.py        # 斷鏈類必須全 0；覆蓋率類只報數、不算失敗
python scripts/verify_all.py -n 50   # 全鏈路 ＋ 50 遍冪等壓測；倉庫必須零漂移
python scripts/repo_hygiene.py      # 冗余／衛生；清理時加 --clean（只動本機垃圾）
```

---

## 四點五、腳本（**強制層 —— 能跑代碼就必須用**）

### 🧭 一接案先跑：流程嚮導（`flow.py`）

```bash
python scripts/flow.py --dir 案子目錄      # 告訴你「現在第幾步、下一步跑哪條命令」
```

把 `references/08-操作流程SOP.md` 的九階段流程（S0 盤點 → S1 門禁 → … → S8 沉澱）
變成**可機械檢查的狀態機**：它看目錄裡的產物（`rules.json` / `skeleton.md` / `plan.md` / `*.docx`）
自動判斷你卡在哪一步。

### ⛔ 出稿前第一步：組裝骨架（`composer.py` —— 真正的「skill」引擎）

> **為什麼需要它**（用戶 2026-09-16 定調）：「模型根本做不了強制使用，它只做到讀取，相當於 prompt / RAG，不是 skill。」
> → 所以**知識由腳本機械注入，不靠模型自願引用**。

```bash
python scripts/composer.py --rules rules.json --out skeleton.md --tier 标准 --top 5 \
    --internal skeleton.internal.md      # ← 施工说明/缺口/自检单，不进交付稿
#  --tier  速览(=小客户/快速预览标准) | 标准(=C端品牌) | 大赛 | B端 | G端 | 投标
#          不同档位给不同骨架；现有骨架即各类型客户（含小企业/大客户）的标准
```

**六档各自一套「范式」**（骨架 ＋ 逐节填写指引 ＋ 关键句片段）：见 `references/12-范式库.md`。

> 用户原话（2026-09-17）：「每一个都需要有一个范式，例如B端的、C端的、小客户的，
> 都需要有他们各自的一个范式，**不仅只有大纲，也需要有里面的内容可以参考**。」
> → **范式 ≠ 模板**：模板只给空表；范式连「这一节写什么算合格、写几句、必须含哪几个数」
> 一起给，再附可直接改写套用的句片段。
> → `composer --internal` 会把**本档**的逐节指引一并写进内部文件（**不进交付稿**）。
> → 骨架的唯一真相在 `composer.py`，指引在 `scripts/paradigm_data.py`，
> 由 `scripts/build_paradigm.py` 拼装 —— 拼装前先做一致性校验，不一致就退出 1。

它做什麼（**確定性，非模型生成**）：
1. 解析 `references/00-打法库.md` 全部 §X.Y 打法
2. 按 `SKILL.md §二 狀況→打法 路由表`（編碼在 `scripts/knowledge_map.json`）匹配客戶狀況 → 選 3–7 條打法
3. 自動拉取每條打法的**具體動作／驗收指標／可抄案例**
4. **注入「理論依據」**：打法 → `knowledge_map.json` → `03` 模型碼 ＋ `49` 書籍
5. 按檔位輸出骨架（`速览`／`标准`／`G端`）

**產出 `skeleton.md`**：打法／理論依據／可抄案例**已注入**，模型只需補 `【填】` 處 → 交付前 `selfcheck` 第【8】關再校驗。**知識刪不掉。**

> 逐步操作手冊見 `references/08-操作流程SOP.md`。映射改動改 `scripts/knowledge_map.json`（**不要寫在文字裡——模型不會看**）。

### ⛔ 唯一出稿入口：`run_pipeline.py`

**交付物只能由 `scripts/run_pipeline.py` 產出。** 它不是「建議」，是唯一路徑：

```bash
pip install python-docx                      # 唯一依賴
python scripts/run_pipeline.py \
    --rules  rules.json \                    # 《任務規則表》（沒問過門禁就沒有它 → 直接拒絕）
    --budget budget.json \                   # 預算表（建議填）
    --plan   plan.md \                       # 方案 Markdown
    -o       方案.docx \
    --title  "客戶名 營銷方案" --date 2026-09-16
```

它會**依序**跑完下面六關，**任何一關不過就中止、不生成 `.docx`**：

| 關卡 | 腳本 | 不過的後果 |
|---|---|---|
| ① 門禁 | `gate_check.py` | 缺項 → 禁止產出方案內容（另查關鍵項「答案是否夠具體」） |
| ② 分工 | `role_check.py` | 缺角色產出／裁決記錄 → 不准出稿（SKILL：多 Agent 分工每次必走） |
| ③ 預算 | `budget_check.py` | 分項加總 ≠ 合計 → 不准交付 |
| ④ 自檢 | `selfcheck.py` | 缺章節／自檢單／內部文檔洩漏／打法組合／知識庫引用／**繁體** → 不准交付 |
| ⑤ 深度診斷 | `depth_check.py` | **只診斷、不阻攔**（報告給你看，改不改由你決定） |
| ⑥ 出稿 | `build_docx.py` | 內部再跑一次自檢，不過就拒絕生成。**支持真腳註**：正文寫 `……400 亿元[^1]，`，出處另起一行寫 `[^1]: 弗若斯特沙利文《…》2025, p.12`（定義行不進正文）—— 官方常把「引用須用腳註標明」列為硬項，而 python-docx 原生沒有腳註 API，由 `docx_footnote.py` 直接改寫 OOXML 裝配 |
| ⑦ 交付形態核對 | `delivery_check.py` | **附件／封面／文件名／佔位符**。默認**大聲報告但不阻塞**（Demo 視頻、承諾書掃描件等附件不歸本管線產，管線分不清「還沒做」和「忘了做」）；加 `--strict-delivery` 則直接判定本次不完成 |

> **為什麼要單一入口**：單獨跑這幾個腳本時，執行者很容易「忘記跑」「選擇性跑」「跑不過就手工繞過」。
> 串成一條鏈之後，**用這個 skill 就必然經過代碼** —— 想拿到 `.docx`，只能從這條鏈走。

### 後置環節：按客戶格式範本重排

客戶／比賽自帶提交格式時，**不要重寫，要映射**（協議 2：合併，不取捨）：

```bash
python scripts/reformat_to_template.py --source 方案.md --template 客戶範本.docx --dry-run   # 看建議
python scripts/reformat_to_template.py --source 方案.md --template 客戶範本.docx \
    --map 映射.json -o 方案-按範本.md --report 要素核對.md                                   # 重排
python scripts/run_pipeline.py --rules rules.json --plan 方案-按範本.md -o 方案-按範本.docx --title "…"  # 出稿
```

- 範本要、源稿沒有的章節 → 標【待補】；源稿有、範本沒位的 → **整節移入附錄**
- **硬要素核對**：丟一件就退出碼 1（不准出稿）

**也可以單獨跑某一關**（除錯用）：| 腳本 | 時機 | 不過的後果 |
|---|---|---|

| 腳本 | 時機 | 不過的後果 |
|---|---|---|
| `gate_check.py` | 門禁問完、動筆前 | 缺項 → 禁止產出方案內容 |
| `budget_check.py` | 寫完預算表 | 分項加總 ≠ 合計 → 不准交付 |
| `selfcheck.py` | 交付前 | 缺章節／缺自檢單／內部文檔洩漏 → 不准交付 |
| `build_docx.py` | 出稿 | **先跑 selfcheck，不過就拒絕生成 `.docx`** |

```bash
pip install python-docx          # 唯一依賴
python scripts/gate_check.py gate.json
python scripts/budget_check.py budget.json
python scripts/selfcheck.py plan.md
python scripts/build_docx.py plan.md -o 方案.docx --title "客戶名 營銷方案" --date 2026-09-14
```

**為什麼這是真強制**：`run_pipeline.py` 是唯一出稿路徑，而它必跑前四關 —— **拿不到 `.docx` 就等於沒完成**。
這比「靠 AI 自覺讀規則」強一個量級：規則可以假裝讀過，退出碼假裝不了。

**各腳本的輸入格式**（詳見腳本檔頭註解）：
- `gate.json`：`{"client": "...", "gate": {"賣什麼": "...", ...13 項...}, "rules_table_confirmed": true}`
- `budget.json`：`{"items": [{"name":"...","amount":3000}], "total": 15000, "unit_economics": {...}}`（也支援直接吃含預算表的 Markdown）
- `plan.md`：方案正文 Markdown（八篇結構）

**沒有代碼執行能力的平台** → 退回 Markdown 協議（手工對照清單 + 原樣輸出 12 項自檢單 + 附樣式對照說明）。**降級不等於跳步。**

---

## 五、檔案地圖

> ⚠️ **本表只是鏡像，不是導航來源。** 唯一權威導航是 **`SKILL.md` 的「第 -1 步：你要做什麼 → 只讀這幾份」** —— 那張表還寫了「**明確不要讀**什麼」，本表沒有。**兩者衝突時以 `SKILL.md` 為準。**
> 目錄級導航：`references/cases/README.md`（50 個行業檔只讀 1 個）。**每個檔開頭都有「自解釋頭」**（這是什麼／什麼時候讀／讀完你能／目錄／規模），先讀那 5 行再決定往不往下讀。

| 檔案 | 作用 | 什麼時候讀 |
|---|---|---|
| `SKILL.md` | **唯一入口**：第 -1 步導航 + 門禁 + 六步工作流 + 交付規範 + 自檢清單 | 永遠先讀 |
| `scripts/` | **腳本強制層**：**`flow.py`（流程嚮導）＋ `composer.py`（組裝引擎·知識機械注入）＋ `build_paradigm.py`（六檔範式庫生成·拼裝前先驗骨架一致性）＋ `knowledge_map.json`（映射表）＋ `paradigm_data.py`（逐節填寫指引）＋ `structure_fix.py`（結構體檢·重編號·引用同步）＋ `docx_footnote.py`（真 Word 腳註裝配）＋ `delivery_check.py`（交付形態核對）** ＋ **`run_pipeline.py`（唯一出稿入口）** ＋ `reformat_to_template.py`（按範本重排）＋ `gate_check` / `budget_check` / `selfcheck` / `depth_check` / `build_docx` | 產出交付物時**必用**（見 §四點五） |
| `references/08-操作流程SOP.md` | ⭐ **逐步操作手冊**：接案到出稿每一步的 輸入／命令／產出／校驗 | **一接案就讀** |
| `README.md` | 給人看的說明書 | 使用者想了解怎麼用時 |
| `AGENTS.md` | 本檔：跨工具接入 | 接入新平台時 |
| `references/00-打法库.md` | 104 條打法（狀況 → 打法） | **生成方案時最先讀** |
| `references/cases/01~51-{大類}.md` | **51 個大類 / 649 張深度案例卡**（01–45 行業 411 張 ＋ 46–51 機構與方法論 238 張） | 找同行參照案例時**只讀你客戶那 1 檔** |
| `references/03-方法论操作手册.md` | **111 個模型（A–M）怎麼填** | 需要用具體模型時 |
| `references/01-接案输出模板.md` | **逐章寫作指南**（目的／必含／寫法／範例／常見錯誤） | **寫方案時必讀** |
| `references/02-多Agent分工简报.md` | 五份角色簡報 + 三種環境執行方式 + 裁決規則 | 多 Agent 分工時 |
| `references/04-失败归因总库.md` | 12 個失敗模式（附錄另 5 個 AI 執行者模式）+ 34 條預演清單 | 方案風險自檢 |
| `references/05-小企业与新品牌从零打造.md` | 小企業／新品牌主入口 | 客戶是小企業或新品牌 |
| `references/cases/51-本土数字营销与MCN.md` | 平台官方模型（AIPL／FAST／5A／DEEPLINK） | 要用平台方法論時 |
| `references/06-选流派矩阵与对照表.md` | 判斷外包還是自己幹 | 要決定分工邊界時 |
| `references/07-质量范式-便利店开学季案.md` | ⭐ **質量標尺**：角色產出規格／骨架模板／可校驗指標 | 不確定「寫到什麼程度算夠」時 |
| `references/范例/` | 📄 **樣張（簡體）**：`-交付稿.md`、`-原版.md`、`-任务规则表.json`、`-预算表.json` | 想看成品／輸入文件長什麼樣時 |
| ⭐ `references/12-范式库.md` | **六檔範式（骨架＋逐節填寫指引＋關鍵句片段）**：速覽（小客戶）／標準（C 端品牌）／大賽／B 端／G 端／投標，共 169 節、3.4 萬字 | 動手填骨架前，想知道「這一節寫什麼算合格」時 |
| ~~`references/archive/`~~ | **已退役（2026-09-16）**：內容併入 `cases/46-50` | — |

---

## 六、語言約定

- **skill 內部文件**：繁體中文（`SKILL.md`／本檔／`README.md`／`references/00–09`／`cases/`）
- **交付給客戶的 Word**：**簡體中文**（硬要求，`build_docx.py` 出稿前檢查）
- **`references/范例/` 例外**：交付稿樣張是**簡體**——因為它們就是交付物本身
- 若使用者要求其他語言，以使用者當次指示為準

---

## 七、怎麼讓它越用越準

項目結束後，把新學到的東西寫回對應檔案：

| 新學到什麼 | 寫到哪 |
|---|---|
| 新打法 | `references/00-打法库.md` |
| 新案例 | `references/cases/{行業}.md` |
| 新失敗 | `references/04-失败归因总库.md` |
| 新的好問法 | `SKILL.md` §五 |
| 新模型／工具 | `references/03-方法论操作手册.md` |

**不寫進來的**：公司背景（創始人／成立年份／股權／融資／人事）、未經核實的數據、客戶隱私。

**原則**：增量續寫，不整篇覆蓋；同名迭代走升級替換，不版本堆疊。

---

## 八、驗收：一份合格的交付長什麼樣

- **格式**：`.docx`（或明確的 Word 可貼格式 + 樣式說明）
- **篇幅**：以《任務規則表》確認的**目標字數**為準（未指定時預設 **5,000–10,000 字**）。深度以「硬要素齊全」衡量 —— **不以字數注水，也不為省字砍硬要素**
- **結構**：執行摘要 + 八篇 + 附件（見 `SKILL.md` §三）
- **必含**：數據事實（有來源）、禁用詞與口徑、執行人力檢查、關鍵假設與驗證方式、待解決問題清單、不做什麼
- **不合格的訊號**：出現「做私域運營」「加強內容營銷」這類沒有動作的廢話；預算分項加總對不上；每條動作沒寫「誰做」

---

## 九、可選：在支持 hook 的平台加裝硬約束

> **前提**：協議 1–7 是**跨平台通用**的約束（靠執行者遵守 ＋ 用戶看《流程狀態》行驗證）。
> 若平台支持 hooks（**Claude Code 與 WorkBuddy 同構**），可再加一層硬約束 —— **這是可選增強，不是必需**。

### 9.1 支持情況
| 平台 | hook 支持 | 配置位置 |
|---|---|---|
| WorkBuddy | ✅（與 Claude Code 同構，27+ 事件） | `~/.workbuddy/settings.json` 的 `hooks` 字段 |
| Claude Code | ✅ | `~/.claude/settings.json` |
| ChatGPT／豆包／其他 | ❌ | 靠協議 1–7 ＋ 用戶檢查《流程狀態》行 |

### 9.2 最值得配的一個：Stop（強制輸出交付自檢單）
AI 想結束回答時，檢查是否已輸出 12 項自檢單；沒輸出就攔住、逼它補齊。

```json
{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "判斷本輪對話是否屬於「營銷方案生成」任務（關鍵詞：營銷方案/策劃/品牌推廣/出方案/比稿，或使用了 marketing-playbook skill）。\n\n若【不是】→ 返回 {\"ok\": true}\n\n若是 → 檢查最後一條回覆是否已【原樣輸出 12 項《交付自檢單》並逐項標 ✅/❌】：\n- 已輸出且無 ❌ → {\"ok\": true}\n- 已輸出但有 ❌ → {\"ok\": false, \"reason\": \"自檢單有未通過項，請先修正再交付。\"}\n- 未輸出 → {\"ok\": false, \"reason\": \"這是營銷方案任務，但還沒輸出 12 項《交付自檢單》。請原樣輸出並逐項標 ✅/❌，未通過的先修正。\"}",
            "continueOnBlock": true,
            "timeout": 30
          }
        ]
      }
    ]
  }
}
```

### 9.3 可選再加：UserPromptSubmit（開頭注入協議）
讓用戶不必每次說「先出規則表」：

```json
{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "判斷用戶這條輸入是否涉及「營銷方案/策劃/品牌推廣/接案出方案」。\n\n不是 → {\"ok\": true}\n\n是 → {\"ok\": false, \"reason\": \"【marketing-playbook 強制協議】這是營銷方案任務。請先讀該 skill 的 SKILL.md，然後：① 執行門禁，把 13 項一次問全；② 輸出《任務規則表》等用戶確認；③ 確認前不得產出任何方案內容；④ 每輪回覆以《流程狀態》行開頭；⑤ 交付前原樣輸出 12 項自檢單。\"}",
            "continueOnBlock": true,
            "timeout": 30
          }
        ]
      }
    ]
  }
}
```

### 9.4 注意
- hook 是**系統級配置**，對所有專案生效 → 判斷條件務必加「只在營銷任務時觸發」，否則會干擾其他對話。
- 每次觸發多一次 LLM 判斷（有消耗）。**建議先只配 Stop**，觀察一週再加 UserPromptSubmit。
- 改完 settings.json 後，部分平台需在 `/hooks` 面板確認變更才生效。
