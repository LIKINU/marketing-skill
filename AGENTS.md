# AGENTS.md · 跨工具接入说明

> **这个文件夹是一套「营销方案生成器」，纯 Markdown 写成，不依赖任何特定 AI 工具的语法。**
> 任何能读文件、或能接受粘贴的 Agent 都能用：ChatGPT／Claude／Claude Code／豆包／Cursor／Windsurf／Codex 类工具／本地 Agent／其他。

---

## 一、这是什么（给 Agent 看的一句话）

商业对接后，把客户情况喂进来 → 产出一份完整的 Word 营销方案（现状分析 + 策划方案 + 预算 + 行动清单 + 风险与假设）。
**核心是「生成方案」，不是「查资料」。**

**先读 `SKILL.md`**，它是唯一入口，里面有完整工作流。

> ⛔ **开新会话第一步**：跑 `python scripts/start_here.py --client "客户情况一句话"`。
> 仓库已 263 万字，**通读是最大的浪费** —— 它会告诉你「该读哪几份、读哪一节」与「不要读什么」；
> 想查某个词就用 `--grep`，**用「查」代替「读」**。没有代码执行能力时，手动只读 SKILL.md §0／§二／§六 三节。

---

## 二、先记住这一节：强制执行协议 + 门禁

### 2.1 门禁（第一道硬规则）
**第一次使用时，必须先把客户情况问清楚（13 项），没问清不准出方案、不准往下走、不准用「一般情况」代替。**
- 13 项清单见 `SKILL.md` 第 0 步（含**第 13 项：目标字数** —— 没问就预设 5,000–10,000 字）
- 问完后**必须产出《任务规则表》请用户确认**，确认前不得产出任何方案内容
- 唯一例外：用户明说「先按假设出一版」—— 此时文档首页必须列出假设清单

### 2.1.5 ⛔ 最高优先级条款（P0 · **优先于下面八条，也优先于 SKILL 的一切默认结构**）

> 由 2026-09-17 一次真实返工事故反推（校园大赛项目书 1.5 万字**重做三次**）。完整协议（6 条铁律 ＋ 交付前自检 8 项 ＋ 4 条结构性根因）见 `references/11-防返工交付协议.md`。
> **根因不是模型偷懒，是「默认以自己的结构生成全文」凌驾了「用户给定的结构」。**

- **P0-1 · 用户给定的结构 > 本 skill 的一切默认结构与默认流程** —— 模板／大纲／目录／提交清单在场时，一级二级标题**逐字沿用**（不替换／不合并／不拆分／不重排／不自造）；模板没有的内容作为**新增同级／下级章节**补进去。模板写「仅供参考」也默认原样保留。
- **P0-2 · 用户给的素材要「看得见地插进去」，不能溶解** —— 用户大纲必须作为**文档中可见章节**出现，保留原名与编号（如「一、活动背景」）；⛔ 禁止打散揉进别的小节。
- **P0-3 · 「skill 自检通过」≠「交付达成」** —— 自检单（协议 3）只代表满足 skill 默认标准；**用户自定义的验收项（主办方格式／字数／匿名／双格式／附件）必须另行逐条核对并原样输出**。
- **P0-4 · 形状类任务先交 1 页目录，确认后再写正文**（唯一例外：用户明说「直接给完整版」）；一句话含**两个约束**时 → 动手前**分别复述**两个约束。

### 2.2 八条强制执行协议（**跨平台都适用，违反任何一条＝本次使用失败**）

| # | 协议 | 要点 |
|---|---|---|
| 1 | **启动即立规** | 门禁一次问全 → 产出《任务规则表》→ 用户确认。**用户此后每次纠偏＝规则表升级**，先复述再动手，禁止当一次性修补 |
| 2 | **结构冲突「合并，不取舍」** | 用户要求的结构（比赛清单／客户模板）与八篇不一致时，**把八篇映射进去，硬要素一件不丢**；没有对应位的放附件。**禁止为贴合格式砍深度** |
| 3 | **交付前自检单必须原样输出** | 宣告完成前，在回复中逐项输出 12 项自检单并标 ✅/❌；任何一项 ❌ → 回去改，不得声称完成 |
| 4 | **工具盘点先于路径** | 要读外部资料，**第一步先盘点已连接的连接器／MCP／API**。连接器可达就**禁止用爬虫硬绕**（烧额度主因）。涉及付费生成 → 先报预估消耗并征得同意 |
| 5 | **交付形态红线** | 对外交付一律**简体 `.docx`**（除非另有约定）；**内部过程文档（事实底稿／Agent 分工／裁决记录）不得出现在交付稿中** —— 用户要的是作品，不是工作过程 |
| 6 | **每轮输出《流程状态》行** | **每轮回复第一行**必须是：`【营销方案 · 流程状态】门禁 13 项：x/13（缺：…）｜ 任务规则表：{未出/待确认/已确认}｜ 当前阶段：{…}`。**缺这一行＝已跑偏**。这是纯 Markdown 在任何平台唯一有效的「强制力」来源 |
| 7 | **跨平台一致性** | **禁止依赖平台专有能力**（工具名／专有语法／特定 hook）；能力不足按 §四 对照表降级，**降级不等于跳步**；支持 hook 的平台可选加装（见 §九），**但不得以「没有 hook」为由降低标准** |
| 8 | **卡死处理** | **强制不得变成卡死**。同一项校验连续 2 次不过 → **停止重试**，把问题摊给用户并给 2–3 个选项；脚本报错／环境不可用 → **降级 Markdown 继续产出**；用户说跳过 → 允许（`--force`），但**必须声明未校验并列出未通过项**。**禁止**：同一错误重试 >2 次／因校验不过就不交付／静默跳过 |

> 完整条文见 `SKILL.md` 顶部「⚡ 强制执行协议」。

### 2.3 为什么有这些条款
执行 AI 最常见的五个失败模式：**骨架式使用**（最高频——只抄结构不填硬要素）／跳过交付前自检／门禁漏项＋纠偏不沉淀／工具惯性绕路／结构冲突做减法。八条协议，每一条对应堵住其中一个。
**P0 四条另堵四个结构性失败**（`04-失败归因总库.md` 附录模式 F–I）：用自己的结构替换用户结构／溶解用户素材／把自检绿当成可交／长输出绑架判断。完整说明见 `references/04-失败归因总库.md` 附录与 `references/11-防返工交付协议.md`。

---

## 三、各平台怎么接入

### A. 有 skill／插件机制的平台
| 平台 | 放哪里 | 怎么触发 |
|---|---|---|
| **Claude Code / Claude.ai** | 专案目录 `.claude/skills/marketing-playbook/` 或用户级 `~/.claude/skills/` | 说「出方案」等触发语；SKILL.md 的 frontmatter（name/description）会被读取 |
| **WorkBuddy** | 平台的 skills／插件目录 | 直接说客户情况即触发 |
| **Cursor / Windsurf / ZCode 类** | 放在专案根目录，并在专案的 `AGENTS.md` 或规则档中写一行「营销方案相关任务请先读 `marketing-playbook/SKILL.md`」 | 依工具的规则加载机制 |
| **其他支持通用 skill 规范的工具** | 依其约定放置；核心是让 Agent 能读到 `SKILL.md` | 同左 |

### B. 有知识库／专案空间的平台
| 平台 | 做法 |
|---|---|
| **ChatGPT Projects** | 把 `SKILL.md` + `README.md` 传进 Project 知识库，自订指令写「处理营销方案时，先读 SKILL.md 并依其流程执行」 |
| **豆包／其他国产 AI** | 上传文件或粘贴内容；若有「知识库／智慧体」功能，同样把 SKILL.md 放进去 |
| **飞书知识库 / Notion** | 导入整个文件夹，需要时让 Agent 读对应页面 |

### C. 什么都没有（纯对话环境）
1. 把 `SKILL.md` **全文粘贴**，说「照这个流程帮我出方案」
2. 需要哪个参考档再贴哪个 —— **本 skill 就是为「按需加载」设计的**，不用一次贴完
3. 常用顺序：`SKILL.md` → `references/00-打法库.md` → `references/cases/{行业}.md` → `references/01-接案输出模板.md`

---

## 四、环境能力对照表（没有某项能力时怎么办）

| 能力 | 有 | 没有 |
|---|---|---|
| **读文件** | 按需读 `references/` 下的文件 | 请用户粘贴需要的档 |
| **派出独立 Agent** | 五个角色（策略／品牌／触达／文案／财务风控）独立执行，互不看草稿 | **顺序扮演**：每次宣告「现在我是 X 角色」，只读事实底稿、不回顾前序输出；五个都写完才裁决（详见 `references/02-多Agent分工简报.md` §二） |
| **执行代码** | 用 `python-docx` 生成 `.docx`（带标题层级、表格、页码、目录） | 产出结构化 Markdown ＋ **样式对照说明**（哪一级对应标题几、哪些是表格），让用户贴进 Word 一键套样式 |
| **联网检索** | 可核查案例数据与市场事实 | 只用文件内已有内容，并明确标注「未联网核实」 |
| **写入文件** | 产出后直接落档 | 在对话中输出完整内容，提醒用户自行保存 |

---

## 四之一、质检方法论（**用血换来的九条，任何平台都适用**）

背景：这个知识库曾连续多轮「质检全部通过」，却仍然在交付物里看不到知识库的内容
（`composer.py` 一直在印「来源 cases/xx.md，请展开…」这种占位符）。原因是**质检的量错了东西**：

| 错误做法 | 为什么必然漏 | 正确做法 |
|---|---|---|
| 只查**文件内性质**（这档有五节吗／字够吗／标题一致吗） | 这些性质**全部合格**也不代表知识能从「打法」走到「案例」再走到「交付物」。**零件合合格 ≠ 机器能动** | 加一层**连通性审计**：逐条链路问「引用走得到终点吗」→ `scripts/kb_audit.py`（L1–L7） |
| 用**自证式指标**（上限／阈值自己定） | `case_lint --max` 由 300 调到 900、「≥2500 字」由自己定 —— **达标率当然永远好看** | 指标要能**否证自己**：优先量「外部可验证的事实」（引用能否解析、实跑一次产出物里有几条真实内容） |
| **改哪查哪**，只回归当次改动 | 库从 6 档长到 51 档，早期产物（`00-打法库` 的案例行）指向的档早就变了，**却从未复检** → 断链累积 | **每次改完跑全量回归**（`kb_audit.py` 的 L6 会一次跑完既有 6 个校验脚本） |
| **只跑一次**就宣称没问题 | 有一类 bug 只在「反复执行」时现形：**非幂等**（每次跑都改一点点）。单跑一次看不出来 | **50 遍幂等压测**：`scripts/verify_all.py` —— 第 1 轮之后每轮仓库哈希必须不变 |
| 「引用**文件**」就算查过 | 引用了档 ≠ 引得到**内容**：实测 104 条打法的案例行只指向 6 个文件、`农夫山泉`／`星巴克` 在库里根本没有卡片 | 量**可达性**：引用行里**指名**的品牌，产出引擎要真的取得到（L2 的「引用行＝实取」一致性） |
| 指标的分母与被测对象不一致 | `kb_audit` 原本数「深度卡 609」，picker 操作的是「清单∪深卡 983」→ **覆盖率算出来是假的** | 指标的**分母必须等于被测函数实际操作的那个集合** |
| 短键／模糊匹配**按第一个命中**就停 | ①`play_overrides` 里一个 2 字符旧键 `'VI'` 把 `'VI 一致性'` 挡掉，模型永远挑不到；②**同一个 bug 还长在影响面分析器自己身上**：`impact.py` 命中第一条规则就 `break`，于是通用规则（`scripts/*.py`）把专门规则（`scripts/repo_hygiene.py`）整个遮掉 —— 改它时算出的全是通用建议 | 模糊匹配一律**最长键优先**（越长＝越具体＝越该赢）。⚠️ 「具体度」不是「pattern 字符数」——是**通配符之前的字面前缀长度**（`references/*.md` 比 `references/范例/` 长，但后者才是更具体的那条）。已知三处实作：`infer_industry()`／`play_overrides`／`impact.py` 的 `specificity()` |
| 只查**内容对不对**，不查**文件该不该存在** | 连通性审计再密也只管「引用通不通」；**孤儿档／过期的派生物／本机垃圾**三类冗余它照不到，于是仓库只增不减 | **仓库卫生扫描**：`scripts/repo_hygiene.py` —— 孤儿档（零入链）／派生物（可再生且会比源档旧）／本机垃圾（`--clean` 清废纸篓） |
| **把「判据文件里写着」当成「判据会跑」** | 2026-09-19 复测时发现**三条判据从来没执行过**：`selfcheck` 的 Red Team（`### 8.7`）／主动放弃（`### 1.5`）／渠道形态（`### 5.1`）。真因：节提取用的 `_secs` **只切 `^## `（二级标题）**，而骨架 88 个标题里 **64 个是三级** → 按标题名找不到 → `if _sec:` 不成立 → **整段静默跳过**（其中一条连 `else` 都没有，连「没找到」都不说） | ① 按标题名取节一律走 `^#{2,3}`（`_sec_any`／`_secs_all`）；② **多目标必须取「所有命中」**，不能取第一个（1.3 排在 1.5 前 → 1.5 里的内容根本没被看）；③ **`else` 分支必须报出来**，禁止静默跳过；④ **判据的死活只有一种证明方式：删掉它要查的东西，看它会不会叫** |
| 骨架里**承诺了检查**，判据却没兑现 | `8.7` 的骨架里写着「三条必须互不相同（两两相似会被判复读）」，而判据只数条数与数字 → 填稿的人**以为有人把关**。同一个病还有「骨架给了 10 列、只硬校验 2 列」 | 新增章节时**承诺与判据同时长出来**；发现「说了会被检查却没人检查」的句子，**要么补判据、要么删掉那句话** |

**落地要求**：改完知识库或脚本 → **必须**跑
```bash
python scripts/kb_audit.py        # 断链类必须全 0；覆盖率类只报数、不算失败
python scripts/verify_all.py -n 50   # 全链路 ＋ 50 遍幂等压测；仓库必须零漂移
python scripts/repo_hygiene.py      # 冗余／卫生；清理时加 --clean（只动本机垃圾）
python scripts/lang_unify.py        # 还有繁体残留？判据类文件不要动（见 README §七点五）
```

---

## 四点五、脚本（**强制层 —— 能跑代码就必须用**）

### 🧭 一接案先跑：流程向导（`flow.py`）

```bash
python scripts/flow.py --dir 案子目录      # 告诉你「现在第几步、下一步跑哪条命令」
```

把 `references/08-操作流程SOP.md` 的九阶段流程（S0 盘点 → S1 门禁 → … → S8 沉淀）
变成**可机械检查的状态机**：它看目录里的产物（`rules.json` / `skeleton.md` / `plan.md` / `*.docx`）
自动判断你卡在哪一步。

### ⛔ 出稿前第一步：组装骨架（`composer.py` —— 真正的「skill」引擎）

> **为什么需要它**（用户 2026-09-16 定调）：「模型根本做不了强制使用，它只做到读取，相当于 prompt / RAG，不是 skill。」
> → 所以**知识由脚本机械注入，不靠模型自愿引用**。

```bash
python scripts/composer.py --rules rules.json --out skeleton.md --tier 标准 --top 5 \
    --internal skeleton.internal.md      # ← 施工说明/缺口/自检单，不进交付稿
#  --tier  速览(=小客户/快速预览标准) | 标准(=C端品牌) | 大赛 | B端 | G端 | 投标
#          不同档位给不同骨架；现有骨架即各类型客户（含小企业/大客户）的标准
```

**六档各自一套「范式」**（骨架 ＋ 逐节填写指引 ＋ 关键句片段）：见 `references/12-范式库.md`。

> 用户原话（2026-09-17）：「每一个都需要有一个范式，例如B端的、C端的、小客户的，
> 都需要有他们各自的一个范式，**不仅只有大纲，也需要有里面的内容可以参考**。」
> → **范式 ≠ 模板**：模板只给空表；范式连「这一节写什么算合格、写几句、必须含哪几个数」
> 一起给，再附可直接改写套用的句片段。
> → `composer --internal` 会把**本档**的逐节指引一并写进内部文件（**不进交付稿**）。
> → 骨架的唯一真相在 `composer.py`，指引在 `scripts/paradigm_data.py`，
> 由 `scripts/build_paradigm.py` 拼装 —— 拼装前先做一致性校验，不一致就退出 1。

它做什么（**确定性，非模型生成**）：
1. 解析 `references/00-打法库.md` 全部 §X.Y 打法
2. 按 `SKILL.md §二 状况→打法 路由表`（编码在 `scripts/knowledge_map.json`）匹配客户状况 → 选 3–7 条打法
3. 自动拉取每条打法的**具体动作／验收指标／可抄案例**
4. **注入「理论依据」**：打法 → `knowledge_map.json` → `03` 模型码 ＋ `49` 书籍
5. 按档位输出骨架（`速览`／`标准`／`G端`）

**产出 `skeleton.md`**：打法／理论依据／可抄案例**已注入**，模型只需补 `【填】` 处 → 交付前 `selfcheck` 第【8】关再校验。**知识删不掉。**

> 逐步操作手册见 `references/08-操作流程SOP.md`。映射改动改 `scripts/knowledge_map.json`（**不要写在文字里——模型不会看**）。

### ⛔ 唯一出稿入口：`run_pipeline.py`

**交付物只能由 `scripts/run_pipeline.py` 产出。** 它不是「建议」，是唯一路径：

```bash
pip install python-docx                      # 唯一依赖
python scripts/run_pipeline.py \
    --rules  rules.json \                    # 《任务规则表》（没问过门禁就没有它 → 直接拒绝）
    --budget budget.json \                   # 预算表（建议填）
    --plan   plan.md \                       # 方案 Markdown
    -o       方案.docx \
    --title  "客户名 营销方案" --date 2026-09-16
```

它会**依序**跑完下面六关，**任何一关不过就中止、不生成 `.docx`**：

| 关卡 | 脚本 | 不过的后果 |
|---|---|---|
| ① 门禁 | `gate_check.py` | 缺项 → 禁止产出方案内容（另查关键项「答案是否够具体」）；规则表带 `governance` 块时一并严格校验（变更审批人／答复时限／评审节奏／范围边界／交付物清单／争议升级），缺项即拦；不带只提醒 |
| ② 分工 | `role_check.py` | 缺角色产出／裁决记录 → 不准出稿（SKILL：多 Agent 分工每次必走） |
| ③ 预算 | `budget_check.py` | 分项加总 ≠ 合计 → 不准交付 |
| ④ 自检 | `selfcheck.py` | 缺章节／自检单／内部文档泄漏／打法组合／知识库引用／**繁体** → 不准交付 |
| ⑤ 深度诊断 | `depth_check.py` | **只诊断、不阻拦**（报告给你看，改不改由你决定） |
| ⑥ 出稿 | `build_docx.py` | 内部再跑一次自检，不过就拒绝生成。**支持真脚注**：正文写 `……400 亿元[^1]，`，出处另起一行写 `[^1]: 弗若斯特沙利文《…》2025, p.12`（定义行不进正文）—— 官方常把「引用须用脚注标明」列为硬项，而 python-docx 原生没有脚注 API，由 `docx_footnote.py` 直接改写 OOXML 装配 |
| ⑦ 交付形态核对 | `delivery_check.py` | **附件／封面／文件名／占位符**。默认**大声报告但不阻塞**（Demo 视频、承诺书扫描件等附件不归本管线产，管线分不清「还没做」和「忘了做」）；加 `--strict-delivery` 则直接判定本次不完成 |

> **为什么要单一入口**：单独跑这几个脚本时，执行者很容易「忘记跑」「选择性跑」「跑不过就手工绕过」。
> 串成一条链之后，**用这个 skill 就必然经过代码** —— 想拿到 `.docx`，只能从这条链走。

### 后置环节：按客户格式范本重排

客户／比赛自带提交格式时，**不要重写，要映射**（协议 2：合并，不取舍）：

```bash
python scripts/reformat_to_template.py --source 方案.md --template 客户范本.docx --dry-run   # 看建议
python scripts/reformat_to_template.py --source 方案.md --template 客户范本.docx \
    --map 映射.json -o 方案-按范本.md --report 要素核对.md                                   # 重排
python scripts/run_pipeline.py --rules rules.json --plan 方案-按范本.md -o 方案-按范本.docx --title "…"  # 出稿
```

- 范本要、源稿没有的章节 → 标【待补】；源稿有、范本没位的 → **整节移入附录**
- **硬要素核对**：丢一件就退出码 1（不准出稿）

**也可以单独跑某一关**（调试用）：| 脚本 | 时机 | 不过的后果 |
|---|---|---|

| 脚本 | 时机 | 不过的后果 |
|---|---|---|
| `gate_check.py` | 门禁问完、动笔前 | 缺项 → 禁止产出方案内容 |
| `budget_check.py` | 写完预算表 | 分项加总 ≠ 合计 → 不准交付 |
| `selfcheck.py` | 交付前 | 缺章节／缺自检单／内部文档泄漏 → 不准交付 |
| `build_docx.py` | 出稿 | **先跑 selfcheck，不过就拒绝生成 `.docx`** |

```bash
pip install python-docx          # 唯一依赖
python scripts/gate_check.py gate.json
python scripts/budget_check.py budget.json
python scripts/selfcheck.py plan.md
python scripts/build_docx.py plan.md -o 方案.docx --title "客户名 营销方案" --date 2026-09-14
```

**为什么这是真强制**：`run_pipeline.py` 是唯一出稿路径，而它必跑前四关 —— **拿不到 `.docx` 就等于没完成**。
这比「靠 AI 自觉读规则」强一个量级：规则可以假装读过，退出码假装不了。

**各脚本的输入格式**（详见脚本文件头注解）：
- `gate.json`：`{"client": "...", "gate": {"卖什么": "...", ...13 项...}, "rules_table_confirmed": true}`
- `budget.json`：`{"items": [{"name":"...","amount":3000}], "total": 15000, "unit_economics": {...}}`（也支持直接吃含预算表的 Markdown）
- `plan.md`：方案正文 Markdown（八篇结构）

**没有代码执行能力的平台** → 退回 Markdown 协议（手工对照清单 + 原样输出 12 项自检单 + 附样式对照说明）。**降级不等于跳步。**

---

## 五、文件地图

> ⚠️ **本表只是镜像，不是导航来源。** 唯一权威导航是 **`SKILL.md` 的「第 -1 步：你要做什么 → 只读这几份」** —— 那张表还写了「**明确不要读**什么」，本表没有。**两者冲突时以 `SKILL.md` 为准。**
> 目录级导航：`references/cases/README.md`（50 个行业档只读 1 个）。**每个文件开头都有「自解释头」**（这是什么／什么时候读／读完你能／目录／规模），先读那 5 行再决定往不往下读。

| 文件 | 作用 | 什么时候读 |
|---|---|---|
| `SKILL.md` | **唯一入口**：第 -1 步导航 + 门禁 + 六步工作流 + 交付规范 + 自检清单 | 永远先读 |
| `scripts/` | **脚本强制层**：**`flow.py`（流程向导）＋ `composer.py`（组装引擎·知识机械注入）＋ `build_paradigm.py`（六档范式库生成·拼装前先验骨架一致性）＋ `knowledge_map.json`（映射表）＋ `paradigm_data.py`（逐节填写指引）＋ `structure_fix.py`（结构体检·重编号·引用同步）＋ `docx_footnote.py`（真 Word 脚注装配）＋ `delivery_check.py`（交付形态核对）** ＋ **`run_pipeline.py`（唯一出稿入口）** ＋ `reformat_to_template.py`（按范本重排）＋ `gate_check` / `budget_check` / `selfcheck` / `depth_check` / `build_docx` | 产出交付物时**必用**（见 §四点五） |
| `references/08-操作流程SOP.md` | ⭐ **逐步操作手册**：接案到出稿每一步的 输入／命令／产出／校验 | **一接案就读** |
| `README.md` | 给人看的说明书 | 用户想了解怎么用时 |
| `AGENTS.md` | 本档：跨工具接入 | 接入新平台时 |
| `references/00-打法库.md` | 104 条打法（状况 → 打法） | **生成方案时最先读** |
| `references/cases/01~51-{大类}.md` | **51 个大类 / 649 张深度案例卡**（01–45 行业 411 张 ＋ 46–51 机构与方法论 238 张） | 找同行参照案例时**只读你客户那 1 档** |
| `references/03-方法论操作手册.md` | **111 个模型（A–M）怎么填** | 需要用具体模型时 |
| `references/01-接案输出模板.md` | **逐章写作指南**（目的／必含／写法／范例／常见错误） | **写方案时必读** |
| `references/02-多Agent分工简报.md` | 五份角色简报 + 三种环境执行方式 + 裁决规则 | 多 Agent 分工时 |
| `references/04-失败归因总库.md` | 12 个失败模式（附录另 5 个 AI 执行者模式）+ 34 条预演清单 | 方案风险自检 |
| `references/05-小企业与新品牌从零打造.md` | 小企业／新品牌主入口 | 客户是小企业或新品牌 |
| `references/cases/51-本土数字营销与MCN.md` | 平台官方模型（AIPL／FAST／5A／DEEPLINK） | 要用平台方法论时 |
| `references/06-选流派矩阵与对照表.md` | 判断外包还是自己干 | 要决定分工边界时 |
| `references/07-质量范式-便利店开学季案.md` | ⭐ **质量标尺**：角色产出规格／骨架模板／可校验指标 | 不确定「写到什么程度算够」时 |
| `references/范例/` | 📄 **样张（简体）**：`-交付稿.md`、`-原版.md`、`-任务规则表.json`、`-预算表.json` | 想看成品／输入文件长什么样时 |
| ⭐ `references/12-范式库.md` | **六档范式（骨架＋逐节填写指引＋关键句片段）**：速览（小客户）／标准（C 端品牌）／大赛／B 端／G 端／投标，共 169 节、3.4 万字 | 动手填骨架前，想知道「这一节写什么算合格」时 |
| ~~`references/archive/`~~ | **已退役（2026-09-16）**：内容并入 `cases/46-50` | — |

---

## 六、语言约定

- **全仓库统一简体中文**：`SKILL.md`／本档／`README.md`／`references/`／`cases/`／脚本注释，一律简体。
- **交付给客户的 Word**：**简体中文**（硬门槛，`selfcheck`【9】判硬错误，`build_docx.py` 出稿前再喊一次）。
  统一语言后这条**没有放松** —— 它的意义变成「**挡住外部粘贴进来的繁体**」。
- **两个地方必须保留繁体**（不要顺手统一）：`scripts/t2s_data.py` 的 `T2S_PAIRS`（繁体字表是
  侦测繁体交付稿的判据）、`scripts/smoke_test.py` 的繁体负向夹具（转掉＝测试静默失效）。
- 若用户要求其他语言，以用户当次指示为准

---

## 七、怎么让它越用越准

项目结束后，把新学到的东西写回对应文件：

| 新学到什么 | 写到哪 |
|---|---|
| 新打法 | `references/00-打法库.md` |
| 新案例 | `references/cases/{行业}.md` |
| 新失败 | `references/04-失败归因总库.md` |
| 新的好问法 | `SKILL.md` §五 |
| 新模型／工具 | `references/03-方法论操作手册.md` |

**不写进来的**：公司背景（创始人／成立年份／股权／融资／人事）、未经核实的数据、客户隐私。

**原则**：增量续写，不整篇覆盖；同名迭代走升级替换，不版本堆叠。

---

## 八、验收：一份合格的交付长什么样

- **格式**：`.docx`（或明确的 Word 可贴格式 + 样式说明）
- **篇幅**：以《任务规则表》确认的**目标字数**为准（未指定时预设 **5,000–10,000 字**）。深度以「硬要素齐全」衡量 —— **不以字数注水，也不为省字砍硬要素**
- **结构**：执行摘要 + 八篇 + 附件（见 `SKILL.md` §三）
- **必含**：数据事实（有来源）、禁用词与口径、执行人力检查、关键假设与验证方式、待解决问题清单、不做什么
- **不合格的信号**：出现「做私域运营」「加强内容营销」这类没有动作的废话；预算分项加总对不上；每条动作没写「谁做」

---

## 九、可选：在支持 hook 的平台加装硬约束

> **前提**：协议 1–7 是**跨平台通用**的约束（靠执行者遵守 ＋ 用户看《流程状态》行验证）。
> 若平台支持 hooks（**Claude Code 与 WorkBuddy 同构**），可再加一层硬约束 —— **这是可选增强，不是必需**。

### 9.1 支持情况
| 平台 | hook 支持 | 配置位置 |
|---|---|---|
| WorkBuddy | ✅（与 Claude Code 同构，27+ 事件） | `~/.workbuddy/settings.json` 的 `hooks` 字段 |
| Claude Code | ✅ | `~/.claude/settings.json` |
| ChatGPT／豆包／其他 | ❌ | 靠协议 1–7 ＋ 用户检查《流程状态》行 |

### 9.2 最值得配的一个：Stop（强制输出交付自检单）
AI 想结束回答时，检查是否已输出 12 项自检单；没输出就拦住、逼它补齐。

```json
{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "判断本轮对话是否属于「营销方案生成」任务（关键词：营销方案/策划/品牌推广/出方案/比稿，或使用了 marketing-playbook skill）。\n\n若【不是】→ 返回 {\"ok\": true}\n\n若是 → 检查最后一条回复是否已【原样输出 12 项《交付自检单》并逐项标 ✅/❌】：\n- 已输出且无 ❌ → {\"ok\": true}\n- 已输出但有 ❌ → {\"ok\": false, \"reason\": \"自检单有未通过项，请先修正再交付。\"}\n- 未输出 → {\"ok\": false, \"reason\": \"这是营销方案任务，但还没输出 12 项《交付自检单》。请原样输出并逐项标 ✅/❌，未通过的先修正。\"}",
            "continueOnBlock": true,
            "timeout": 30
          }
        ]
      }
    ]
  }
}
```

### 9.3 可选再加：UserPromptSubmit（开头注入协议）
让用户不必每次说「先出规则表」：

```json
{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "判断用户这条输入是否涉及「营销方案/策划/品牌推广/接案出方案」。\n\n不是 → {\"ok\": true}\n\n是 → {\"ok\": false, \"reason\": \"【marketing-playbook 强制协议】这是营销方案任务。请先读该 skill 的 SKILL.md，然后：① 执行门禁，把 13 项一次问全；② 输出《任务规则表》等用户确认；③ 确认前不得产出任何方案内容；④ 每轮回复以《流程状态》行开头；⑤ 交付前原样输出 12 项自检单。\"}",
            "continueOnBlock": true,
            "timeout": 30
          }
        ]
      }
    ]
  }
}
```

### 9.4 注意
- hook 是**系统级配置**，对所有专案生效 → 判断条件务必加「只在营销任务时触发」，否则会干扰其他对话。
- 每次触发多一次 LLM 判断（有消耗）。**建议先只配 Stop**，观察一周再加 UserPromptSubmit。
- 改完 settings.json 后，部分平台需在 `/hooks` 面板确认变更才生效。
