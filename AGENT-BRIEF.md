# AGENT-BRIEF · 給 agent 的單檔快照

> **為什麼有這份**：每個 agent 都是全新上下文。沒有這份，它會把整個倉庫重讀一遍 ——派十幾個 agent 就重讀十幾遍。
> **怎麼用**：派 agent 時**只給這一份** ＋ **1–2 個目標檔的「那一節」**；需要核對證據時才打開原檔，且**只開對應那一段，不要通讀**。

> 本檔由 `scripts/agent_brief.py` 生成並掛在 `verify_all` 的冪等壓測裡 —— 改架構忘了更新它會被哈希漂移抓出來。


## 一、這是什麼（30 秒）

**營銷方案生成器**：輸入客戶情況 → 輸出一份可直接遞給客戶的 Word 營銷方案（現狀分析＋策劃方案＋預算＋行動清單＋風險）。
**核心設計**：知識由**腳本機械注入**骨架，模型只填 `【填】` —— **知識繞不過去**。
**六檔客戶**：速覽（小客戶）／标准（C端品牌）／大赛／B端／G端／投标。


## 二、現在有多大（自動統計）

| 項 | 數量 | 體量 |
|---|---|---|
| `references/` 編號文檔 | 14 份 | 849字 |
| `references/cases/` 行業案例 | 52 份 | 3450字 |
| `scripts/` | 42 支（36 支有 CLI） | — |
| 範式庫 | 6 檔 | 見 `references/12-范式库.md` |
| **合計** | — | **4299字** |

→ **通讀一遍＝燒掉大量上下文。用「查」代替「讀」**：`python scripts/start_here.py --grep "關鍵詞"`。


## 三、目錄結構

```
SKILL.md        唯一入口（§0 門禁 13 項 / §二路由表 / §六自檢清單）
AGENTS.md       跨工具接入說明 + 能力不足時怎麼降級
README.md       給人看的說明 + §零 倉庫地圖
AGENT-BRIEF.md  ← 本檔（給 agent 的快照）
references/     知識庫：00–13 編號文檔 + cases/（52 行業）+ 范例/
scripts/        強制層：知識注入 + 校驗 + 出稿
优化轮次/       過程記錄（不是交付物）
```


## 四、腳本分工（按「什麼時候跑」）

- **① 接案**：`flow.py` · `gate_check.py` · `start_here.py`
- **② 組裝**：`composer.py` · `build_paradigm.py` · `paradigm_data.py`
- **③ 校驗**：`selfcheck.py` · `budget_check.py` · `depth_check.py` · `role_check.py` · `structure_fix.py` · `docx_footnote.py` · `delivery_check.py`
- **④ 出稿**：`run_pipeline.py` · `build_docx.py` · `reformat_to_template.py`
- **⑤ 品質保障**：`verify_all.py` · `kb_audit.py` · `promise_check.py` · `optimize_scan.py` · `smoke_test.py` · `t2s_data.py`
- **⑥ 案例庫維護**：`case_sections.py` · `case_order.py` · `case_lint.py` · `case_upgrade.py` · `case_scan.py` · `case_clean.py` · `case_relabel.py` · `case_digest.py` · `case_gap_fill.py` · `case_play_index.py` · `fix_play_cases.py` · `play_case_candidates.py` · `backlog_cleanup.py`
- **⑦ 倉庫維護**：`file_meta.py` · `rename_tidy.py` · `top_titles.py` · `relocate_06.py`
- **⑧ 共用**：`_common.py`
- **其他**：`agent_brief.py` · `case_upkit.py`

## 五、18 關機械自檢（`selfcheck.py`，任一硬錯誤＝不得交付）

- 【1】文檔結構（完整版九部分；**精煉版可省「現狀分析」**）
- 【1b】與 paradigm_data 骨架對賬（治本：文檔承諾 ≠ 實際生成）
- 【2】交付自檢單
- 【3】內部過程文檔檢查（不得出現在正文；附件區除外）
- 【4】可信度標註
- 【5】動作五要素（啟發式檢查）
- 【6】禁用詞掃描（只掃描非「禁用詞說明」段落）
- 【7】核心方法論要素（打法組合表／問題類型／失敗歸因編號／可抄案例）
- 【8】知識展開度（理論／案例須寫成可執行內容，不得只留編號）
- 【10】交付稿潔淨度（不得出現任何內部座標）
- 【11】場景完整性（大賽／B端／G端／投標 各自必須有的章節）
- 【12】交付自檢單防偽（自評不得高於腳本判定）
- 【13】交叉引用與編號連續性（指向不存在的章節＝硬錯誤）
- 【14】實質與創意判據（執行摘要／AI 腔／場景深度／Big Idea）
- 【15】新增章节实质校验（利益相关者／Red Team／洞察／取舍／回指／渠道）
- 【16】合規紅線與量化可驗（個人信息／文號／偏離值／可證偽／因果／敏感性）
- 【17】承接與危機（私域閉環／客服／超賣退款／輿情發聲人）
- 【18】版权与授权（音乐／字体／肖像／UGC／非遗专利）

## 六、硬約定（**不可違反**）


- **語言**：交付物一律**簡體**；skill 內部文檔一律**繁體**；`references/09–13` 例外（簡體，因內容原樣進交付物）。
- **P0 四條款**（優先於一切默認結構）：① 用戶給的結構 > skill 默認，標題逐字沿用
  ② 用戶大綱要作為**可見章節**存在，不打散 ③ 「自檢通過」≠「交付達成」，用戶的驗收項要另行核對
  ④ 形狀類任務**先交一頁目錄**再寫正文。
- **交付形態紅線**：對外一律簡體 `.docx`；內部過程文檔（事實底稿／分工／裁決記錄）**不得混進交付稿**。
- **出稿唯一入口**：`run_pipeline.py`。拿不到 `.docx` ＝ 沒完成，繞不過。


## 七、已知坑（每一條都是踩過才知道的，別再踩）


1. **新加的硬關，先問「這條要求對最輕的那一檔也成立嗎」** —— 已連續五次誤傷
   `smoke_test` 的速覽類標杆稿。完整版才強制的走 `_hard_if_full()`。
2. **檢測器要能區分「代碼在找這個字符串」與「代碼要訪問這個路徑」** —— 否則會把自己的
   正則、佔位符詞表、檔名索引串當成真問題（「提到 ≠ 讀取」「引用 ≠ 消費」）。
3. **拿真稿調參，不要拿想像調參** —— 「為什麼這麼做」的判據初版誤傷了機制句，
   補了條件推演／對照取捨／推理鏈三類句式才準。
4. **`except: pass` 一律不許** —— 最危險的一處：`verify_all` 哈希讀不到檔就跳過，
   於是「檔怎麼變都測不出漂移」，而報告照樣印「零漂移」。
5. **腳本查了 ≠ 骨架給了位** —— 檢查要求「表＋若干列」而骨架只給 `【填】`，檢查必然空轉。
   三環必須齊：**文檔承諾 → 腳本檢查 → 骨架給位**。
6. **口徑只能在同層比較** —— 「月/年混用」檢查做成整份文檔範圍，把分列清楚的標杆稿誤判了；
   改成按**表塊**判斷才對。
7. **章節編號必須與實際出現順序同向** —— 插了新節不改編號，一致性校驗器會連續報「順序不一致」。
8. **能生成的文檔就不要手寫** —— README 的結構表、這份 BRIEF，都掛在冪等壓測裡自動重生成；
   手寫的必然過時。


## 八、當前狀態

- 最新優化輪次：**R6-20条**（清單見 `优化轮次/`，含未做項與理由）
- 回歸五條（改完任何東西都要跑）：
  ```bash
  python scripts/smoke_test.py          # 12 項冒煙（含標杆稿必須通過）
  python scripts/kb_audit.py            # L1–L7 斷鏈必須全 0
  python scripts/promise_check.py       # 文檔承諾 ↔ 實際執行
  python scripts/optimize_scan.py       # 機械可查的優化點
  python scripts/verify_all.py -n 50    # 全鏈路＋50 遍冪等（約 7 分鐘）
  ```
- ⛔ **`verify_all` 運行期間絕對不要改倉庫檔** —— 它會把「運行中被改動」如實報成哈希漂移，而你會以為腳本非冪等。


## 九、給 agent 的三條規矩

1. **只讀本檔 ＋ 1–2 個目標檔的「那一節」**；需要證據時用 `grep` 定位行號，**不要通讀整份檔**。
2. **每個結論都要帶證據**（檔 ＋ 行號 ＋ 原文片段 ≤40 字）。不許寫「文中多處」。
3. **不許改任何文件**（除非常明確被要求）；湊不滿條數就寫「湊不滿」。

