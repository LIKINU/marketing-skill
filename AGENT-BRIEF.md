# AGENT-BRIEF · 给 agent 的单文件快照

> **为什么有这份**：每个 agent 都是全新上下文。没有这份，它会把整个仓库重读一遍 ——派十几个 agent 就重读十几遍。
> **怎么用**：派 agent 时**只给这一份** ＋ **1–2 个目标档的「那一节」**；需要核对证据时才打开原档，且**只开对应那一段，不要通读**。

> 本档由 `scripts/agent_brief.py` 生成并挂在 `verify_all` 的幂等压测里 —— 改架构忘了更新它会被哈希漂移抓出来。


## 一、这是什么（30 秒）

**营销方案生成器**：输入客户情况 → 输出一份可直接递给客户的 Word 营销方案（现状分析＋策划方案＋预算＋行动清单＋风险）。
**核心设计**：知识由**脚本机械注入**骨架，模型只填 `【填】` —— **知识绕不过去**。
**六档客户**：速览（小客户）／标准（C端品牌）／大赛／B端／G端／投标。


## 二、现在有多大（自动统计）

| 项 | 数量 | 体量 |
|---|---|---|
| `references/` 编号文档 | 14 份 | 849字 |
| `references/cases/` 行业案例 | 52 份 | 3450字 |
| `scripts/` | 48 支（42 支有 CLI） | — |
| 范式库 | 6 档 | 见 `references/12-范式库.md` |
| **合计** | — | **4299字** |

→ **通读一遍＝烧掉大量上下文。用「查」代替「读」**：`python scripts/start_here.py --grep "关键词"`。


## 三、目录结构

```
SKILL.md        唯一入口（§0 门禁 13 项 / §二路由表 / §六自检清单）
AGENTS.md       跨工具接入说明 + 能力不足时怎么降级
README.md       给人看的说明 + §零 仓库地图
AGENT-BRIEF.md  ← 本档（给 agent 的快照）
references/     知识库：00–13 编号文档 + cases/（52 行业）+ 范例/
scripts/        强制层：知识注入 + 校验 + 出稿
优化轮次/       过程记录（不是交付物）
```


## 四、脚本分工（按「什么时候跑」）

- **① 接案**：`flow.py` · `gate_check.py` · `start_here.py`
- **② 组装**：`composer.py` · `build_paradigm.py` · `paradigm_data.py`
- **③ 校验**：`selfcheck.py` · `budget_check.py` · `depth_check.py` · `role_check.py` · `structure_fix.py` · `docx_footnote.py` · `delivery_check.py`
- **④ 出稿**：`run_pipeline.py` · `build_docx.py` · `reformat_to_template.py`
- **⑤ 质量保障**：`verify_all.py` · `kb_audit.py` · `promise_check.py` · `optimize_scan.py` · `smoke_test.py` · `t2s_data.py`
- **⑥ 案例库维护**：`case_sections.py` · `case_order.py` · `case_lint.py` · `case_upgrade.py` · `case_scan.py` · `case_clean.py` · `case_relabel.py` · `case_digest.py` · `case_gap_fill.py` · `case_play_index.py` · `fix_play_cases.py` · `play_case_candidates.py` · `backlog_cleanup.py`
- **⑦ 仓库维护**：`lang_unify.py` · `repo_hygiene.py` · `file_meta.py` · `rename_tidy.py` · `top_titles.py` · `relocate_06.py`
- **⑧ 共用**：`_common.py`
- **其他**：`agent_brief.py` · `case_upkit.py` · `facts_check.py` · `impact.py` · `industry_rules.py` · `tautology_scan.py`

## 五、18 关机械自检（`selfcheck.py`，任一硬错误＝不得交付）

- 【1】文档结构（完整版九部分；**精炼版可省「现状分析」**）
- 【1b】与 paradigm_data 骨架对账（治本：文档承诺 ≠ 实际生成）
- 【2】交付自检单
- 【3】内部过程文档检查（不得出现在正文；附件区除外）
- 【4】可信度标注
- 【5】动作五要素（启发式检查）
- 【6】禁用词扫描（只扫描非「禁用词说明」段落）
- 【7】核心方法论要素（打法组合表／问题类型／失败归因编号／可抄案例）
- 【8】知识展开度（理论／案例须写成可执行内容，不得只留编号）
- 【10】交付稿洁净度（不得出现任何内部座标）
- 【11】场景完整性（大赛／B端／G端／投标 各自必须有的章节）
- 【12】交付自检单防伪（自评不得高于脚本判定）
- 【13】交叉引用与编号连续性（指向不存在的章节＝硬错误）
- 【14】实质与创意判据（执行摘要／AI 腔／场景深度／Big Idea）
- 【15】新增章节实质校验（利益相关者／Red Team／洞察／取舍／回指／渠道）
- 【16】合规红线与量化可验（个人信息／文号／偏离值／可证伪／因果／敏感性）
- 【17】承接与危机（私域闭环／客服／超卖退款／舆情发声人）
- 【18】版权与授权（音乐／字体／肖像／UGC／非遗专利）

## 六、硬约定（**不可违反**）


- **语言**：**全仓统一简体**（知识库／脚本／文档／交付物都是简体；2026-09-18 用户裁定）。
  两个例外必须保留繁体：`t2s_data.py` 的 `T2S_PAIRS`（繁体字表＝侦测判据）、`smoke_test.py` 的繁体负向夹具。
- **P0 四条款**（优先于一切默认结构）：① 用户给的结构 > skill 默认，标题逐字沿用
  ② 用户大纲要作为**可见章节**存在，不打散 ③ 「自检通过」≠「交付达成」，用户的验收项要另行核对
  ④ 形状类任务**先交一页目录**再写正文。
- **交付形态红线**：对外一律简体 `.docx`；内部过程文档（事实底稿／分工／裁决记录）**不得混进交付稿**。
- **出稿唯一入口**：`run_pipeline.py`。拿不到 `.docx` ＝ 没完成，绕不过。


## 七、已知坑（每一条都是踩过才知道的，别再踩）


1. **新加的硬关，先问「这条要求对最轻的那一档也成立吗」** —— 已连续五次误伤
   `smoke_test` 的速览类标杆稿。完整版才强制的走 `_hard_if_full()`。
2. **检测器要能区分「代码在找这个字符串」与「代码要访问这个路径」** —— 否则会把自己的
   正则、占位符词表、文件名索引串当成真问题（「提到 ≠ 读取」「引用 ≠ 消费」）。
3. **拿真稿调参，不要拿想像调参** —— 「为什么这么做」的判据初版误伤了机制句，
   补了条件推演／对照取舍／推理链三类句式才准。
4. **`except: pass` 一律不许** —— 最危险的一处：`verify_all` 哈希读不到档就跳过，
   于是「档怎么变都测不出漂移」，而报告照样印「零漂移」。
5. **脚本查了 ≠ 骨架给了位** —— 检查要求「表＋若干列」而骨架只给 `【填】`，检查必然空转。
   三环必须齐：**文档承诺 → 脚本检查 → 骨架给位**。
6. **口径只能在同层比较** —— 「月/年混用」检查做成整份文档范围，把分列清楚的标杆稿误判了；
   改成按**表块**判断才对。
7. **章节编号必须与实际出现顺序同向** —— 插了新节不改编号，一致性校验器会连续报「顺序不一致」。
8. **能生成的文档就不要手写** —— README 的结构表、这份 BRIEF，都挂在幂等压测里自动重生成；
   手写的必然过时。


## 八、当前状态

- 最新优化轮次：**R6-20条**（清单见 `优化轮次/`，含未做项与理由）
- 回归五条（改完任何东西都要跑）：
  ```bash
  python scripts/smoke_test.py          # 12 项冒烟（含标杆稿必须通过）
  python scripts/kb_audit.py            # L1–L7 断链必须全 0
  python scripts/promise_check.py       # 文档承诺 ↔ 实际执行
  python scripts/optimize_scan.py       # 机械可查的优化点
  python scripts/verify_all.py -n 50    # 全链路＋50 遍幂等（约 7 分钟）
  ```
- ⛔ **`verify_all` 运行期间绝对不要改仓库档** —— 它会把「运行中被改动」如实报成哈希漂移，而你会以为脚本非幂等。


## 九、给 agent 的三条规矩

1. **只读本档 ＋ 1–2 个目标档的「那一节」**；需要证据时用 `grep` 定位行号，**不要通读整份文件**。
2. **每个结论都要带证据**（档 ＋ 行号 ＋ 原文片段 ≤40 字）。不许写「文中多处」。
3. **不许改任何文件**（除非常明确被要求）；凑不满条数就写「凑不满」。

