# 全库质检报告（2026-09-14）

> 审计范围：`marketing-playbook/` 全库（SKILL.md、AGENTS.md、README.md、references/ 全部文件、references/archive/机构档案/、scripts/ 四个脚本）
> 审计方式：逐行阅读 + 全库交叉引用扫描 + 脚本实测（构造 12 组测试输入，含正常/边界/恶意/空洞输入）
> 审计结论只出报告，未改动任何文件。
> 实测环境：macOS / Python 3（本机未安装 `python-docx`，故 `build_docx.py` 的 Word 渲染路径为静态审查 + 拒绝生成路径实测）

---

## 一、总体评估

**这套 skill 的「知识层」健康度明显高于「规范层」，而「规范层」又高于「脚本层」——问题几乎全部集中在「说完之后有没有人真的做得到」这一环。**

- **知识资产扎实**：18 个行业案例集、104 条打法（实测 104，正确）、63 个模型（实测 63，正确）、26 号失败归因库（12 模式 + 34 条预演，实测正确）、31 号小企业手册（55 条零预算打法 + 16 模式，实测正确）都名副其实，且普遍带可信度标注与「查不到的部分」诚实清单——这是全库最值得保留的部分。
- **规范层已经「改过 4 轮」，但每次改都只改了主入口，没回头改旁证文件**：门禁 12→13 项、字数「≥2 万字」→「5,000–10,000 字」、协议 5 条→8 条，这三处改动都留下了 3–5 处旧说法（README、12 号模板、SKILL.md 自己的变更记录）。协议条数在三个文件里分别是 8 / 7 / 5，**用户按 README 数会数出 7 条**。
- **脚本层存在 1 个 P0 级死循环**：`selfcheck.py` 把 SKILL.md 明令必備的「附件 C 事實底稿」当成违规泄漏词拦下，同时要求在 plan.md 里出现「自檢單」字样（而协议 3 要求自检单输出在**聊天回覆**里）——**按规范写的稿子 100% 过不了校验，`build_docx.py` 就永远拒绝生成 `.docx`**。这直接击穿了全库反复宣称的「拿不到 .docx ＝ 没完成」这一唯一强制点。
- **另外三个脚本的正确性也有实质缺陷**：`gate_check.py` 实测能被「没有 / 无 / 暂无 / 不自适用 / - / 0」这类空洞答案骗过 12/13 项；`budget_check.py` 对「項目|單價|數量|小計」四列表、多张预算表、以及「3 萬」这种中文单位——三种最典型的写法全部误判，而 SKILL.md §二 路由表自己就用「5 千–5 萬」。
- **一句话：知识层已经可用，规范层需要一次「一致性收口」，脚本层需要一次「重写 + 与规范对表」。** 在脚本修好之前，建议把协议里的「拿不到 .docx ＝ 没完成」改为「校驗不過須留痕說明」，否则 AI 会被这套工具卡死（而协议 8 又说「强制不得变成卡死」——脚本正在制造协议 8 明令禁止的结果）。

---

## 二、问题清单

### P0 —— 会直接导致执行出错 / 流程卡死

**[P0-1] `selfcheck.py` 把 SKILL.md 明令必備的「事實底稿」判为硬违规 → `build_docx.py` 拒绝生成 → 死循环**
- 位置：`scripts/selfcheck.py:41-44`（`INTERNAL_LEAK_HARD` 含「事實底稿」/「事实底稿」）↔ `SKILL.md:301`（附件 C = 事實底稿，属交付物必備結構）↔ `SKILL.md:52`（自檢單第 3 項「文檔結構完整（八篇＋附件…）」）↔ `SKILL.md:366`（build_docx「內部先跑 selfcheck，不過就拒絕生成 .docx」）
- 实测证据：构造一份**完全符合 SKILL.md §三 規範**的 plan.md（執行摘要 + 八篇 + 附件 C 事實底稿），`python3 scripts/selfcheck.py plan_full.md` → `EXIT=1`，硬錯誤明列 `交付稿混入內部過程字樣：事實底稿`；随后 `build_docx.py` 一律拒绝出稿。
- 影响：**越按规范写，越出不了稿。** 执行 AI 只有两条路：① 删掉附件 C（违反 §三）；② 用 `--force` 跳过（等于废掉唯一强制点）。这是全库最严重的缺陷。
- 建议修法：区分「词」与「语境」——把「事實底稿」从 `INTERNAL_LEAK_HARD` 移出，改为**只在它出现在「第一篇～第八篇正文」范围内才报警**（附件区豁免）；或在 §三 把附件 C 改名（如「附錄 C · 客戶原始情況與約束」）并同步全库。二者择一，必须同步。

**[P0-2] `selfcheck.py` 要求「自檢單」出現在 plan.md 內，但协议 3 要求它輸出在「交付回覆」中 → 按规范做必不过校验**
- 位置：`scripts/selfcheck.py:119-126`（`has_checklist = ("自檢單" in text or "自检单" in text)`，不在 plan.md 里就 `hard_errors.append`）↔ `SKILL.md:46`（「必須**在回覆中**逐項輸出下表」）↔ `SKILL.md:383`（「原樣輸出**在回覆中**」）↔ `scripts/selfcheck.py:216`（脚本自己的收尾提示也写「請把 12 項《交付自檢單》原樣輸出在**交付回覆中**」）
- 实测证据：`plan_full.md`（含八篇 + 附件 C、無自檢單）→ `EXIT=1`，硬錯誤 `未輸出 12 項《交付自檢單》`。
- 影响：与 P0-1 叠加，构成**双重必然失败**。反过来若 AI 为过校验把自检单塞进正文，这份带 12 行 ✅/❌ 的表会被 `build_docx.py` 渲染进**给客户的 Word 交付稿**里，违反协议 5「交付形态红线」（用户要的是作品，不是工作过程）。
- 建议修法：`selfcheck.py` 增加 `--checklist` 参数或 `--reply-checklist` 开关；默认不要求 plan.md 含自检单，改为「要求交一份 `checklist.md`/`checklist.json`」或「由 build_docx.py 从参数读取已输出标记」。同时正文渲染前应剔除自检单区块。

---

### P1 —— 影响质量 / 会误导执行者

**[P1-1] 协议条数三个文件三个数：8 / 7 / 5**
- 位置：
  - `SKILL.md:31-98`：`⚡ 強制執行協議` 实为 **8 条**（協議 1…協議 8）
  - `AGENTS.md:25`：标题写「**五條**強制執行協議」，但 `AGENTS.md:27-36` 表格列了 **8 行**；`AGENTS.md:40` 又写「為什麼有這**五條**」
  - `README.md:23`：标题写「**七條**強制協議」，表格 `README.md:25-33` 只列 **7 條**（缺「協議 8 · 卡死處理」）；`README.md:35` 又写「這**七條**」
  - `SKILL.md:540`：变更记录自称「頂部新增 ⚡ 強制執行協議 **五條**」
- 影响：用户按 README 只能数到 7 条，会以为「卡死處理」不是协议；AGENTS.md 标题与表格自相矛盾（标题 5、表 8），AI 可能只执行前 5 条。
- 建议修法：三处统一为 **8 條**；README 补入「協議 8 · 卡死處理」并把它从 §一點八 的重复叙述改为交叉引用。

**[P1-2] SKILL.md 变更记录里的旧数字没删干净，且被自己的下一行推翻**
- 位置：`SKILL.md:534`（「第 0 步門禁：**12 項**必問清單」）、`SKILL.md:536`（「**正文 ≥ 2 萬字**（比羅森案更詳盡）」）、`SKILL.md:540`（「協議 **五條**」）vs `SKILL.md:541`（同一条记录里写「刪除『正文 ≥2 萬字』硬編碼，改為…5,000–10,000 字」）
- 实测：门禁实数 13 项（`SKILL.md:122`）、字数实数 5,000–10,000（`SKILL.md:225`）、协议实数 8 条。
- 影响：这是典型的「D 类过时内容」——同一页上下两段打架。题目里专门点名的「還寫着照羅森案更詳盡（≥2萬字）」正是 `SKILL.md:536`。
- 建议修法：变更记录是「历史留痕」，应保留原貌但**加删除线并注明「已废止，见第 5 条」**；或统一改写为只记录「当前生效值」。

**[P1-3] 门禁项数在三个文件里 12 / 13 混用**
- 位置：
  - `README.md:112`：「第 0 步 門禁 ──── **12 項**問清」（同文件 `README.md:14`、`README.md:19` 写的是 13 項 → 同文件自相矛盾）
  - `references/12-接案输出模板.md:17`：「SKILL.md §0 的 **12 項**清單全部拿到（賣什麼／賣給誰／規模／卡在哪／預算／資源／時間／硬約束／決策人／對手／試過什麼／怎麼算成功）」——**不仅数错，还把第 13 項「目標字數」整个漏掉**
  - `SKILL.md:534`（见 P1-2）
- 影响：`12-接案输出模板.md` 被 SKILL.md:352 标为「**寫方案時必讀**」，AI 读它就会按 12 项收口，直接漏问「目標字數」——这正是 `26-失败归因总库.md:970` 附录模式 C「門禁漏項」的成因。
- 建议修法：三处统一为 13 项，并让 12 号模板不再复述清单，改为「見 SKILL.md §0 的 13 項清單」。

**[P1-4] 失败模式数：三份文件都说 26 号文件有「16 個失敗模式」，实际只有 12 个**
- 位置：`SKILL.md:349`、`README.md:132`、`AGENTS.md:124` 均写「`references/26-失败归因总库.md` — 16 個失敗模式 + 34 條預演清單」
- 实测：`references/26-失败归因总库.md:21` 自述「失敗模式分類（**12 個**）」，`## 模式 NN` 编号只到 `模式 12`（`:497`）；第 968 行起的附錄另有「模式 A–E」5 条（AI 执行者失败模式），12+5=17。
- 对照：`SKILL.md:350` 说「31 号文件…失敗歸因 **16 模式**」是**正确**的（`31-小企业与新品牌从零打造.md:1238-1399` 实测 `模式 01`–`模式 16`）。说明写索引时把 31 号的数字误抄给了 26 号。
- 建议修法：26 号改为「12 個失敗模式 + 5 個 AI 執行者失敗模式（附錄）+ 34 條預演清單」；34 條实测正确（A6+B5+C5+D5+E5+F5+G3 = 34）。

**[P1-5] 断链引用：归档后路径未同步（共 15 处）**
- 位置（括号内为实际位置）：
  - `references/打法库.md:709` → `references/27-营销书籍与作者库.md`（实际 `references/archive/机构档案/27-营销书籍与作者库.md`）
  - `references/打法库.md:813, 1294, 1320, 1345, 1371, 1449, 1692, 1772, 2202, 2902` → `references/11-甲方in-house打法库.md`（实际在 `references/archive/机构档案/`，**共 10 处**）
  - `references/90-方法论操作手册.md:2914` → `references/11-甲方in-house打法库.md`
  - `references/90-方法论操作手册.md:3901` → `references/23-其他咨询公司.md`
  - `references/90-方法论操作手册.md:4` 与 `:4048` → 「上游：`references/01` ~ `references/31` 各機構文檔」（01–11、13–23、27–30 已全部移入 `archive/机构档案/`）
  - `references/archive/机构档案/30-机构出版物与观点库.md:774` → 「詳見 `SKILL.md` §報價結構」（**SKILL.md 无此节**；实际内容在 `references/12-接案输出模板.md` §四 的「模板 3：報價結構與合約必寫五條」）
- 影响：打法库有 10 条打法的「案例出处」是死链，AI 按图索骥会找不到文件，退化成凭空编造或静默跳过。
- 建议修法：全库检索 `references/(01|02|…|30)-` 批量替换为 `references/archive/机构档案/…`；`SKILL.md §報價結構` 改为 `references/12-接案输出模板.md §四 模板 3`。

**[P1-6] `selfcheck.py` 八篇章节判定用极松关键词，两句话的稿子能过 5 个章节**
- 位置：`scripts/selfcheck.py:28-37`（如「二 · 策略」的命中词包含裸词 `策略`；「三 · 定位與口徑」含 `定位`；「四」含 `渠道`；「五」含 `物料`；「八」含 `風險`）
- 实测：输入只有两句 —— 「本專案涉及策略調整、品牌定位、渠道投放與物料準備，並關注風險。」→ 输出显示 **二/三/四/五/八 共 5 个章节全部 ✅**，只报缺「執行摘要/一/六/七」。
- 影响：脚本宣称的「缺章節 → 不準交付」形同虚设；真正缺内容的稿子只要多写几个名词就能通过。
- 建议修法：改为按**标题行**匹配（`^#{1,3}\s*(一|二|…|八)\s*[·、.]`）并要求标题命中，而非全文子串命中。

**[P1-7] `selfcheck.py` 的「五要素」检查只查了 4 项，且缺的那项恰是最关键的**
- 位置：`scripts/selfcheck.py:161-166`（字典变量名为 `four`，键为 誰做 / 時間 / 花多少 / 怎麼驗收，**无「做什麼」**）↔ `SKILL.md:54`（自檢單第 5 項要求「每條打法/動作**五要素**齊全（做什麼/誰/何時/花多少/怎麼驗收）」）
- 影响：SKILL.md:306 最强调的「禁止寫『做私域運營』這類概括」——即「做什麼」这一要素——恰恰无人校验。这是 P0-1/P0-2 之外的第三处规范—脚本不对表。
- 建议修法：补入「做什麼」检查（可用「動詞+數量+對象」启发式，或改为人工清单 + 脚本只做兜底），并把 `four` 更名为 `five`。

**[P1-8] `gate_check.py` 会被空洞答案骗过（is_filled 白名单过窄）**
- 位置：`scripts/gate_check.py:58-66`（`bad = ["todo","tbd","待補","未知","不清楚","n/a","na","?","？","查不到","未提供"]`）
- 实测：构造 13 项但填入 `没有 / 暂无 / 不适用 / 无 / 不知道呢 / - / 0 / 没做过 / 没想过 / 没定 / 待定 / （空）` → 脚本判定 **12 项已填、仅 1 项缺失**（空字符串那一项）。
- 影响：门禁是全库第一道也是最重要的一道闸（协议 1），却能用一个「无」字骗过。而 SKILL.md:114 明写「不准用『一般情況』代替」——脚本没有能力执行这条。
- 建议修法：`bad` 扩为「空值 + 否定式 + 未定式」三类，至少加入：`无/沒有/没有/暂无/暂无/不适用/不知道/不清楚/没想过/没做过/没定/待定/未定/待确认/TBD/-/—/N/A/0/无数据`；并加一条**最短长度阈值**（如 <4 字直接判为未填，因门禁答案必然长于 4 字）。

**[P1-9] `gate_check.py` 别名子串匹配会张冠李戴**
- 位置：`scripts/gate_check.py:69-78` + `:31-45`
- 实测：输入 `{"目标用户数": "10万"}` 与 `{"待解决问题": "还没想清楚"}`，脚本把前者判为「現在什麼規模 ✅」、后者判为「卡在哪（客戶原話）✅」，而真正的「賣給誰」「卡在哪」全部缺失 → **13 项全绿，EXIT=0**。
- 影响：门禁"通过"不代表问全了，AI 与用户都会误以为齐了。
- 建议修法：改为「精确键名优先，别名匹配降级为人工确认提示」；对同一条目的多个候选命中要求唯一且不冲突；把未匹配字段的多余提示（`:124-126`）从 `ℹ️` 提示升级为**必须人工确认**的阻断项。

**[P1-10] `budget_check.py` 的 Markdown 解析对三种最常见的写法全部误判**
- 位置：`scripts/budget_check.py:52-74`
- 实测证据（三组）：
  1. **四列表**：`| 項目 | 單價 | 數量 | 小計 |` + 合計行 → 脚本只取第 2 格（單價）相加，得出 `分項加總 4,003`（正确应为 9,500），合計行因单元格错位未识别 → **误报「沒有填合計欄位」**
  2. **多张表**：文档同时有「7.1 預算總表」和「7.3 分月支出」两张 `| 項目 | 金額 |` 表 → 脚本把**所有表的所有行**合并相加，得出 `22,000 vs 11,000` → **误报「對不上！差額 +11,000」**（实际上是两张不同口径的表）
  3. **中文单位**：`| 物料 | 3 萬 |` → 正则只剥离 `[¥￥$,\s元]`，不认「萬」→ 解析不到任何行 → **直接报「找不到可解析的預算表」**
- 影响：SKILL.md §二 路由表自己的预算列就写成「5 千–5 萬」「1–10 萬」，而绝大多数中文方案预算表都用「萬」——**这个脚本在真实稿件上的误报率会很高**，而后果是「不準交付」（SKILL.md:364）。用户侧只会看到「卡住了」。
- 建议修法：① 增加「萬/万/千/K/w」单位解析；② 只解析**标题含「預算/總表/合計」的那一张表**，或提供 `--table-index`；③ 表头列名做语义识别（識別「小計/金額/費用」列而非死取第 2 格）；④ 把所有不确定情形降级为 `⚠️ 需人工確認`，不要直接判 `❌ 不通過`。

**[P1-11] `selfcheck.py` 完全不检查「附件 A–D」，而规范把附件列为必備**
- 位置：`scripts/selfcheck.py:28-38`（`SECTIONS` 只有 9 项：執行摘要 + 八篇，**无附件项**）↔ `SKILL.md:298-302`（附件 A 物料文案 / B 話術表 / C 事實底稿 / D 參考案例）↔ `SKILL.md:52`（自檢單第 3 項「八篇＋附件」）
- 实测：构造一份**只有八篇、完全没有附件**的 plan.md → `selfcheck.py` 输出 `✅ 自檢通過（硬錯誤 0 項）`，`EXIT=0`。
- 影响：自检单第 3 项永远靠 AI 自述，脚本给不出任何证据。
- 建议修法：`SECTIONS` 补入附件 A–D 四项（建议 D 项「參考案例」可降为 ⚠️）。

**[P1-12] 自检单第 4 项「字數達標」在四个脚本里都没有对应实现**
- 位置：`SKILL.md:53`（自檢單第 4 項）、`SKILL.md:225`（「以門禁確認的目標字數為準…預設 5,000–10,000 字」）、`SKILL.md:460`（H5 目標字數）↔ `scripts/selfcheck.py`（全文无任何 `len()`/字数统计）、`scripts/build_docx.py`（无字数输出）、`scripts/gate_check.py:44`（虽存了「目標字數」，但不做数值校验，也不传给后续脚本）
- 影响：字数规则是全库改过两次的核心质量门槛（2 萬字 → 5,000–10,000 字），却完全没有任何机械校验；且第 13 项门禁答案与自检第 4 项之间没有数据通路（`gate.json` → `plan.md` 无衔接）。
- 建议修法：`selfcheck.py` 增加 `--target-length 5000-10000`（或从 gate.json 读取），用中文字符计数（排除 Markdown 标记与表格）并输出实测字数；`build_docx.py` 落稿后回报实际字数。

**[P1-13] `范本-罗森案范式.md` 是「自称质量基准线」的孤兒文件——全库无人引用，且内容与现行政策冲突**
- 位置：`references/范本-罗森案范式.md`（1168 行）。全库检索：**仅该文件自身**（`:1` 标题、`:1124` 脚本注释）出现「罗森案范式」，未被 `SKILL.md §四 索引`、`AGENTS.md §五 檔案地圖`、`README.md §五` 任何一处收录。`git status` 显示该文件为未提交状态（`??`）。
- 三重问题：① **孤兒**：SKILL.md:215/219/287/293 反复宣称质量标准「來自用戶認可的『羅森案』那套」，但读者无从找到这份范式文件；② **语言违规**：全文为**簡體中文**，违反 `AGENTS.md:134`「skill 內部文件：繁體中文」；③ **政策冲突**：`范本-罗森案范式.md:29-35` 仍把「每份 ≥400 行 / 四角色稿合计 ≥5,000 行 / 合并稿 3–8%」列为「可直接寫成 skill 硬門檻」，与 `SKILL.md:225`、`SKILL.md:541`「不以字數注水、也不為省字砍硬要素」直接矛盾。
- 影响：这是全库「改了一半」最典型的样本——基准文件没接进索引，也没跟进字数政策。
- 建议修法：三选一 —— ① 接入 SKILL.md §四 索引，同时改繁体、把 §0.2 三条体量纪律明确标注为「**已被 5,000–10,000 字政策取代，僅作歷史實測記錄**」；② 拆成「結構模板」部分并入 `12-接案输出模板.md`，删除重复；③ 移入 `archive/`。**不要让它继续以「基准线」身份躺在索引之外。**

**[P1-14] `00-选流派矩阵与对照表.md` 自称「整套 skill 的入口」，与 SKILL.md 的「唯一入口」定位相冲**
- 位置：`references/00-选流派矩阵与对照表.md:3`（「這份是整套 skill 的入口。**接到任何營銷需求，先來這裏查。**」）↔ `AGENTS.md:13`（「**先讀 `SKILL.md`**，它是唯一入口」）、`AGENTS.md:115`（「`SKILL.md` — **唯一入口**」）、`SKILL.md:105-106`（「任何 Agent 平台都能用…接入方式見 `AGENTS.md`」）↔ `SKILL.md:354` 把它定位为「要判斷某件事該外包還是自己幹」才讀（与「接到任何營銷需求先查」不同）
- 影响：AI 若先读到 00 号，可能先做流派选型再补门禁，与「门禁先行」的强制顺序冲突。这是 F 类「流程路由歧义」。
- 建议修法：把 00 号首句改为「**本檔是 SKILL.md 流程中的『選型』子步驟專用工具**，不是入口；入口是 SKILL.md」，并在 SKILL.md §四 索引里明确它在六步工作流的哪一步（第 2/3 步之间）使用。

**[P1-15] 两条逃逸通道，文档只写了一条**
- 位置：`scripts/build_docx.py:208`（`--skip-check`，help 写「僅內部預覽，不建議」）与 `:209-210`（`--force`，help 写「緊急出口（協議 8）」）；`build_docx.py:214` 把两者**合并为同一个分支**处理
- 引用侧：`SKILL.md:93`（只写 `--force`）、`AGENTS.md:36`（只写 `--force`）、`README.md:63`（只写「跳過這項，先出稿」）
- 影响：AI 可以通过未记载的 `--skip-check` 产出 `.docx`，而且**这个分支不输出「未校驗」的强制留痕语**（`:216-222` 有打印，但措辞与协议 8 的三项要求不完全对齐），等于绕过了协议 8 的「必須聲明 + 列出未通過項 + 不得聲稱已完成」。
- 建议修法：删除 `--skip-check`，只保留 `--force`；或在 `--skip-check` 分支强制写入 `<输出名>.unverified.txt` 留痕文件。

---

### P2 —— 可优化

**[P2-1] `SKILL.md:141`「40+ 條原話問法」实为 37 條**
- 位置：`SKILL.md:141`。实测 §五 各组合计：A5 + B4 + C4 + D5 + E4 + F4 + G5 + H6 = **37 條**（8 組分類正确）。
- 建议：改「40+」为「37」。

**[P2-2] `references/12-接案输出模板.md:1109`「四類 30 條」实为 32 條**
- 位置：`references/12-接案输出模板.md:1109`；实测 `:851-891`：數字類 8 + 執行類 8 + 風險類 8 + 表達類 8 = **32**。
- 建议：改为「32 條」或改为「不寫死條數」。

**[P2-3] 「內容重複」四处，任何一处结构改动都要同步改 4 个文件**
- `SKILL.md:228-303`（八篇 + 附件結構）
- `references/12-接案输出模板.md:27-895`（逐章寫作指南，逐条复述同一套八篇）
- `references/范本-罗森案范式.md:557-780`（模板 1–8：「四段式」「四件套」「三次收窄」「七項模板」「五段式」）
- `SKILL.md:465-497`（§六 自檢清單 16 條）vs `references/12-接案输出模板.md:847-891`（§三 清單 32 條）vs `references/14-多Agent分工简报.md:356-364`（交付前自檢 + 額外 3 條）——**三份清单三套条数，无主从关系**
- 影响：这正是本次审计发现的多处数字失配（P1-3、P1-4、P2-2）的结构性原因。
- 建议：确立唯一权威源 —— 結構以 `SKILL.md §三` 为准，清單以 `SKILL.md §六` 为准，其余文件只**引用不複述**（写「見 SKILL.md §三」）。重复的模板内容合并到一份。

**[P2-4] `SKILL.md §一` 标题写「工作流（六步）」但节内只列了第 1–5 步**
- 位置：`SKILL.md:148`（「## 一、工作流（六步）」）→ `:150-188`（只有 `### 第 1 步` … `### 第 5 步`）。第 0 步门禁另立在 `SKILL.md:112` 的独立章节。
- 对照：`README.md:111-118` 的流程图列了「第 0 步 ～ 第 5 步」共 6 步，是自洽的。
- 建议：`SKILL.md §一` 开头加一行「（含第 0 步門禁，共六步；第 0 步見上一節）」或补一个 `### 第 0 步` 的交叉引用条目。

**[P2-5] 简繁混用**
- `references/范本-罗森案范式.md`：**全文簡體**（内部文件应为繁體，违反 `AGENTS.md:134`）
- `SKILL.md:531-541`（變更記錄段落）：簡體（「机构档案」「用户指出内容主体应是」等）
- `references/12-接案输出模板.md:17`：簡體
- 说明：文件名与路径使用简体（如 `cases/03-美妆个护.md`、`references/打法库.md`）是与实际文件一致，**不算问题**，但**正文**应统一为繁體。
- 建议：以 `AGENTS.md §六` 为准统一；如确实需要简体段落（如引用客户原话），加引号并注明。

**[P2-6] 术语与称呼不统一（E 类）**
- 「交付回覆」/「回覆」/「交付說明」/「交付稿」/「交付物」混用（`SKILL.md:383`、`:92`、`:93`、`:69`）
- 「《任務規則表》」/「規則表」（`SKILL.md:37` 用全稱，`:38`、`:100` 用簡稱）
- 「門禁」/「門禁清單」/「13 項清單」/「13 項門禁清單」
- 「打法」/「動作」（`SKILL.md:305`「每個打法的寫法要求」内又写「每條必須是：做什麼…」）
- 「見效週期」/「见效周期」（`SKILL.md:196` 表头用繁，`:209` 行文用簡「见」）
- 建议：在 SKILL.md 末尾或 AGENTS.md 加一份「术语表」，至少钉死「門禁 / 任務規則表 / 交付自檢單（12 項）/ 自檢清單（16 條）/ 交付稿」五个词。

**[P2-7] 两条「讀取時機」冲突**
- `references/12-接案输出模板.md`：`SKILL.md:352` 说「**寫方案時必讀**」，`README.md:135` 说「**小單／快速溝通**」才讀 → 定位不同
- `references/00-选流派矩阵与对照表.md`：见 P1-14
- 建议：以 SKILL.md §四 索引为唯一权威，README/AGENTS 只做同样描述。

**[P2-8] `build_docx.py` 有死代碼与渲染不一致**
- `scripts/build_docx.py:277`：`tmp_doc = Document()` 建立后**从未使用**（下面用的是 `headings` 列表），浪费一次 docx 初始化，属死代码
- `scripts/build_docx.py:280`：目录抓标题用 `^(#{1,3})`，而正文渲染 `:148` 支持 `#{1,4}`（四級標題渲染为「■ 文字」并 `headings.append((4, text))`）→ **四級標題進了 headings 但目录里看不到**，两处不一致
- 建议：删死代码；目录与正文统一使用 1–4 级（或统一限 1–3 级）。

**[P2-9] `build_docx.py` 字体硬编码且无 fallback**
- 位置：`scripts/build_docx.py:35`（`CN_FONT = "微軟雅黑"`），在 `:41-57` 与 `:252-255` 强制写入 `w:eastAsia`/`ascii`/`hAnsi`
- 影响：非 Windows 环境（本机即 macOS）通常无「微軟雅黑」，生成后 Word 会回退字体，但脚本不做检测也不提示，用户打开才发现排版跑掉。
- 建议：加 `--font` 参数（默认改为跨平台的「Noto Sans CJK / PingFang / 思源黑體」+ 雅黑为候选），并检测系统字体后给出提示。

**[P2-10] 「拿不到 `.docx` ＝ 沒完成」的表述过强**
- 位置：`SKILL.md:368`（「交付物只能由 `build_docx.py` 產出…拿不到 `.docx` ＝ 沒完成，AI 繞不過」）、`AGENTS.md:100`（同义）
- 反例：`build_docx.py:214` 的 `--force` / `--skip-check` 分支可以不经自检产出 `.docx`（SKILL.md:93、AGENTS.md:36 自己承认了 `--force`）。加上 P0-1/P0-2 的死循环，这句话在有代码能力的环境里**既不正确也不可执行**。
- 建议：改为「交付物應由 `build_docx.py` 產出；未通過校驗而出稿的，必須在交付說明中按協議 8 留痕」。真正的强制力应来自 P0-1/P0-2 修好之后的脚本，而非口号。

**[P2-11] 仓库卫生**
- `.DS_Store` 三处实体文件仍在：`./.DS_Store`、`./references/.DS_Store`、`./references/archive/.DS_Store`（`.gitignore` 已忽略，但实体文件会随目录拷贝传播）
- `scripts/*.py` 权限为 `-rw-r--r--`（无执行位），文档里的调用方式全是 `python scripts/xxx.py` 所以不影响使用，但四个脚本都有 `#!/usr/bin/env python3` shebang，权限与 shebang 不一致
- `references/archive/机构档案/打法庫-孤儿半成品.md`（147 行）：**只有 §0 總表、无任何正文打法**，内容与 `references/打法库.md:1-148` 完全重复，是典型的半成品残留
- `references/archive/机构档案/29-小主体从零起盘手册（已合并进31）.md`（738 行）：`references/31-小企业与新品牌从零打造.md:39` 已明说「**不需要再翻原件**」，属「该删未删」
- 建议：删三个 `.DS_Store` 与「打法庫-孤儿半成品.md」；29 号文件加 `_deprecated` 前缀或删除（31 号合并说明已确认内容无损）。

**[P2-12] `references/12-接案输出模板.md` 文件名与内容不符**
- 文件名「接案输出模板」，但 `:1` 的 H1 是「# 方案寫作指南（Word 交付）」，正文主体是「逐章寫作指南 + 通用寫作紀律 + 交付前檢查清單」，附件模板只是 `:893-1100` 的最后一节；`SKILL.md:352` 的索引描述反而是准确的
- 同时该文件 `## 四、附件模板` 与 `SKILL.md ## 四、參考文件索引` 同名不同义，跨文件引用「§四」时容易串
- 建议：改名为 `12-方案写作指南与附件模板.md`（或拆成两份），并在引用时始终带文件名。

---

### F 类 —— 缺口（本该有但没有）

**[F-1] 「樣式對照說明」被引用 5 次，但全库没有给出它的模板或规格**
- 引用处：`SKILL.md:108`、`SKILL.md:188`、`SKILL.md:384`、`AGENTS.md:75`、`AGENTS.md:107`
- 实测：全库检索「樣式對照」只有上述 5 处提及，**没有任何一处说明它由哪几项组成**（只说了「哪一級是標題幾、哪些是表格」）
- 影响：这是「无代码能力环境」的唯一交付降级路径（协议 7 明确要求不能跳步），但没有任何可执行的产出规格 → AI 每次自创，用户每次拿到不同的东西。
- 建议：在 `references/12-接案输出模板.md` 加一节「Markdown → Word 樣式對照表模板」，钉死 8–10 行固定格式（H1→標題1、H2→標題2、`|`表格→Table Grid、引用→內文框線/灰色字、`**粗體**`→粗體、`---`→分頁…）。

**[F-2] 协议 2 的「用户结构 ↔ 八篇映射表」没有模板**
- 位置：`SKILL.md:40-43` 只给了「映射規則」一句话（`SKILL.md:42`），要求「八篇中用戶結構沒有對應位的內容 → 放入附件，不得刪除」，但全库没有映射表的填写模板
- 实测：全库检索「映射表」仅命中 `90-方法论操作手册.md` 里的「平台指標映射表」，与协议 2 无关
- 影响：协议 2 被 SKILL.md:43 称为「本 skill 歷史上最大的翻車點」，却只给了原则没给工具。
- 建议：在 `references/12-接案输出模板.md` 加一张「用戶結構 ↔ 八篇 映射檢核表」（左列：用戶模組；中列：對應八篇章節；右列：無對應位的內容去向＝附件 X），要求交付時一并输出。

**[F-3] 门禁答案与后续脚本之间没有数据通路**
- `gate.json`（含「目標字數」`scripts/gate_check.py:44`）→ `plan.md` → `selfcheck.py` → `build_docx.py` 四步之间不传任何数据；`selfcheck.py`/`build_docx.py` 都不知道目标字数、预算上限、客户名（`build_docx.py:203` 的 `--title` 要手工再填一次）
- 影响：脚本「機械校驗」的能力被限制在纯文本启发式，无法做真正的「规则表 ↔ 成稿对表」。
- 建议：统一为一个 `project.json`（client / gate / rules_table / target_length / budget_total），三个脚本都读它。

**[F-4] 协议 8「同一項校驗連續 2 次不過 → 停止重試」在脚本层无任何支持**
- `SKILL.md:91` 与 `AGENTS.md:36` 都规定了「连续 2 次不过就停下来摊给用户」，但四个脚本都没有状态记录（无 `.retry` 计数文件、无日志），AI 只能靠记忆判断「第几次」
- 建议：脚本在输出目录写一个 `.check_attempts.json`（含检查项 + 时间戳 + 结果），或在输出中直接打印「本項這是第 N 次失敗」。

---

## 三、逐文件体检表

> 状态判定：**良好** = 无 P0/P1 问题；**可用** = 仅 P2 或少量 P1；**待修** = 含 P0/P1 实质问题。

| 文件 | 行数 | 状态 | 主要问题 |
|---|---|---|---|
| `SKILL.md` | 542 | 待修 | P1-1（协议 8 条 vs 自称 5 条）、P1-2（變更記錄 12 項/2 萬字/五條 三处过时）、P1-4（26 号说 16 模式实为 12）、P1-15（`--skip-check` 未記載）、P2-1（40+ 實為 37）、P2-4（六步只列 5 步）、P2-5（變更記錄簡繁混用）、P2-10（「拿不到 docx＝沒完成」過強） |
| `AGENTS.md` | 227 | 待修 | P1-1（§2.2 標題「五條」但表列 8 條、§2.3 又寫「這五條」）、P1-4（16 模式）、P1-15（只寫 `--force`）、F-1（引用樣式對照說明） |
| `README.md` | 171 | 待修 | P1-1（「七條」且表缺協議 8）、P1-3（`:112` 寫 12 項，同檔 `:14/:19` 寫 13 項）、P1-4（16 模式）、P2-7（12 号定位與 SKILL 衝突） |
| `scripts/gate_check.py` | 161 | 待修 | P1-8（空洞答案騙過 12/13 項）、P1-9（別名張冠李戴）、F-3（不向後傳資料）※正常輸入路徑正確，13 項齊備時行為無誤 |
| `scripts/budget_check.py` | 188 | 待修 | P1-10（四列表／多表／「萬」單位 三種誤判）、F-3。JSON 輸入路徑邏輯正確（含單位經濟與保本單量） |
| `scripts/selfcheck.py` | 232 | **P0** | **P0-1（誤殺「事實底稿」）、P0-2（自檢單須在 plan.md）**、P1-6（關鍵詞過鬆，5/9 章節誤通過）、P1-7（五要素只查 4 項）、P1-11（不檢附件 A–D）、P1-12（無字數校驗）、F-4 |
| `scripts/build_docx.py` | 317 | 待修 | 繼承 P0-1/P0-2（前置自檢必掛）；P2-8（死代碼 `:277`、目錄漏 4 級標題）、P2-9（字體硬編碼微軟雅黑無 fallback）、P1-15（`--skip-check`） |
| `references/打法库.md` | 2936 | 待修 | **P1-5（11 處斷鏈：`:709` 的 27 号、`:813/:1294/:1320/:1345/:1371/:1449/:1692/:1772/:2202/:2902` 的 11 号）**。條目數 104 ✓ 正確、§0 總表 ✓、§9 診斷類常駐規則 ✓ |
| `references/90-方法论操作手册.md` | 4052 | 待修 | P1-5（`:2914` 引 11 号、`:3901` 引 23 号、`:4`/`:4048` 引 `references/01`~`31`）。63 個模型 ✓ 正確、附錄一 63 行 ✓、附錄三 12 條待核實 ✓、七段結構 ✓ |
| `references/31-小企业与新品牌从零打造.md` | 1605 | 良好 | 55 條零預算打法 ✓、16 模式 ✓、「查不到的部分」22 條 ✓。僅合併說明提到原 29 号（搭配 P2-11 建議刪 29 号） |
| `references/24-本土数字营销与MCN.md` | 1322 | 良好 | AIPL／FAST／5A／DEEPLINK 齊備（`SKILL.md:351` 引用正確）。未見 P0/P1 |
| `references/26-失败归因总库.md` | 990 | 良好 | 12 模式 + 34 條預演 + 13 個翻車檔案 + 附錄 5 條 ✓。**內容正確，是索引寫錯（P1-4）** |
| `references/12-接案输出模板.md` | 1116 | 待修 | **P1-3（`:17` 寫 12 項且漏「目標字數」）**、P2-2（30 條實為 32）、P2-5（`:17` 簡體）、P2-12（檔名與內容不符）、P2-7（讀取時機衝突）、F-1/F-2（樣式對照、映射表模板缺口） |
| `references/14-多Agent分工简报.md` | 383 | 良好 | §二/§三/§四 齊備且與 `SKILL.md:163/182` 引用對得上。清單與 §六 的重複見 P2-3 |
| `references/00-选流派矩阵与对照表.md` | 154 | 待修 | P1-14（自稱「整套 skill 的入口」與 SKILL.md「唯一入口」衝突） |
| `references/范本-罗森案范式.md` | 1168 | 待修 | **P1-13（孤兒、全文簡體、§0.2 體量門檻與 5,000–10,000 字政策衝突）**、P2-3（模板 1–8 與 12 号重複）。體量數字本身自洽 ✓（7 份角色稿 9,064 行、含底稿與合併稿 9,604 行，均驗算正確） |
| `references/cases/01–18-*.md`（18 檔案） | 542–717 | 良好 | 18 個行業全齊；H1 命名統一為「{行業} · 營銷案例集」；首節「案例清單」體例一致（每條寫「做了什麼／結果／可抄的點」）；可信度標註與「服務方查不到」的誠實寫法一致。**未發現 P0/P1** |
| `references/archive/机构档案/`（28 檔案） | 61–944 | 可用 | P1-5（`30-…md:774` 引 `SKILL.md §報價結構` 不存在）、P2-11（`打法庫-孤儿半成品.md` 147 行應刪；`29-…（已合并进31）.md` 738 行該刪未刪）。其餘 26 個檔案作為歸檔合理保留；`23`／`27`／`28`／`30` 仍被主庫交叉引用（見 P1-5） |

---

## 四、脚本专项检查

### 4.1 实测总表（12 组输入）

| 编号 | 输入 | 期望 | 实测结果 | 判定 |
|---|---|---|---|---|
| T1 | gate.json：用 SKILL.md 的 13 个标准 label 作 key、值正常 | 通过 | `EXIT=0` ✅ | 正常 |
| T2 | gate.json：用客户口语 key（卖什么/卖给谁/卡点/预算…） | 通过 | `EXIT=0` ✅ 全部命中 | 别名机制本身可用 |
| T3 | gate.json：`目标用户数` 冒充「規模」、`待解决问题` 冒充「卡在哪」 | 应报缺「賣給誰」「卡在哪」 | `EXIT=0`，13 项全绿 | **❌ P1-9** |
| T4 | gate.json：13 项填入 `没有/暂无/不适用/无/-/0/没做过/没想过/没定/待定`（仅 1 项真空） | 应大面积报缺 | 报仅缺 1 项 | **❌ P1-8** |
| T5 | gate.json：只给 12 项（缺「目標字數」） | 应报缺 | 正确报缺「目標字數」 | 正常 |
| T6 | 空文件 / 不存在 / 非 JSON / gate 是数组 / 无参数 | 清晰报错 | 四种均给出可读报错 `EXIT=1` | 正常（**但 `--help` 未实现**，被当作文件名报「No such file」，而 `:159` 的错误提示又叫用户「用 --help 看输入范例」→ 自相矛盾，P2） |
| T7 | budget：空 md / 无表格 md / 不存在 | 清晰报错 | 均 `EXIT=1` 且提示改用 JSON | 正常 |
| T8 | plan.md：完全符合 §三 規範（八篇 + 附件 C 事實底稿） | 应通过 | `EXIT=1`，硬錯誤 2 項 | **❌ P0-1 / P0-2** |
| T9 | build_docx：以 T8 的 plan.md 出稿 | 应先跑自检 | 本机缺 `python-docx`，`EXIT=1` 并提示安装（错误处理正确）；静态审查确认自检不过时会 `EXIT=1` 拒绝生成 | 前置逻辑正确，但被 T8 拖死 |
| T10-a | budget md：`| 項目 | 單價 | 數量 | 小計 |` 四列表 | 加總 9,500 | 得出 4,003 且报「沒有填合計欄位」 | **❌ P1-10** |
| T10-b | budget md：预算总表 + 分月表两张表 | 只校验总表 | 合并相加 22,000，误报「差額 +11,000」 | **❌ P1-10** |
| T10-c | budget md：金额用「3 萬」 | 解析为 30000 | 报「找不到可解析的預算表」 | **❌ P1-10** |
| T11 | plan.md：只有两句名词（策略/定位/渠道/物料/風險），无实际章节 | 应报缺全部章节 | 5/9 章节判为 ✅ 通过 | **❌ P1-6** |
| T12 | plan.md：八篇齐 + 12 行自检单 ✅，**完全无附件** | 应报缺附件 | `EXIT=0` 通過 | **❌ P1-11** |

### 4.2 逻辑漏洞与误报风险汇总

| 脚本 | 问题 | 位置 | 风险等级 |
|---|---|---|---|
| gate_check | 空洞/否定式答案被判为已填 | `:58-66` | 高（可用一个「无」字过门禁） |
| gate_check | 别名子串匹配抢键（规模↔用户数、卡在哪↔问题） | `:69-78`、`:31-45` | 高（假通过） |
| gate_check | 未匹配的多余字段只给 `ℹ️` 提示，不阻断 | `:124-126` | 中 |
| gate_check | `--help` 未实现却在错误提示里推荐 | `:159` | 低（但会误导） |
| budget_check | 金额只取第 2 格（不支持單價/數量/小計列） | `:63-73` | 高 |
| budget_check | 不同表格行被合并相加 | `:52-74` | 高 |
| budget_check | 不认「萬/万/千/K/w」单位 | `:66` | 高（中文方案高发） |
| budget_check | 单位毛利 ≤0 判为硬错误 `failed=True` | `:161-162` | 中（有的商业模式本就靠规模摊薄固定成本，应降级为 ⚠️） |
| budget_check | 无 `total` 直接 `failed=True` | `:120-123` | 中（Markdown 多表场景下会误判） |
| selfcheck | 「事實底稿」硬拦截（与 §三 冲突） | `:41-44` | **致命** |
| selfcheck | 自检单必须在 plan.md 内 | `:119-126` | **致命** |
| selfcheck | 章节关键词过松 | `:28-37` | 高 |
| selfcheck | 五要素只查 4 项，缺「做什麼」 | `:161-166` | 高 |
| selfcheck | 不检查附件 A–D | `:28-38` | 中 |
| selfcheck | 无字数校验 | 全文 | 中 |
| selfcheck | 禁用词白名单只覆盖「保本/回本」两种语境，其他财务术语（如「最優」「極致」在财务/评价语境）仍会误报 | `:65-68` | 低 |
| selfcheck | 禁用词段落的「豁免窗」为前后 ±2 行，表格化禁用词表（每行一个词）会被整片豁免 → **漏报** | `:178-184` | 中（真实方案若用表格列禁用词表，正文再出现这些词也不会被发现） |
| build_docx | 死代码 + 目录与正文标题层级不一致 | `:277`、`:280` vs `:148` | 低 |
| build_docx | 字体硬编码「微軟雅黑」无 fallback | `:35` | 中（非 Windows 环境排版跑掉） |
| build_docx | 两条逃逸通道，`--skip-check` 未在文档记载 | `:208`/`:209`/`:214` | 中 |
| 全部 | 四个脚本之间无数据传递（gate → selfcheck → build 各读各的） | — | 中（限制了机械校验的深度） |
| 全部 | 无「同一项连续 2 次不过」的状态记录，协议 8 无法落地 | — | 中 |

### 4.3 脚本与 SKILL.md 一致性对表

| SKILL.md 的承诺 | 位置 | 脚本是否兑现 |
|---|---|---|
| 門禁 13 項全部拿到才放行 | `SKILL.md:122`、`:363` | 部分兑现（T1/T5 正确；T3/T4 假通过） |
| 預算分項加總＝合計 | `SKILL.md:56`、`:364` | JSON 路径兑现；Markdown 路径**三种常见写法全部误判** |
| 缺章節／缺自檢單／內部文檔洩漏 → 不準交付 | `SKILL.md:365` | 三条都有，但**第 1 条过松、第 2 条位置错、第 3 条误杀必备附件** |
| 每條動作五要素齊全 | `SKILL.md:54`、`:307` | **只查 4 項，缺最关键的「做什麼」** |
| 字數達標 | `SKILL.md:53`、`:225` | **完全未实现** |
| 文檔結構完整（八篇＋附件） | `SKILL.md:52` | 八篇有（过松），**附件完全没有** |
| 對不同人說什麼／禁用詞口徑 | `SKILL.md:259-260` | 只在禁用词层面部分覆盖；口径分层未校验 |
| 內部過程文檔不得洩入交付稿 | `SKILL.md:69`、`:60` | 有，但**误杀 §三 规定的附件 C** |
| 拿不到 `.docx` ＝ 沒完成 | `SKILL.md:368` | **不成立**（`--force`/`--skip-check` 可绕；且 P0 使正常路径也拿不到） |
| 同一項連續 2 次不過 → 停止重試 | `SKILL.md:91` | **无任何实现** |

### 4.4 闭环结论

> SKILL.md:368 宣称「交付物只能由 build_docx.py 產出，而它必跑自檢 —— 拿不到 .docx ＝ 沒完成，AI 繞不過」。

**实测结论：这个闭环目前是断的，而且是「逆向断」——正常流程走不通，只能靠 `--force` 绕。**
- 按 §三 规范写 → 含「事實底稿」→ 被 selfcheck 判违规（P0-1）
- 按协议 3 把自检单放在回覆里 → plan.md 里没有「自檢單」→ 被 selfcheck 判违规（P0-2）
- 两条硬错误同时触发 → `build_docx.py:230-235` 拒绝生成 → 用户拿不到 `.docx`
- 唯一出路是协议 8 的 `--force` —— 而这恰好是「拿不到 .docx ＝ 沒完成」这句口号要防的行为

**因此 P0-1 与 P0-2 必须最优先修，否则整套脚本强制层不但无效，还会稳定地把合规交付卡死（正好落在协议 8 开篇「一個把人卡死的流程，比一個寬鬆的流程更糟」所禁止的情形上）。**

---

## 五、建议的修复优先级

**第 0 批（当天，只改脚本判据，不动内容 —— 解开死循环）**
1. P0-1：`selfcheck.py:41-44` 把「事實底稿/事实底稿」从 `INTERNAL_LEAK_HARD` 移出（或改为「仅在附件區外出現才報」）。
2. P0-2：`selfcheck.py:119-126` 把「必須含自檢單」从硬错误降为 `⚠️ 警告`，或新增 `--checklist-file` 参数由交付回覆另行提供。
3. 修完后立刻回归测试：T8 的 plan.md 必须 `EXIT=0`，`build_docx.py` 必须能出稿。

**第 1 批（一致性收口 —— 纯文字替换，风险最低、收益最高）**
4. P1-1：三处协议条数统一为 **8 條**（README 补入協議 8、AGENTS §2.2/§2.3 改「八條」、SKILL.md:540 改「八條」）。
5. P1-3：门禁项数统一为 **13 項**（`README.md:112`；`references/12-接案输出模板.md:17` 补「目標字數」并改为引用而非复述）。
6. P1-2：`SKILL.md:534/536/540` 三处旧数字加删除线并注明已废止。
7. P1-4：`SKILL.md:349`、`README.md:132`、`AGENTS.md:124` 的「16 個失敗模式」改为「12 個失敗模式 + 5 個 AI 執行者模式（附錄）」。
8. P1-5：全库把 `references/(01|02|…|23|27|28|30)-*.md` 批量替换为 `references/archive/机构档案/…`；`30-…md:774` 的 `SKILL.md §報價結構` 改为 `references/12-接案输出模板.md §四 模板 3`。
9. P2-1/P2-2：`40+ → 37`、`30 條 → 32 條`。

**第 2 批（脚本正确性 —— 决定「差方案能不能被拦住」）**
10. P1-8：`gate_check.py:58-66` 扩充 `bad` 清单 + 最短长度阈值。
11. P1-9：`gate_check.py:69-78` 改为精确优先、冲突即阻断。
12. P1-10：`budget_check.py:52-74` 支持「萬/千/K/w」、按表选择、按列名语义识别，并把不确定情形降级为 ⚠️。
13. P1-6/P1-7/P1-11/P1-12：`selfcheck.py` 章节按标题行匹配、补「做什麼」、补附件 A–D、补字数校验（含 `--target-length`）。
14. P2-8/P2-9/P1-15：`build_docx.py` 删死代码、目录统一 1–4 级、字体可配、删 `--skip-check`。

**第 3 批（结构与内容收敛）**
15. P1-13：`范本-罗森案范式.md` 决定去留 —— 建议**接入 SKILL.md §四 索引 + 改繁體 + 把 §0.2 标注为已被 5,000–10,000 字政策取代**（它是全库唯一把「好方案长什么样」量化下来的文件，删掉可惜）。
16. P1-14：`00-选流派矩阵与对照表.md:3` 改为「SKILL.md 的子步驟工具」。
17. F-1/F-2：补「樣式對照說明」模板与「用戶結構 ↔ 八篇映射檢核表」两份模板。
18. P2-3：确立唯一权威源（結構＝SKILL §三、清單＝SKILL §六），其余文件改为引用不複述。
19. P2-11：删 `.DS_Store`×3、删 `打法庫-孤儿半成品.md`、处理 `29-…（已合并进31）.md`。
20. F-3/F-4：引入统一 `project.json` 打通脚本数据流；脚本记录重试次数以支撑协议 8。

**第 4 批（体验层）**
21. P2-4/P2-5/P2-6/P2-7/P2-12：六步标题、简繁统一、术语表、读取时机、12 号文件改名。
22. P2-10：把「拿不到 .docx ＝ 沒完成」改为与协议 8 相容的表述。

---

## 六、查不到 / 无法判断的部分

1. **`AGENTS.md §九` 的 hook 配置是否可用**（`:183-221`）——文档给出 `"type": "prompt"`、`"continueOnBlock"`、`"timeout"` 三个字段与 `~/.workbuddy/settings.json` 的 `hooks` 结构。本次仅做静态审查，**未在 WorkBuddy / Claude Code 上实际加装验证**，无法确认字段名、事件名（`Stop` / `UserPromptSubmit`）与 `/hooks` 面板说法是否与当前版本一致。文档自称「WorkBuddy 與 Claude Code 同構，27+ 事件」，亦无法本机核实。
2. **各案例的【已核實】标注是否真的对得上公开来源**——18 个案例集共约 10,900 行，含大量「【已核實：某某財報／研報】」标注。逐条联网核查（尤其蜜雪 97 億次曝光、雪王話題 195 億次、泡泡瑪特會員 7,258 萬、瑞幸私域 3,000 萬企微用戶等）远超本次审计范围。**注意 31 号文件 `:1576-1605` 的「查不到的部分」22 條自己已列出多处数字存疑（如瑞幸私域数据「財報未單列此口徑」），建议以此为线索抽查。**
3. **`范本-罗森案范式.md` 引用的 9 份原始產出无法核對**——其数据来源写为 `$HOME/WorkBuddy/2026-08-29-02-45-24/deliverables/marketing-campaign/`（`范本-罗森案范式.md:5`），本机不存在该路径。因此 §0.1 的体量数字（169/1105/1932/685/1552/482/2069/1239/371 行）**只能在文件内部做算术自洽性校验（已验证自洽：7 份角色稿 9,064 行、含底稿与合并稿 9,604 行、合并稿占比 4.1% 均正确）**，无法与原始文件比对。
4. **`references/12-接案输出模板.md` 当前为未提交修改状态**（`git status` 显示 ` M`）。本次审计基于**工作区当前内容**，若该文件正处于编辑中，P1-3（`:17` 的 12 項）等问题可能是编辑中间态。同理 `SKILL.md` 亦为 ` M`。
5. **`references/archive/机构档案/` 内 26 个机构档案的「该留未留」判断**——任务只要求检查「该删未删/该留未留」。本次判定了「该删未删」2 项（`打法庫-孤儿半成品.md`、`29-…（已合并进31）.md`），但**各机构档案内部是否存在内容重复、过时（如 2025–2026 年的集团并购变动）未逐份细读**，属未覆盖范围。
6. **`cases/` 18 个文件的内容质量**——已核对结构一致性（H1 命名、首节「案例清单」体例、可信度标注）均为良好，但**每个文件 542–717 行内的具体案例事实未逐条核验**。
7. **协议 1 的「《任務規則表》」格式未定义**——SKILL.md、AGENTS.md、README 三处都提到要产出《任務規則表》，但**全库没有给出这张表的字段定义**（只在 `SKILL.md:37` 描述为「13 項門禁答案＋交付結構＋紅線＋工具路徑」）。这算 F 类缺口（未列入正式清单，因为它已有一句描述），但也可能是有意留给 AI 发挥——**判为「需确认是否为刻意留白」**。

---

*報告結束 · 本次審計未修改任何文件 · 所有實測記錄位於 `/tmp/sctest/`（含 12 組測試輸入與輸出）*
